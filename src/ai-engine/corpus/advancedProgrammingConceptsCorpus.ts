import { KnowledgeItem } from '../../types';

// Code knowledge expansion (2026-09-14) — more advanced/practical programming concepts:
// concurrency fundamentals, SOLID principles, dynamic programming, algorithmic techniques,
// dev tooling (Docker, CI/CD, package managers), and common API/web concepts (auth, GraphQL,
// WebSockets). Plain neutral educational content, language-agnostic where possible.

const now = Date.now();
const k = (id: string, title: string, keywords: string[], content: string): KnowledgeItem => ({
  id,
  title,
  category: 'Programming',
  keywords,
  content,
  createdAt: now,
});

export const ADVANCED_PROGRAMMING_CONCEPTS_CORPUS: KnowledgeItem[] = [
  k(
    'kb-code-concurrency-threads-processes',
    'Threads vs processes',
    ['threads vs processes', 'process vs thread difference'],
    "A process is an independent running instance of a program, with its own private memory space that other processes can't directly access — the operating system isolates processes from each other by default, which is safer but means processes need explicit mechanisms (pipes, sockets, shared memory) to communicate. A thread is a smaller unit of execution WITHIN a process, and multiple threads in the same process SHARE that process's memory space directly, including its variables and data structures, which makes communication between threads trivially easy (they can just read/write the same variables) but also introduces the risk of one thread's changes unexpectedly affecting another thread's view of shared data if not carefully coordinated. Creating a new process is generally more expensive (more memory, more OS overhead) than creating a new thread, since a thread reuses most of its parent process's existing resources. Multi-process designs (like separate microservices, or Python's `multiprocessing` module working around the GIL) trade some communication overhead for stronger isolation and fault containment — a crash in one process typically doesn't directly crash another, unlike a crash in one thread which can bring down the entire shared process.",
  ),
  k(
    'kb-code-concurrency-race-conditions',
    'Race conditions',
    ['race condition programming', 'what is a race condition'],
    "A race condition occurs when multiple threads (or processes) access and modify shared data concurrently, and the final outcome depends unpredictably on the exact timing/order in which they happen to execute — a bug that may only appear intermittently, making race conditions notoriously hard to reproduce and debug. A classic example: two threads both read a shared counter's current value (say, 5), both independently compute \"5 + 1 = 6\" in their own local state, and both write 6 back — the counter should have ended at 7 after two increments, but ended at 6 because neither thread saw the other's update before computing its own. The fix generally involves synchronization: a mutex (mutual exclusion lock) ensures only one thread can execute a critical section of code (like the read-modify-write sequence above) at a time, forcing other threads to wait their turn rather than interleaving unpredictably. Many languages also provide atomic operations (like an atomic increment) for simple cases, which the hardware itself guarantees complete as one indivisible step, avoiding the need for a full lock for that specific single operation.",
  ),
  k(
    'kb-code-concurrency-deadlocks',
    'Deadlocks',
    ['deadlock programming', 'what is a deadlock'],
    "A deadlock happens when two or more threads (or processes) are each waiting for a resource the other one holds, and neither can proceed — resulting in both being stuck forever, since neither will ever release what the other needs. A classic scenario: Thread A locks Resource 1 and then tries to lock Resource 2, while Thread B simultaneously locks Resource 2 and then tries to lock Resource 1 — each thread is now waiting on a lock the other already holds, and neither will ever release its own lock since it's stuck waiting. Four conditions must all hold simultaneously for a deadlock to be possible: mutual exclusion (a resource can only be held by one thread at a time), hold-and-wait (a thread holds one resource while waiting for another), no preemption (a resource can't be forcibly taken away from a thread holding it), and circular wait (a cycle of threads each waiting on the next). The most common practical prevention strategy is enforcing a consistent global ORDER in which locks are always acquired across the whole codebase (e.g. always lock Resource 1 before Resource 2, everywhere, never the reverse), which breaks the circular-wait condition and makes deadlock structurally impossible for that specific set of locks.",
  ),
  k(
    'kb-code-solid-principles',
    'SOLID principles overview',
    ['solid principles', 'solid design principles'],
    "SOLID is an acronym for five object-oriented design principles intended to make software more maintainable, flexible, and understandable. Single Responsibility Principle: a class should have only one reason to change — one clearly defined responsibility, not several unrelated ones bundled together. Open/Closed Principle: code should be open for extension but closed for modification — new behavior should ideally be addable without editing and risking breaking existing, already-tested code, often achieved through interfaces/polymorphism rather than modifying a big conditional inside an existing class. Liskov Substitution Principle: a subclass should be usable anywhere its parent class is expected without breaking the program's correctness — if substituting a subclass instance changes expected behavior in a surprising way, the inheritance relationship is likely modeled incorrectly. Interface Segregation Principle: many small, specific interfaces are better than one large, general-purpose one — a class shouldn't be forced to implement methods it doesn't actually need just because they're bundled into a fat interface. Dependency Inversion Principle: high-level code should depend on abstractions (interfaces), not on concrete low-level implementation details directly, making it easier to swap implementations (like a database) without changing the code that depends on it.",
  ),
  k(
    'kb-code-dependency-injection',
    'Dependency injection',
    ['dependency injection', 'what is dependency injection'],
    "Dependency injection is a design technique where an object's dependencies (other objects or services it needs to do its job) are provided to it from the OUTSIDE, typically via its constructor or a setter method, rather than the object creating those dependencies itself internally. Without dependency injection: `class OrderService {\\n    constructor() { this.db = new PostgresDatabase(); }\\n}` — `OrderService` is tightly coupled to a specific concrete database implementation baked directly into its own code. With dependency injection: `class OrderService {\\n    constructor(db) { this.db = db; }\\n}`, and calling code decides which concrete database to actually pass in: `new OrderService(new PostgresDatabase())` in production, or `new OrderService(new FakeDatabase())` in a test — `OrderService` itself only needs to know about a general database INTERFACE, not any specific implementation. This makes unit testing dramatically easier (swap in a fake/mock dependency without touching the class under test) and makes the codebase more flexible to changing implementations later. Many frameworks (Spring for Java, Angular for TypeScript) provide a dedicated dependency injection \"container\" that automatically constructs and wires together an entire object graph based on declared dependencies.",
  ),
  k(
    'kb-code-dynamic-programming',
    'Dynamic programming basics',
    ['dynamic programming', 'memoization vs tabulation'],
    "Dynamic programming (DP) is an algorithmic technique for solving problems by breaking them into overlapping subproblems, solving each distinct subproblem only ONCE, and reusing (rather than recomputing) that stored result whenever the same subproblem comes up again — this specifically targets problems where naive recursion would otherwise recompute identical subproblems exponentially many times. Fibonacci is the classic teaching example: naive recursive `fib(n) = fib(n-1) + fib(n-2)` recomputes the same smaller values over and over, giving O(2ⁿ) time, whereas storing each `fib(k)` result the first time it's computed (memoization) reduces it to O(n). There are two common implementation styles: \"top-down\" memoization, which is essentially normal recursion plus a cache (often a dict/hash map) checked at the start of each call, and \"bottom-up\" tabulation, which instead builds up an array of results iteratively from the smallest subproblems to the largest, avoiding recursive call overhead entirely. Recognizing a DP-solvable problem usually comes down to two properties: \"overlapping subproblems\" (the same smaller computation is genuinely needed multiple times) and \"optimal substructure\" (an optimal solution to the whole problem can be constructed directly from optimal solutions to its subproblems).",
  ),
  k(
    'kb-code-greedy-algorithms',
    'Greedy algorithms',
    ['greedy algorithm', 'what is a greedy algorithm'],
    "A greedy algorithm builds up a solution step by step, at each step making whichever choice looks locally best RIGHT NOW, without reconsidering that choice later or looking ahead to see how it affects future steps. This makes greedy algorithms typically simple and fast to implement, but they only produce a globally optimal (truly best possible) solution for certain problems where making the locally best choice at every step is provably guaranteed to still lead to the overall best outcome — for many other problems, a greedy approach produces a solution that's merely good/reasonable, not actually optimal, because an early locally-best choice can foreclose a better overall solution reachable through a different, less obviously appealing early choice. A classic example where greedy DOES work optimally: making change with standard coin denominations (like US coins) by always picking the largest coin that doesn't exceed the remaining amount — this happens to always produce the fewest coins for that particular set of denominations, though it would NOT be optimal for an arbitrary or adversarially-chosen set of coin denominations. Dijkstra's shortest-path algorithm is another well-known example of a greedy algorithm that IS provably optimal for its specific problem (finding shortest paths in a graph with non-negative edge weights).",
  ),
  k(
    'kb-code-two-pointer-sliding-window',
    'Two-pointer and sliding window techniques',
    ['two pointer technique', 'sliding window algorithm'],
    "The two-pointer technique uses two index variables moving through a data structure (usually a sorted array or a string) to solve a problem in a single linear pass, instead of the naive O(n²) approach of checking every possible pair with nested loops. A common pattern: to find a pair of numbers in a SORTED array summing to a target value, start one pointer at the beginning and one at the end; if the current pair's sum is too small, move the left pointer forward (increasing the sum); if too large, move the right pointer backward (decreasing the sum) — this converges on an answer in O(n) time rather than O(n²), specifically exploiting the fact the array is sorted. The sliding window technique is a related idea, typically used for finding a contiguous subarray or substring satisfying some condition (like the longest substring without repeating characters): instead of re-examining every possible subarray from scratch, a \"window\" defined by a start and end pointer expands and contracts as it slides across the data, incrementally updating a running result rather than recomputing it from zero each time the window moves — this again typically reduces an O(n²) or worse brute-force approach down to O(n).",
  ),
  k(
    'kb-code-docker-basics',
    'Docker basics',
    ['docker basics', 'what is docker', 'containers vs virtual machines'],
    "Docker packages an application together with everything it needs to run (code, runtime, system libraries, configuration) into a single, portable unit called a container, ensuring it behaves consistently across different machines — solving the classic \"it works on my machine\" problem where an app runs fine in development but breaks in a different environment due to subtle differences in installed dependencies or configuration. A Dockerfile is a text file with step-by-step instructions for building a container image: `FROM node:18` (start from a base image with Node.js pre-installed), `COPY . /app` (copy the project's files in), `RUN npm install` (install dependencies as part of building the image), `CMD [\"node\", \"server.js\"]` (the command to run when a container starts from this image). `docker build` creates an image from a Dockerfile; `docker run` starts a new container from an image. Containers differ from full virtual machines in that they share the host machine's OS kernel directly rather than each running their own separate full guest operating system, which makes containers dramatically more lightweight and faster to start than a comparable VM, at the cost of slightly weaker isolation (a full VM's isolation boundary is stronger since it doesn't share a kernel with the host at all).",
  ),
  k(
    'kb-code-ci-cd-basics',
    'CI/CD basics',
    ['ci cd basics', 'continuous integration continuous deployment'],
    "CI/CD stands for Continuous Integration and Continuous Delivery/Deployment, an automated pipeline for building, testing, and shipping code changes reliably and frequently instead of manually and rarely. Continuous Integration means every code change (typically each push or pull request) automatically triggers a build and a full test suite run on a shared server, catching integration problems and regressions quickly — soon after they're introduced, while the relevant context is still fresh — rather than discovering them much later after many unrelated changes have piled up on top, making the actual cause much harder to isolate. Continuous Delivery extends this by automatically preparing every passing change to be release-ready (often building a deployable artifact automatically), while still requiring a manual approval step to actually deploy it to production. Continuous Deployment goes one step further and automatically deploys every change that passes all its automated checks straight to production with no manual gate at all, requiring a very high degree of confidence in the automated test suite's ability to catch real problems before they reach users. Common CI/CD tooling includes GitHub Actions, GitLab CI, Jenkins, and CircleCI, each letting a team define its pipeline (build → test → deploy steps) as configuration checked into the repository itself.",
  ),
  k(
    'kb-code-semantic-versioning',
    'Semantic versioning',
    ['semantic versioning', 'semver basics'],
    "Semantic versioning (semver) is a widely adopted convention for version numbers in the format `MAJOR.MINOR.PATCH` (e.g. `2.4.1`), where each segment communicates a specific kind of change to consumers of a piece of software, most commonly a library or package. Incrementing the PATCH number (e.g. `2.4.1` → `2.4.2`) signals a backward-compatible bug fix — nothing that depends on the package should break from upgrading. Incrementing the MINOR number (`2.4.1` → `2.5.0`, resetting PATCH to 0) signals new functionality added in a backward-compatible way — new features are available, but nothing that already worked should have stopped working. Incrementing the MAJOR number (`2.4.1` → `3.0.0`, resetting both MINOR and PATCH to 0) signals a BREAKING change — something that could require consuming code to be updated to keep working, like a removed or renamed function. Package managers (npm, pip, Cargo) let a project specify version RANGES rather than one exact pinned version, e.g. `^2.4.1` in npm conventionally means \"any compatible version starting from 2.4.1 up to, but not including, 3.0.0,\" automatically picking up bug fixes and new features while deliberately avoiding breaking major version changes without explicit developer action.",
  ),
  k(
    'kb-code-package-managers',
    'Package managers',
    ['package managers programming', 'npm pip cargo overview'],
    "A package manager automates installing, updating, configuring, and removing external libraries/dependencies a project relies on, along with resolving the often-complex web of THEIR OWN dependencies (\"transitive dependencies\") automatically, rather than requiring a developer to manually track and download every needed library and its correct compatible version by hand. Most languages have a dominant package manager tied closely to their ecosystem: npm (and increasingly Yarn or pnpm) for JavaScript/Node.js, pip for Python, Cargo for Rust, Maven or Gradle for Java, NuGet for .NET, Composer for PHP, and gem/Bundler for Ruby. A typical workflow: a manifest file (`package.json` for npm, `requirements.txt` or `pyproject.toml` for Python, `Cargo.toml` for Rust) declares the project's direct dependencies and acceptable version ranges; running an install command resolves and downloads that full dependency tree; a generated lock file (`package-lock.json`, `Cargo.lock`) records the EXACT specific versions actually resolved and installed, ensuring every developer on a team (and every deployment) gets bit-for-bit identical dependency versions rather than potentially different versions that happen to satisfy the same declared range at different times.",
  ),
  k(
    'kb-code-oauth-jwt',
    'API authentication: OAuth and JWT',
    ['oauth basics', 'jwt json web token', 'api authentication'],
    "OAuth is an authorization framework/protocol that lets a user grant a third-party application LIMITED access to their data on another service, without ever sharing their actual password with that third-party app — this is the mechanism behind \"Sign in with Google\" or \"Connect your GitHub account\" flows. The user is redirected to the actual service (Google, GitHub) to log in and explicitly approve specific requested permissions (\"scopes\"), and that service then issues the requesting third-party app a limited-purpose access token, rather than the app ever seeing the user's real credentials directly. A JWT (JSON Web Token) is a specific, widely-used compact token FORMAT (not itself an authentication protocol) consisting of three base64-encoded, period-separated parts: a header (describing the token type and signing algorithm), a payload (claims — data about the user/session, like a user ID and expiration time), and a signature (cryptographically verifying the token hasn't been tampered with since it was issued by the server that signed it). A key property: a JWT's payload can be READ by anyone who has the token (it's only base64-encoded, not encrypted, so it's NOT a safe place to put actual secrets), but it CAN'T be forged or modified undetected without knowing the server's private signing key, since any change would invalidate the signature.",
  ),
  k(
    'kb-code-graphql-basics',
    'GraphQL basics',
    ['graphql basics', 'graphql vs rest'],
    "GraphQL is a query language and runtime for APIs, offering an alternative to REST's fixed, server-defined response shapes. Instead of REST's model where each endpoint returns a predetermined, fixed set of fields (often requiring multiple separate round-trip requests to different endpoints to gather all the data a screen actually needs, or \"over-fetching\" unused fields the client doesn't actually need), a GraphQL client sends a single query describing EXACTLY which fields it wants, potentially spanning multiple related resources in one request: `{ user(id: 5) { name, posts { title, createdAt } } }` fetches a specific user's name plus a list of their posts' titles and creation dates, all in one request and response, with the response shape mirroring the query's own shape exactly — no more, no less. A GraphQL API exposes a single endpoint (typically `/graphql`) rather than many separate REST endpoints, and defines a schema up front specifying exactly what types and fields are queryable, which serves as both strict documentation and a validation layer the server enforces automatically. The main tradeoffs versus REST: GraphQL gives clients much more precise, flexible control over exactly what data comes back (reducing both over-fetching and under-fetching), at the cost of more complexity on the server implementing the schema/resolvers, and historically weaker, less uniform HTTP-level caching compared to REST's straightforward per-URL caching model.",
  ),
  k(
    'kb-code-websockets-basics',
    'WebSockets basics',
    ['websockets basics', 'websocket vs http'],
    "A regular HTTP request follows a strict request-response pattern — the client sends a request, the server sends back one response, and the connection is (conceptually) done; the server can never spontaneously push new data to the client without the client first asking for it again. A WebSocket, by contrast, establishes a single persistent, bidirectional connection between client and server (starting as a special HTTP request that \"upgrades\" to the WebSocket protocol), after which EITHER side can send messages to the other at any time, with no need to repeatedly re-request or re-establish the connection. This makes WebSockets well suited for real-time features where the server needs to push updates to the client as they happen — live chat applications, real-time notifications, collaborative editing (multiple users seeing each other's changes instantly), and live-updating dashboards or stock tickers — cases where standard request-response HTTP would otherwise require inefficient techniques like the client repeatedly polling the server every few seconds just to check whether anything new has happened, most of which checks come back with nothing new. The tradeoff is that WebSocket connections are stateful and must be kept open and actively managed by the server for as long as each client stays connected, which uses more server-side resources per connected client than the comparatively lightweight, ephemeral, connectionless nature of typical HTTP request handling.",
  ),
  k(
    'kb-code-code-smells',
    'Common code smells',
    ['code smells', 'what is a code smell'],
    "A code smell is a surface-level pattern in code that isn't necessarily an outright bug, but tends to indicate a deeper underlying design problem worth investigating and often refactoring away. A \"long method\" or \"long function\" trying to do too many distinct things at once is harder to understand, test, and safely modify than several smaller, well-named functions each doing one clear thing. \"Duplicate code\" (the same or very similar logic copy-pasted in multiple places) means a bug fix or behavior change has to be remembered and correctly applied in every single copy, and copies inevitably drift out of sync over time as only some get updated. A \"god object\" or \"god class\" that has taken on far too many responsibilities becomes a single, fragile point that nearly every other part of the codebase ends up depending on, making it disproportionately risky to change. \"Magic numbers\" — unexplained literal values embedded directly in code, like a bare `86400` instead of a named constant `SECONDS_PER_DAY` — force a reader to guess at their meaning and intent, and get silently missed when the same value legitimately needs to change somewhere. Recognizing these patterns early, before they compound, is generally far cheaper than untangling them after a codebase has grown substantially around a badly-shaped foundation.",
  ),
  k(
    'kb-code-agile-scrum-basics',
    'Agile and Scrum basics',
    ['agile methodology', 'scrum basics'],
    "Agile is a broad family of software development philosophies emphasizing iterative, incremental delivery of working software in short cycles, close collaboration with stakeholders, and adapting readily to changing requirements — contrasted with older, more rigid \"waterfall\" approaches that tried to fully specify all requirements up front before any development began, an approach that struggles badly when requirements inevitably turn out to be incomplete or change partway through a long project. Scrum is one of the most widely adopted specific frameworks implementing agile principles. Work is organized into fixed-length iterations called \"sprints\" (commonly one to four weeks), each starting with sprint planning (deciding what work to tackle from a prioritized backlog) and ending with a sprint review (demonstrating completed, working functionality) and a retrospective (the team reflecting on what went well and what to improve for next time). Short daily \"standup\" meetings keep the team synchronized on progress and blockers without needing a longer, more disruptive meeting every day. A \"product backlog\" is a prioritized, continuously-refined list of desired features and fixes, from which specific items get pulled into each sprint's more focused, time-boxed \"sprint backlog.\" Other agile-family frameworks include Kanban (a continuous-flow model without fixed sprint boundaries, focused instead on limiting how much work is in progress at once) and various hybrids blending multiple approaches.",
  ),
  k(
    'kb-code-environment-variables',
    'Environment variables in software configuration',
    ['environment variables', 'env vars configuration'],
    "Environment variables are key-value pairs set at the operating system or shell level, available to any process running within that environment, commonly used to configure an application's behavior without hardcoding values directly into the source code. A typical use case is storing secrets and environment-specific configuration — a database connection string, an API key, whether the app is running in \"development\" or \"production\" mode — as environment variables rather than committing them directly into version control, since a hardcoded secret in a public (or even private) Git repository's history is a significant, persistent security risk (it remains recoverable from the repository's history even after later being removed from the current files). Most languages provide a simple way to read them: `process.env.DATABASE_URL` in Node.js, `os.environ.get(\"DATABASE_URL\")` in Python. A `.env` file (loaded by a small library like `dotenv`) is a common convention for defining environment variables for local development in one place without needing to manually export each one in the shell — and such `.env` files are themselves almost universally added to `.gitignore` specifically to prevent committing real secrets, with only a template `.env.example` (containing variable NAMES but no real secret values) actually checked into the repository.",
  ),
  k(
    'kb-code-testing-pyramid',
    'The testing pyramid',
    ['testing pyramid', 'unit vs integration vs end to end tests'],
    "The testing pyramid is a widely referenced mental model for how a healthy automated test suite's different test types should be proportioned. At the base (and largest in number) are unit tests — fast, focused tests of small individual pieces of code (a single function or class) in isolation, typically using mocks/stubs for external dependencies, which run in milliseconds and can be executed extremely frequently (every save, every commit) without meaningfully slowing anyone down. In the middle are integration tests — verifying that multiple pieces actually work correctly TOGETHER (e.g. does the code correctly read from and write to a real, or realistically simulated, database), inherently slower than pure unit tests since they involve more real moving parts, so there are fewer of them by design. At the top (smallest in number) are end-to-end (E2E) tests — testing a complete real user flow through the ENTIRE running application, often literally driving a real browser, which give the strongest overall confidence that a full feature genuinely works from a real user's perspective, but are the slowest to run, the most brittle/flaky (small unrelated UI changes can break them), and the most expensive to write and maintain, which is exactly why the pyramid recommends having comparatively few of them rather than relying on E2E tests as the primary safety net for everyday changes.",
  ),
  k(
    'kb-code-api-versioning',
    'API versioning',
    ['api versioning', 'how to version an api'],
    "API versioning lets an API evolve and introduce breaking changes over time without immediately breaking every existing client application still depending on the previous behavior — since, unlike a library that a developer can simply choose when to upgrade, an already-shipped mobile app or third-party integration calling a live API often can't be instantly updated the moment the API's backend changes underneath it. A common approach is URL-based versioning, embedding the version directly in the path: `/api/v1/users` vs `/api/v2/users`, making the version highly visible and simple for clients and API documentation alike. Header-based versioning instead specifies the desired version via a custom request header (like `Accept: application/vnd.myapi.v2+json` or a simpler custom header), keeping URLs cleaner and more stable over time at the cost of being somewhat less immediately visible/discoverable than a version embedded right in the URL. Regardless of the specific mechanism chosen, the underlying practice is the same: maintaining the OLD version's endpoints running and fully functional alongside the new version for some deliberately communicated deprecation period, giving existing client applications enough advance time to migrate to the newer version before the old one is eventually shut down and removed entirely.",
  ),
  k(
    'kb-code-idempotency',
    'Idempotency in APIs',
    ['idempotency api', 'what is idempotent'],
    "An operation is idempotent if performing it multiple times produces the exact same end result as performing it just once — repeating it causes no additional, different side effect beyond what the first call already did. In HTTP terms: `GET`, `PUT`, and `DELETE` are all conventionally expected to be idempotent — calling `DELETE /users/5` five times in a row should leave the exact same end state as calling it just once (the user is deleted; the extra calls simply find nothing left to delete and typically just return a \"not found\" or similarly harmless response rather than doing anything further). `POST`, by contrast, is conventionally NOT idempotent by default — calling `POST /orders` five times with identical data would naturally create five separate, distinct new orders, since each individual POST call is inherently a request to create a new resource. This distinction matters enormously for reliability in real networked systems: if a client sends a request but the response is lost or delayed due to a network issue, it's generally SAFE to automatically retry an idempotent request (retrying a `DELETE` won't accidentally delete anything twice), but automatically retrying a non-idempotent `POST` risks accidentally creating duplicate resources (like a duplicate order or duplicate payment charge) — which is exactly why some APIs add an explicit client-supplied \"idempotency key\" specifically to make an otherwise non-idempotent POST-style operation safely retryable.",
  ),
];
