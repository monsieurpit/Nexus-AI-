import { KnowledgeItem } from '../../types';

// Batch 310 corpus fixes — coding-priority batch (2026-09-14). 19/25 misses.
// Topic: cache eviction/invalidation strategies, serialization/data-interchange formats, and
// type-system concepts (duck/structural/nominal typing, covariance/contravariance, gRPC) — chosen
// because grep confirmed zero prior corpus coverage of LRU/LFU, protobuf/MessagePack, duck vs
// structural vs nominal typing, generics-vs-templates depth, and gRPC anywhere across the ~300+
// existing corpus files, unlike REST/GraphQL, git, testing, and concurrency which were already
// well covered. Very high miss rate (19/25) confirms this was genuinely untested territory.
// Two severe wrong-domain hallucinations: serialization vs deserialization dumped a whole answer
// about MANGA VS WESTERN COMICS (black-and-white/right-to-left vs colored/left-to-right), never
// touching programming at all; marshalling vs serialization answered about the historical
// "Marshall Plan" and military troop marshalling, never the programming term. One factual error:
// gRPC vs REST described gRPC as "a strict protocol based on XML with a rigid contract called
// WSDL" — that is SOAP, not gRPC (which is actually Protobuf/HTTP2-based); REST itself was
// described correctly. One factual error: cache warming was described as "clearing out the old
// data so you get fresh copies" — the opposite of what it means (proactively pre-loading a cache
// BEFORE it's needed, not clearing it). One wrong-domain mixup: structural vs nominal typing
// answered about statistics "nominal data" (categorical labels like "red"/"blue"/"dog"), confusing
// it with the type-system term. Two lazy-copy-paste wrong-answers: protobuf vs JSON and YAML vs
// JSON both received the exact same canned "XML vs JSON" answer, ignoring the actual second term
// asked about. One conflated answer: duck typing vs structural typing treated them as basically
// identical without explaining the real distinction (duck typing is a RUNTIME/informal check —
// "if it quacks, call the method"; structural typing is a compile-time/static type-system check of
// shape compatibility, as in TypeScript or Go interfaces). One wrong-domain mixup: TOML vs YAML
// answered about semver "version ranges" in package.json dependency declarations, never actually
// contrasting TOML's key=value/section syntax with YAML's indentation-based syntax. One severe
// dump-and-dodge: LRU vs LFU and LRU vs FIFO both got the exact same full TypeScript LRU-cache
// implementation dumped verbatim, never mentioning LFU or FIFO eviction at all. Six one-sided
// cutoffs: write-through vs write-back (explained only write-through), protobuf vs MessagePack
// (wandered into an unrelated RPC Wikipedia snippet, never finished comparing the two formats),
// covariance vs contravariance (explained only covariance), binary vs text-based protocol
// (explained only binary), Base64 vs hexadecimal encoding (explained only hex), ASCII vs UTF-8
// (explained UTF-8 well but was cut off right as it started to define ASCII), TTL-based vs
// event-based cache invalidation (explained only TTL), and cache vs buffer (dumped generic cache
// hit/miss/replacement-policy trivia, never explained what a buffer is or how it differs).

const now = Date.now();
const k = (id: string, title: string, keywords: string[], content: string): KnowledgeItem => ({
  id,
  title,
  category: 'Programming',
  keywords,
  content,
  createdAt: now,
});

export const CACHING_SERIALIZATION_TYPE_SYSTEMS_GAPS: KnowledgeItem[] = [
  k(
    'kb-gap-code-lru-vs-lfu',
    'LRU vs LFU cache eviction',
    ['lru vs lfu', 'difference lru lfu', 'least recently used least frequently used cache'],
    "Both are policies a fixed-size cache uses to decide WHICH item to throw out once it's full and a new item needs to be added. LRU (Least Recently Used) evicts whichever item was accessed longest ago in time — it only cares about recency, not how many times something was used, so a single popular item that hasn't been touched in a while gets evicted just as readily as a genuinely unpopular one; it's typically implemented with a hash map (for O(1) lookup) plus a doubly linked list (to track access order, moving an item to the front every time it's touched) so both get/put run in O(1). LFU (Least Frequently Used) instead evicts whichever item has been accessed the FEWEST total times, regardless of how recently — it tracks a hit counter per item rather than a timestamp/position, which better protects consistently popular items from ever being evicted, but requires extra bookkeeping (often a hash map plus a frequency-bucketed structure) and can wrongly keep an item that was extremely popular long ago but hasn't been touched in ages ('cache pollution'), a problem LRU doesn't have. The key difference: LRU evicts based on WHEN an item was last used (recency), while LFU evicts based on HOW OFTEN an item has been used overall (frequency) — LRU adapts faster to shifting access patterns, LFU better protects long-term popular items but is more complex to implement.",
  ),
  k(
    'kb-gap-code-lru-vs-fifo',
    'LRU vs FIFO cache eviction',
    ['lru vs fifo', 'difference lru fifo cache', 'first in first out cache eviction'],
    "Both decide which item to evict from a full cache, but they track completely different things. FIFO (First-In-First-Out) evicts strictly in the order items were ADDED to the cache — the oldest inserted item goes first, full stop, no matter how recently or how often it's actually been accessed since; it's implemented with a simple queue, making it cheap and simple, but it can evict an item that was just used a second ago simply because it happened to be inserted first. LRU (Least Recently Used) instead evicts based on the order items were last ACCESSED (read or written) — every access moves an item to the 'most recently used' end, so an old item that keeps getting reused stays in the cache indefinitely, while an item nobody has touched in a while gets evicted even if it was added recently. The key difference: FIFO only tracks insertion order and ignores access patterns entirely, while LRU tracks access (usage) order and re-promotes an item every time it's touched — LRU generally gives a much better hit rate for real-world access patterns because it actually reflects what's currently 'hot,' while FIFO is simpler but can evict frequently-used data purely because of when it happened to arrive.",
  ),
  k(
    'kb-gap-code-write-through-vs-write-back',
    'Write-through vs write-back caching',
    ['write-through vs write-back', 'write through write back cache', 'difference write-through write-back caching'],
    "Both describe WHEN a cache actually propagates a write to the slower underlying storage (a database, disk, or main memory) behind it. Write-through caching writes to the cache AND immediately, synchronously, writes the same data through to the underlying storage as part of the same operation — every write is instantly durable and the cache and the source of truth are never out of sync, but every write pays the full latency cost of the slow underlying storage, since the operation can't be considered 'done' until that slower write also completes. Write-back (also called write-behind) caching instead writes ONLY to the cache immediately and marks that cached entry as 'dirty,' deferring the actual write to underlying storage until later — either after a delay, when the entry is evicted, or via periodic batched flushes — which makes writes much faster since they don't wait on slow storage, and can batch/coalesce many rapid writes to the same data into a single underlying write. The tradeoff: write-back risks losing the not-yet-flushed dirty data if the cache crashes or loses power before it's written through, while write-through never has that risk since data is always durably stored the moment it's written. The key difference: write-through writes to cache and underlying storage synchronously together (safer, slower), while write-back writes to cache immediately and defers the underlying storage write for later (faster, but riskier if the cache fails before flushing).",
  ),
  k(
    'kb-gap-code-protobuf-vs-json',
    'Protobuf vs JSON',
    ['protobuf vs json', 'protocol buffers vs json', 'difference protobuf json'],
    "Both are formats for serializing structured data to send over a network or store on disk, but they take opposite approaches on readability versus efficiency. JSON is a text-based, human-readable format — `{\"name\": \"Alex\", \"age\": 30}` — that needs no predefined schema to parse (any JSON parser can read any valid JSON, since the field names and structure are self-describing right there in the text), which makes it easy to debug, log, and inspect by eye, and is the default format for virtually all REST APIs. Protocol Buffers (protobuf), created by Google, is a BINARY serialization format that requires a predefined schema (a `.proto` file declaring each message's fields, their types, and numeric field tags) compiled ahead of time into code for each language used; because both sides already know the exact structure from that schema, protobuf doesn't need to repeat field names as text in every message the way JSON does, making messages significantly smaller and faster to parse — but the tradeoff is protobuf messages are NOT human-readable as raw bytes, can't be inspected without the schema and a decoding tool, and require a build step to (re)generate code whenever the schema changes. The key difference: JSON is text-based, schema-free, and human-readable but comparatively larger/slower to parse, while protobuf is a compact binary format requiring a predefined schema and generated code, trading readability and flexibility for smaller message size and faster parsing — which is why protobuf is the default wire format for gRPC.",
  ),
  k(
    'kb-gap-code-protobuf-vs-messagepack',
    'Protobuf vs MessagePack',
    ['protobuf vs messagepack', 'protocol buffers vs msgpack', 'difference protobuf messagepack'],
    "Both are compact BINARY serialization formats designed to be smaller and faster than text formats like JSON, but they differ on whether a schema is required. Protobuf (Protocol Buffers) requires a predefined schema (a `.proto` file) compiled ahead of time into language-specific code — both the sender and receiver must already agree on the exact structure (field names, types, and numeric tags) before any message can be encoded or decoded, which is why protobuf messages omit field names entirely from the actual bytes (they're replaced by short numeric tags), making them extremely compact, but this also means you can't decode a protobuf message without its matching schema. MessagePack, by contrast, is essentially 'binary JSON' — it encodes the exact same schema-less, self-describing data model as JSON (objects with string keys, arrays, numbers, strings, booleans, null) but in a compact binary form instead of text, so it needs NO predefined schema at all and any MessagePack decoder can read any valid MessagePack data on its own, at the cost of still including field/key names in the encoded bytes (making it larger than protobuf, though still meaningfully smaller than equivalent JSON text). The key difference: protobuf requires a predefined schema and generated code but produces the smallest, fastest messages by omitting field names entirely, while MessagePack needs no schema at all (it's a drop-in binary replacement for JSON) but is somewhat larger than protobuf since it still carries field names in the data.",
  ),
  k(
    'kb-gap-code-serialization-vs-deserialization',
    'Serialization vs deserialization',
    ['serialization vs deserialization', 'difference serialize deserialize', 'what is serialization programming'],
    "These are the two opposite, complementary directions of converting data between an in-memory program representation and a format that can be stored or transmitted. Serialization is the process of converting a program's in-memory data structure (an object, a list, a struct) into a format — usually a string or a sequence of bytes, like JSON text, XML, or a binary format like protobuf — that CAN be written to a file, stored in a database, or sent over a network, since raw in-memory objects (with pointers/references into a specific process's memory) can't be directly saved or transmitted as-is. Deserialization is the exact reverse process: taking that serialized string/byte data and reconstructing it back into a usable in-memory object or data structure in a program, so the code can actually work with it again as normal typed data rather than raw text or bytes. A common real-world example: a JavaScript object is serialized to a JSON string with `JSON.stringify(obj)` to send over an HTTP request, and the receiving server deserializes that JSON string back into a usable object with `JSON.parse(jsonString)`. The key difference: serialization converts in-memory data INTO a storable/transmittable format (object → bytes/text), while deserialization converts that format BACK into usable in-memory data (bytes/text → object) — they are inverse operations of the same overall process.",
  ),
  k(
    'kb-gap-code-marshalling-vs-serialization',
    'Marshalling vs serialization',
    ['marshalling vs serialization', 'difference marshalling serialization', 'what is marshalling programming'],
    "In a programming/computer-science context (not the unrelated historical 'Marshall Plan' or the military sense of 'marshalling troops'), marshalling and serialization are closely related terms that are often used interchangeably, but marshalling is technically the broader concept. Serialization specifically refers to converting a data structure or object into a storable/transmittable format (like a byte stream or JSON text) — it deals only with representing the DATA itself. Marshalling covers that same data-conversion step, but also can include preparing an object for transfer across a process or network boundary in a way that preserves enough information to properly reconstruct it on the other side — which, in some contexts (particularly older RPC/distributed-object systems like CORBA or Java RMI), can also mean packaging up not just the raw data but relevant TYPE information, object references, or even (in some remote-object systems) enough context to invoke methods remotely on the reconstructed object. In most modern everyday usage (especially in languages like Go, where `marshal`/`unmarshal` are the actual standard-library function names for what other languages call `serialize`/`deserialize`, e.g. `json.Marshal()`), the two terms are treated as synonyms for converting data to/from a transmittable format. The key difference: serialization is specifically about converting data into a storable/transmittable format, while marshalling is the broader, historically RPC-flavored term that can also include preparing type/reference information for crossing a process or network boundary — though in most modern practical usage (and explicitly in Go) the two terms mean the same thing.",
  ),
  k(
    'kb-gap-code-base64-vs-hex',
    'Base64 vs hexadecimal encoding',
    ['base64 vs hex', 'base64 vs hexadecimal encoding', 'difference base64 hex encoding'],
    "Both are ways to represent raw binary data using only safe, printable ASCII text characters, but they differ in how compact the result is. Hexadecimal (base-16) encoding represents each byte as exactly two hex characters (0-9 and A-F), since one hex digit represents exactly 4 bits and a byte is 8 bits — so hex encoding always exactly DOUBLES the size of the original data (1 byte in → 2 characters out), but is extremely simple to read and convert by hand, which is why it's commonly used for things like displaying hash digests (`e3b0c44298fc1c14...`), MAC addresses, or color codes (`#FF5733`). Base64 (base-64) encoding instead uses a larger alphabet of 64 different printable characters (A-Z, a-z, 0-9, +, /) and packs 3 raw bytes (24 bits) into 4 Base64 characters (6 bits each), which makes it noticeably more space-efficient than hex — Base64 only increases data size by about 33% rather than hex's flat 100% overhead — which is why Base64 is the standard choice for embedding binary data (images, files, encryption keys) inside text-based formats like JSON, XML, email (MIME attachments), or data URIs, where hex would waste roughly twice as much space for no benefit. The key difference: hex encoding is simpler and more human-readable but doubles data size (2 characters per byte), while Base64 is less immediately readable by eye but is more space-efficient (about 33% overhead instead of 100%), which is why Base64 is preferred for embedding actual binary payloads in text formats.",
  ),
  k(
    'kb-gap-code-ascii-vs-utf8',
    'ASCII vs UTF-8',
    ['ascii vs utf-8', 'ascii vs utf8', 'difference ascii utf-8 encoding'],
    "ASCII (American Standard Code for Information Interchange) is an old, fixed-width character encoding from the 1960s that represents exactly 128 characters — the English alphabet (upper and lower case), digits 0-9, basic punctuation, and some control characters — using exactly 7 bits (or 1 byte with the top bit unused) per character; it has no way to represent accented letters, non-Latin alphabets (Cyrillic, Chinese, Arabic), emoji, or virtually any character outside basic English text. UTF-8 is a modern, VARIABLE-width encoding for Unicode, the much larger standard that assigns a unique 'code point' number to over 140,000 characters covering virtually every writing system in the world; UTF-8 encodes each Unicode code point using between 1 and 4 bytes depending on the character — critically, it encodes the first 128 code points (which map exactly onto the original ASCII characters) using exactly 1 byte each, identical to plain ASCII, while non-ASCII characters (accented letters, non-Latin scripts, emoji) take 2, 3, or 4 bytes. This design makes UTF-8 fully backward-compatible with ASCII: any valid plain-ASCII text file is automatically also valid UTF-8 with an identical byte representation, which is a major reason UTF-8 became the dominant encoding for the web and most modern software. The key difference: ASCII is a fixed 7-bit encoding limited to 128 basic English characters, while UTF-8 is a variable-width (1-4 byte) encoding of the full Unicode character set that is a strict superset of, and fully backward-compatible with, ASCII.",
  ),
  k(
    'kb-gap-code-duck-typing-vs-structural-typing',
    'Duck typing vs structural typing',
    ['duck typing vs structural typing', 'difference duck typing structural typing', 'what is duck typing programming'],
    "Both relate to the idea that a type is compatible somewhere based on WHAT IT CAN DO (its shape/behavior) rather than its declared name — but the key difference is WHEN that check happens. Duck typing is a RUNTIME, dynamic-language concept: 'if it walks like a duck and quacks like a duck, it's a duck' — there is no compile-time check at all; a language (Python, Ruby, JavaScript) simply attempts to call whatever method or access whatever property is needed on an object at the moment the code actually runs, and if that object happens to have that method/property, it works, regardless of the object's declared type or class — if it doesn't, the program only fails when that specific line actually executes, potentially far into runtime, not before. Structural typing is a STATIC, compile-time concept found in statically-typed languages (TypeScript, Go, OCaml): the compiler itself checks, BEFORE the program ever runs, whether a given type's structure (the set of fields/methods it has) matches what's required — in TypeScript, an object satisfies an interface simply by having all the required properties with matching types, with no explicit `implements` keyword needed; in Go, a type satisfies an interface automatically just by implementing all its methods. The key difference: duck typing is an informal runtime behavior where a language simply tries the operation and only fails if it doesn't work (no upfront checking at all), while structural typing is a formal compile-time type-system feature where the compiler proactively verifies structural compatibility BEFORE the program runs — structural typing can be thought of as duck typing's ideas enforced statically ahead of time, rather than left to fail unpredictably at runtime.",
  ),
  k(
    'kb-gap-code-structural-vs-nominal-typing',
    'Structural typing vs nominal typing (type systems)',
    ['structural typing vs nominal typing', 'difference structural nominal typing', 'nominal typing programming'],
    "This is a programming type-systems concept — NOT related to 'nominal data' in statistics (unordered category labels like 'red'/'blue'/'dog'). Both are ways a compiler decides whether one type is compatible with (or a subtype of) another, but they check fundamentally different things. Nominal typing decides compatibility based purely on the type's DECLARED NAME/identity — two types are only considered compatible if one is explicitly declared to extend or implement the other (via `extends`, `implements`, or similar), even if they have identical structure (fields and methods); Java, C#, and C++ use nominal typing by default, so two unrelated classes with exactly the same methods are still considered totally different, incompatible types unless one explicitly declares a relationship to the other. Structural typing instead decides compatibility based purely on a type's ACTUAL SHAPE — its set of fields and methods — regardless of what it's named or whether it explicitly declares any relationship; TypeScript and Go use structural typing, so any object or type that happens to have all the required properties/methods automatically satisfies an interface, with no explicit declaration needed at all. The key difference: nominal typing requires an explicit declared relationship (by name) between types to be considered compatible, while structural typing only cares whether a type's actual shape (fields/methods) matches what's required, regardless of its name or any declared relationship.",
  ),
  k(
    'kb-gap-code-covariance-vs-contravariance',
    'Covariance vs contravariance (type systems)',
    ['covariance vs contravariance', 'difference covariance contravariance', 'covariant contravariant types'],
    "Both describe how subtyping relationships between complex types (like arrays, generics, or function types) relate to the subtyping relationship of their underlying component types — the question is: if type `Cat` is a subtype of `Animal`, what does that imply about `List<Cat>` vs `List<Animal>`, or about a function that takes/returns a `Cat` vs one that takes/returns an `Animal`? Covariance preserves the SAME direction of the subtyping relationship: if `Cat` is a subtype of `Animal`, a covariant type constructor means `List<Cat>` is treated as a subtype of `List<Animal>` — this makes sense for things you only READ from (you can safely use a `List<Cat>` anywhere a `List<Animal>` is expected for reading, since every Cat IS an Animal), and it's why a function's RETURN type is typically covariant (a function promising to return an `Animal` can safely instead return a more specific `Cat`). Contravariance FLIPS the direction: a contravariant type constructor means `Handler<Animal>` is treated as a subtype of `Handler<Cat>` — this applies to things you WRITE TO or things that CONSUME a value, which is why a function's PARAMETER types are typically contravariant (a function that needs to handle any `Cat` can safely be given a more general function that already knows how to handle any `Animal`, since handling all Animals covers handling Cats too). The key difference: covariance preserves the subtype ordering (more specific type usable where a more general one is expected — used for outputs/return types), while contravariance reverses the subtype ordering (a handler of a more general type is usable where a handler of a more specific type is expected — used for inputs/parameter types).",
  ),
  k(
    'kb-gap-code-grpc-vs-rest',
    'gRPC vs REST',
    ['grpc vs rest', 'difference grpc rest api', 'grpc rest api'],
    "REST is an architectural STYLE (not a strict protocol) for designing APIs over plain HTTP, typically exchanging human-readable JSON text over standard HTTP verbs (GET, POST, PUT, DELETE) against resource-based URLs — it's flexible, widely supported by every language/tool, and easy to debug/inspect by eye, but JSON-over-HTTP/1.1 has real overhead (larger text payloads, and traditionally one request per connection at a time). gRPC (built by Google) is a specific, modern RPC (Remote Procedure Call) FRAMEWORK that runs over HTTP/2 and uses Protocol Buffers (protobuf) as its wire format instead of JSON — it requires a predefined `.proto` schema declaring the exact service methods and message types, which are compiled into strongly-typed client/server code for each language, enabling significantly smaller/faster binary messages and native support for streaming (client-streaming, server-streaming, and full bidirectional streaming over a single long-lived HTTP/2 connection) — capabilities plain REST doesn't have built in. Important correction: gRPC is NOT XML-based and does NOT use WSDL — that describes SOAP, an older, more rigid enterprise protocol; gRPC is comparatively modern, binary (protobuf), and built specifically for fast internal service-to-service (microservice) communication. The key difference: REST is a flexible, text-based (JSON/HTTP) architectural style that's simple and universally supported, while gRPC is a stricter, schema-driven, binary (protobuf/HTTP2) RPC framework that's faster and supports streaming but requires generated code and a shared schema on both ends — REST tends to win for public-facing APIs, gRPC for internal, performance-sensitive service-to-service calls.",
  ),
  k(
    'kb-gap-code-binary-vs-text-protocol',
    'Binary protocol vs text-based protocol',
    ['binary vs text protocol', 'difference binary text protocol', 'binary protocol text protocol'],
    "Both describe how data is actually represented 'on the wire' when two systems communicate over a network, differing in whether the raw bytes are meant to be read directly as human text or interpreted as packed binary fields. A text-based protocol encodes its messages using printable, human-readable characters following a defined text format — HTTP/1.1 headers, SMTP (email), and FTP's control commands are classic examples — which makes messages directly readable and debuggable just by looking at raw network traffic (you can literally read an HTTP request in a packet capture), and easier to hand-write/test with basic tools like `telnet` or `curl`, but text encoding is comparatively wasteful: numbers, dates, and structured data all get spelled out as extra text characters (with delimiters, whitespace, and often repeated field names) that add real size and parsing overhead compared to encoding the same information directly as raw bytes. A binary protocol instead packs data directly into specific byte layouts and bit patterns defined by a fixed specification — protobuf/gRPC, most database wire protocols, and low-level formats like TCP/IP headers themselves are binary — which is far more compact and faster for a computer to parse (no need to search for delimiters or convert text digits to numbers), but the raw bytes are NOT human-readable at all without a decoder that knows the exact specification, making debugging significantly harder without specialized tooling. The key difference: text-based protocols are human-readable and easy to debug/hand-write but less compact and slower to parse, while binary protocols are compact and fast to parse but require a decoder/tooling to inspect since the raw bytes aren't directly readable.",
  ),
  k(
    'kb-gap-code-yaml-vs-json',
    'YAML vs JSON',
    ['yaml vs json', 'difference yaml json', 'yaml versus json'],
    "Both are formats for representing structured data (objects, lists, key-value pairs, scalars), and YAML is actually a strict SUPERSET of JSON (any valid JSON document is also valid YAML) — but they differ sharply in syntax and typical use. JSON uses explicit punctuation for structure: curly braces `{}` for objects, square brackets `[]` for arrays, commas between items, and quotes around every string key and string value — `{\"name\": \"Alex\", \"tags\": [\"a\", \"b\"]}` — which makes it unambiguous and trivial for machines to parse but visually noisy for humans to hand-write and edit, especially for deeply nested data. YAML (YAML Ain't Markup Language) instead uses significant INDENTATION (like Python) to represent nesting, and generally omits quotes and braces entirely for simple values: the same data would be written as `name: Alex` and a list as dashed items under `tags:` on their own indented lines — which is much easier for humans to read and hand-edit, and also supports features JSON lacks, like comments (`# like this`), multi-line strings, and anchors/references to avoid repeating the same data. Because of this readability, YAML is the near-universal choice for human-maintained configuration files (Kubernetes manifests, GitHub Actions workflows, Docker Compose files, CI pipelines), while JSON remains the dominant choice for machine-to-machine data exchange (REST API bodies) where files are generated/parsed by code rather than hand-edited. The key difference: JSON uses explicit braces/brackets/quotes and is optimized for unambiguous machine parsing, while YAML uses indentation and minimal punctuation (and is a superset of JSON) optimized for human readability and hand-editing, which is why YAML dominates config files and JSON dominates API payloads.",
  ),
  k(
    'kb-gap-code-toml-vs-yaml',
    'TOML vs YAML',
    ['toml vs yaml', 'difference toml yaml', 'toml versus yaml config format'],
    "Both are human-readable configuration-file formats (an alternative to JSON for files people hand-edit), but they differ in syntax philosophy. YAML relies on significant whitespace/indentation to represent nested structure, similar to Python — this makes deeply nested data compact to write, but indentation-based nesting can be a genuine source of subtle bugs (an extra or missing space silently changes the structure or breaks parsing), and YAML's type-inference rules have historically caused well-known surprising 'gotchas' (e.g. an unquoted `no` or `off` being silently interpreted as the boolean `false` rather than a string). TOML (Tom's Obvious, Minimal Language) instead uses an explicit `key = value` syntax organized into `[section]` headers (similar in spirit to old INI config files), avoiding YAML's indentation-sensitivity entirely — nesting is expressed with dotted section names (`[server.database]`) rather than indentation depth, which its designers consider less error-prone and more 'obvious' to read at a glance, at the cost of being somewhat more verbose for deeply nested or list-heavy data than YAML. TOML has become the standard config format for several modern tooling ecosystems specifically because of this predictability — Rust's Cargo.toml and Python's pyproject.toml both use TOML. The key difference: YAML uses indentation-based nesting (compact but whitespace-sensitive, with some type-inference gotchas), while TOML uses explicit `key = value` pairs under `[section]` headers (more verbose but avoids indentation bugs) — this is unrelated to semver 'version ranges' in dependency files, which is a separate concept from either format's syntax.",
  ),
  k(
    'kb-gap-code-cache-vs-buffer',
    'Cache vs buffer',
    ['cache vs buffer', 'difference cache buffer', 'what is a buffer computing'],
    "Both are regions of (usually faster) memory that temporarily hold data, but they exist for different PURPOSES. A cache exists specifically to avoid REPEATING expensive work — it stores a copy of data (or the result of a computation) that has already been fetched or computed once, so that a future request for that SAME data can be served instantly from the cache instead of redoing the original slow work (a database query, a network call, a computation); a cache entry can be reused many times, and eviction policies (LRU, LFU) decide what to discard when it fills up. A buffer exists to handle a MISMATCH in the rate, timing, or chunk-size between a producer and a consumer of data — it temporarily holds data that is being produced faster than it can be consumed (or written in larger chunks than the destination wants), smoothing out that difference; a classic example is a video player buffering a few seconds of incoming video ahead of playback to absorb network jitter, or an I/O buffer collecting written bytes so they can be flushed to disk in one efficient batch instead of many tiny slow writes. Critically, a buffer's contents are typically each used (consumed/written) exactly ONCE and then discarded/emptied, while a cache's contents are deliberately KEPT so the same data can be reused repeatedly. The key difference: a cache stores a REUSABLE copy of data specifically to avoid repeating expensive work on future requests for the same data, while a buffer temporarily holds data ONCE to smooth out a rate/timing mismatch between a producer and consumer, after which that specific data is normally passed on or discarded rather than reused.",
  ),
  k(
    'kb-gap-code-cache-invalidation-ttl-vs-event',
    'TTL-based vs event-based cache invalidation',
    ['ttl vs event-based cache invalidation', 'difference ttl event-based cache invalidation', 'cache invalidation strategies'],
    "Both are strategies for deciding WHEN previously cached data should be considered stale and removed or refreshed, so the cache doesn't keep serving outdated information forever. TTL (Time-To-Live) based invalidation assigns every cached entry a fixed expiration time when it's stored (e.g. 'valid for 10 minutes') — once that time elapses, the entry is automatically treated as expired and either removed or refetched on the next request, regardless of whether the underlying data actually changed; this is simple to implement and requires no coordination with whatever system actually owns the data, but it's a blunt guess — set the TTL too long and users see stale data for longer than necessary after a real change, set it too short and you lose much of the caching benefit through unnecessary, frequent refetches even when nothing actually changed. Event-based (or write-through) invalidation instead actively invalidates or updates the specific cached entry the MOMENT the underlying data actually changes — the system that modifies the data explicitly notifies the cache (directly, or via a message/event) to evict or refresh that specific entry right away, guaranteeing the cache is never stale for longer than the brief moment it takes that notification to propagate, at the cost of requiring real integration/coordination between the data-owning system and the cache (every code path that changes the data must remember to trigger the invalidation). The key difference: TTL-based invalidation expires entries after a fixed guessed time interval regardless of whether the data actually changed (simple, no coordination needed, but imprecise), while event-based invalidation reactively invalidates a cache entry the instant its underlying data actually changes (always accurate, but requires the writing system to explicitly notify the cache).",
  ),
  k(
    'kb-gap-code-cache-warming',
    'Cache hit, cache miss, and cache warming',
    ['cache hit vs cache miss', 'what is cache warming', 'cache warming definition'],
    "A cache hit occurs when requested data IS found already sitting in the cache, so it can be returned immediately without redoing the original slow work (a database query, computation, or network call). A cache miss occurs when the requested data is NOT in the cache, so the system has to fall back to the slower original source, fetch/compute the data from there, and typically then store a copy in the cache for next time. Cache warming (also called 'cache priming' or 'pre-warming') is the practice of PROACTIVELY loading likely-to-be-needed data into the cache BEFORE it's actually requested by a real user — commonly done right after a deployment, a server restart, or a cache-clearing event, since a freshly started or empty cache is 'cold' and will otherwise force the very first real users to eat a wave of slow cache misses while the cache gradually fills up on its own through normal traffic. Correction of a common misconception: cache warming does NOT mean clearing out old data — it means the OPPOSITE, deliberately populating an empty or newly-started cache with data ahead of time (often by replaying recent popular queries, or explicitly pre-loading known-hot data) so real traffic hits a already-'warm' cache with a good hit rate from the very first request, rather than suffering a painful burst of cold-cache misses right after a restart or deploy. The key difference: a cache hit/miss describes whether a single specific request found its data already cached or not, while cache warming is a deliberate proactive strategy of pre-loading data into an empty cache ahead of real traffic to avoid a wave of misses right after a cold start.",
  ),
];
