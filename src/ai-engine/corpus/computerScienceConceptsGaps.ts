import { KnowledgeItem } from '../../types';

// Batch 186 (computer science, 40-question batch). nexus-4b handled compiler/
// interpreter, stack/queue, TCP/UDP, process/thread, HTTP/HTTPS, bit/byte,
// array/linked list, static/dynamic typing, class/object, frontend/backend,
// SQL/NoSQL, GET/POST, sync/async, variable/constant, machine code/assembly,
// IP/MAC, symmetric/asymmetric encryption, Git/GitHub, library/framework,
// client/server, 32/64-bit, compilation/linking, primary/foreign key, pass
// by value/reference, virus/worm, cache hit/miss, abstract class/interface,
// DOM/HTML, deadlock/starvation, lossy/lossless. Failures corrected below:
// RAM vs hard drive (said RAM is "larger but slower than a hard drive" -
// backwards), stack vs heap in memory (conflated the memory heap with the
// heap data structure / heapsort), DFS vs BFS (truncated), bug vs feature
// (missed the "it's not a bug it's a feature" meaning), OS vs firmware
// (equated firmware with the boot code only), big O vs big Theta (said Theta
// is "the average case" and O is "worst case"), boolean vs bitwise operator
// (web dump), hash table vs BST (missed ordering).
export const COMPUTER_SCIENCE_CONCEPTS_GAPS: KnowledgeItem[] = [
  {
    id: 'kb-gap-cs-ram-harddrive',
    title: "RAM vs Hard Drive (Storage)",
    category: 'Computer Science',
    keywords: [
      'what is the difference between RAM and a hard drive volatile fast small working memory versus non volatile slow large permanent storage RAM is smaller and faster not larger and slower',
      'RAM random access memory is the computers fast VOLATILE WORKING memory it holds the programs and data the CPU is actively using it is FAST nanosecond access and SMALL typically 8 to 64 GB on a PC and it LOSES its contents when power is cut',
      'a HARD DRIVE HDD or SSD is NON VOLATILE long term STORAGE it keeps the operating system applications and files permanently even with the power off it is much LARGER hundreds of GB to many TB and much SLOWER than RAM an HDD stores data magnetically on spinning platters an SSD uses flash memory chips with no moving parts and is far faster than an HDD but still slower than RAM',
      'the correct ordering RAM is SMALLER in capacity and FASTER in access than a hard drive it is NOT larger and slower when RAM fills up the OS swaps data to a page file on the much slower drive which is why a low RAM machine stutters the memory hierarchy from fast small to slow large is CPU registers cache RAM SSD HDD',
    ],
    content: `RAM and a HARD DRIVE are both places a computer keeps data, but they sit at very different points on the memory hierarchy — and a common mistake is to get their relative size and speed backwards. RAM (Random Access Memory, "memory") is the computer's fast WORKING memory. It holds the code and data that the CPU is actively using right now — the running operating system, your open applications, the document you're editing. Key properties: it is VOLATILE (its contents vanish the instant power is lost); it is FAST (access times on the order of tens of nanoseconds, and very high bandwidth); and it is relatively SMALL (a typical PC has 8–64 GB). A HARD DRIVE (whether a traditional HDD or a solid-state SSD) is long-term STORAGE. It holds the operating system, all installed programs, and all your files PERMANENTLY — the data survives when the computer is powered off. Key properties: it is NON-VOLATILE (keeps data without power); it is much LARGER (256 GB to several TB is typical); and it is much SLOWER than RAM. A hard disk drive (HDD) stores bits magnetically on rapidly spinning platters read by a moving head (milliseconds to seek). A solid-state drive (SSD) stores bits in flash memory chips with no moving parts — dramatically faster than an HDD, but still slower than RAM (microseconds, and over a storage bus). SO THE CORRECT RELATIONSHIP: RAM is SMALLER in capacity and FASTER in access than a hard drive — not "larger but slower." When a program needs data, the CPU looks first in its registers, then its caches (L1/L2/L3), then RAM, and only if the data isn't in RAM does it get loaded ("paged in") from the drive. When RAM runs out, the OS moves the least-used pages to a swap file / page file on the much slower drive; the resulting constant disk access ("thrashing") is why a machine with too little RAM stutters when many apps are open. An analogy: RAM is your desk (small, everything within arm's reach); the hard drive is the filing cabinet (huge, but you have to get up and walk over to it).`,
    createdAt: Date.now(),
  },
  {
    id: 'kb-gap-cs-stack-heap-memory',
    title: "The Stack vs The Heap (in program memory)",
    category: 'Computer Science',
    keywords: [
      'what is the difference between the stack and the heap in memory call stack automatic local variables function frames LIFO fast fixed size versus dynamic heap allocation malloc new manual or garbage collected slower fragmentation not the heap data structure not heapsort',
      'when a program runs its memory is divided into regions including the STACK and the HEAP this memory HEAP is NOT the same thing as the heap DATA STRUCTURE used in heapsort and priority queues they just share a name',
      'the STACK is a region that grows and shrinks automatically as functions are called and return each function call pushes a STACK FRAME holding its parameters return address and LOCAL variables and popping it on return allocation is just moving a pointer so it is very FAST but the stack has a fixed limited size too much recursion overflows it stack overflow and stack data only lives as long as its function',
      'the HEAP is a large pool for DYNAMIC allocation memory you request explicitly at runtime with malloc or new or that a managed runtime allocates for objects it lives until you free it manually C C plus plus or until a GARBAGE COLLECTOR reclaims it Java C sharp Python it is more flexible you control the lifetime and size but slower to allocate and subject to fragmentation and leaks',
    ],
    content: `When a program runs, the operating system gives its process an address space divided into regions: the code/text segment, the data/BSS segments (globals), the STACK, and the HEAP. IMPORTANT: this memory "heap" is NOT the same thing as the HEAP DATA STRUCTURE (the binary heap used in heapsort and priority queues). They are unrelated concepts that unfortunately share a name. THE STACK (the "call stack") is a region of memory that automatically grows and shrinks with function calls. Every time a function is called, a STACK FRAME (activation record) is PUSHED onto it, containing that call's arguments, its local variables, saved registers, and the return address. When the function returns, its frame is POPPED and that memory is instantly reclaimed. Characteristics: allocation/deallocation is trivially cheap (just adjust the stack pointer); memory is automatically managed (you don't free it); it is LIFO and strictly tied to call/return nesting; but the stack has a FIXED, relatively small size (often 1–8 MB per thread), so very deep or infinite recursion, or huge local arrays, cause a STACK OVERFLOW and crash; and a value on the stack only lives until its function returns (returning a pointer to a local variable is a bug). THE HEAP is a large, general-purpose pool for DYNAMIC memory allocation — memory you request explicitly at runtime because you don't know the size or lifetime at compile time. In C you use malloc/free; in C++ new/delete; in Java, C#, Go, Python, JavaScript, objects are allocated on the heap and a GARBAGE COLLECTOR (or reference counting) frees them automatically when nothing references them anymore. Characteristics: flexible (you control size and lifetime; data can outlive the function that created it, and can be resized); but allocation is SLOWER (the allocator must search for a suitable free block, manage metadata); the heap can become FRAGMENTED; and manual management invites bugs — memory LEAKS (forgetting to free), DANGLING POINTERS / use-after-free (using freed memory), and double-free. RULE OF THUMB: small, short-lived, fixed-size data → stack; large, long-lived, or dynamically-sized data → heap.`,
    createdAt: Date.now(),
  },
  {
    id: 'kb-gap-cs-dfs-bfs',
    title: "Depth-First Search (DFS) vs Breadth-First Search (BFS)",
    category: 'Computer Science',
    keywords: [
      'what is the difference between depth first search and breadth first search graph tree traversal stack recursion go deep backtrack versus queue explore level by level shortest path unweighted',
      'both DFS and BFS are algorithms for systematically visiting every node of a graph or tree starting from a source they differ in the ORDER they visit nodes and the data structure they use',
      'DEPTH FIRST SEARCH DFS goes as DEEP as possible along one branch before BACKTRACKING it uses a STACK explicitly or the call stack via recursion memory use is proportional to the maximum depth O(h) which is low for deep narrow graphs it is used for topological sort cycle detection finding connected components maze solving and it does NOT find the shortest path in general',
      'BREADTH FIRST SEARCH BFS explores the graph LEVEL BY LEVEL visiting all nodes at distance 1 then all at distance 2 and so on it uses a QUEUE FIFO memory use can be high O(width) it holds a whole frontier its key property is that in an UNWEIGHTED graph BFS finds the SHORTEST PATH fewest edges from the source to every other node it is also used for finding all nodes within k steps and for bipartite checking for weighted shortest paths you need Dijkstra instead',
    ],
    content: `DEPTH-FIRST SEARCH (DFS) and BREADTH-FIRST SEARCH (BFS) are the two fundamental strategies for traversing (visiting every node of) a graph or tree from a starting node. They visit the same nodes but in a different ORDER, using different auxiliary data structures, with different memory profiles and different uses. DEPTH-FIRST SEARCH goes as DEEP as possible down one path before backing up. From the current node it picks an unvisited neighbour, moves to it, and repeats; when it hits a dead end (no unvisited neighbours) it BACKTRACKS to the most recent node that still has unexplored neighbours. It is naturally implemented with RECURSION (using the program's call stack) or with an explicit STACK (LIFO). Memory usage is O(h) where h is the maximum depth reached — cheap for deep, narrow graphs. DFS is the basis of: topological sorting, cycle detection, finding strongly/weakly connected components, detecting bridges and articulation points, maze generation and solving, and backtracking search (Sudoku, N-queens). DFS does NOT, in general, find the shortest path. BREADTH-FIRST SEARCH explores the graph in "rings" or LEVELS: it visits the start node, then all nodes one edge away, then all nodes two edges away, and so on. It is implemented with a QUEUE (FIFO): dequeue a node, enqueue all its unvisited neighbours, mark them, repeat. Memory usage is O(w) where w is the maximum width of a level — which can be large (potentially the whole graph). BFS's defining property: in an UNWEIGHTED graph (or one where every edge has equal cost), BFS finds the SHORTEST PATH (fewest edges) from the source to every reachable node, because it reaches each node by the minimum number of steps. It is also used to find "all nodes within k hops," to test bipartiteness / 2-colourability, and for level-order tree traversal. For shortest paths in a WEIGHTED graph, plain BFS doesn't work — you need Dijkstra's algorithm (a priority-queue generalisation) or, with negative weights, Bellman-Ford. Iterative deepening DFS combines DFS's low memory with BFS's shortest-path guarantee.`,
    createdAt: Date.now(),
  },
  {
    id: 'kb-gap-cs-bug-feature',
    title: 'A Bug vs A Feature ("It\'s not a bug, it\'s a feature")',
    category: 'Computer Science',
    keywords: [
      'what is the difference between a bug and a feature software defect unintended incorrect behavior versus intended designed functionality specification the joke it is not a bug it is a feature reclassification',
      'a BUG is a DEFECT an error flaw or fault in software that makes it behave in a way it was NOT intended to produce a wrong result crash freeze corrupt data or violate its specification bugs come from mistakes in the code design or requirements the term is often traced to a moth found in a Harvard relay by Grace Hoppers team in 1947 though engineers used bug for faults earlier',
      'a FEATURE is a piece of INTENDED designed functionality a capability the software is supposed to have described in the requirements or specification and delivered on purpose to provide value to the user',
      'the core distinction is INTENT versus SPECIFICATION does the behaviour match what was designed and documented if yes it is a feature if it is an unplanned deviation it is a bug the joke it is not a bug it is a feature refers to deciding that an unintended behaviour is actually useful or not worth fixing and simply redefining it as intended sometimes legitimate often a way to avoid admitting a mistake the reverse is a feature so unwanted users call it a bug',
    ],
    content: `A BUG and a FEATURE are opposite categories of software behaviour, distinguished by INTENT — whether the behaviour was designed and specified on purpose. A BUG (also "defect," "fault," "error," "issue") is a flaw in a program that causes it to behave in a way it was NOT intended to: producing incorrect output, crashing, hanging, leaking memory, corrupting data, exposing a security hole, or otherwise violating its specification or the developer's intent. Bugs arise from human mistakes — a typo, an off-by-one error, a wrong assumption, a missed edge case, an ambiguous requirement, a bad interaction between components. (The word "bug" for a technical fault predates computing — Edison used it in the 1870s — but it entered computing folklore when Grace Hopper's team at Harvard in 1947 taped an actual moth that had shorted a relay into the Mark II's logbook, labelled "first actual case of bug being found.") A FEATURE is a unit of INTENDED functionality — a capability the software is designed and specified to have, built deliberately to deliver value to users (dark mode, autosave, keyboard shortcuts, an export button). THE DISTINCTION is: does the behaviour match the design and specification? Behaviour that matches intent = a feature. An unplanned deviation from intent = a bug. THE JOKE "it's not a bug, it's a feature" describes the (sometimes legitimate, often face-saving) move of taking an unintended behaviour and DECLARING it to be intended — either because someone realises the accidental behaviour is actually useful and decides to keep and document it, or because fixing it isn't worth the cost so it gets "grandfathered" into the spec. The inverse also happens: a deliberately designed feature that users hate gets derided as "a bug." Between the two sits the "won't fix" / "working as intended" resolution, and "undefined behaviour" that some users have come to depend on (a "Hyrum's Law" situation).`,
    createdAt: Date.now(),
  },
  {
    id: 'kb-gap-cs-os-firmware',
    title: "Operating System vs Firmware",
    category: 'Computer Science',
    keywords: [
      'what is the difference between an operating system and firmware large general purpose software managing hardware and running applications user replaceable versus small fixed low level software embedded in a device controlling that specific hardware BIOS UEFI',
      'an OPERATING SYSTEM OS is a large general purpose software layer that manages a computers hardware and resources CPU scheduling memory files devices networking and provides services and a platform for running APPLICATION programs examples Windows macOS Linux Android iOS it is loaded from disk into RAM at boot is frequently updated and can be replaced by the user',
      'FIRMWARE is small specialised low level software stored in NON VOLATILE memory ROM flash EEPROM ON a hardware device that controls THAT specific device it sits between pure hardware and higher software it is written for one exact piece of hardware changes rarely and is updated only through a special flashing process',
      'examples of firmware the PC BIOS or UEFI that initialises the motherboard and starts the boot process the controller firmware inside an SSD hard drive router smart TV printer keyboard mouse or washing machine on a PC the firmware BIOS UEFI runs FIRST at power on does hardware initialisation then hands control to the bootloader which loads the OS so firmware is not only the boot code it is any embedded device level software',
    ],
    content: `An OPERATING SYSTEM and FIRMWARE are both software, but they differ in scope, size, where they live, how general-purpose they are, and how often they change. An OPERATING SYSTEM (OS) is a large, general-purpose software layer whose job is to (1) MANAGE the computer's hardware and resources — scheduling the CPU among processes, allocating and protecting memory, managing files and storage, driving peripherals, handling networking — and (2) provide a PLATFORM and a set of services (system calls, APIs) on which application programs run. Examples: Windows, macOS, Linux, Android, iOS, ChromeOS. The OS is stored as files on disk, LOADED INTO RAM at boot time, is large (gigabytes), is updated frequently, supports arbitrary third-party applications, and can generally be replaced or dual-booted by the user. FIRMWARE is small, specialised, low-level software that is embedded IN a piece of hardware and controls THAT SPECIFIC device. It is stored in non-volatile memory ON the device itself — ROM, flash, or EEPROM — so it's there the moment the device gets power, independent of any disk or OS. It is written for one exact hardware design, is small (kilobytes to a few megabytes), changes rarely, and is updated only via a deliberate "flashing" procedure (with some risk of "bricking" the device if it fails). Examples of firmware: the PC's BIOS or UEFI (initialises the motherboard, RAM, and buses at power-on, runs the power-on self-test, then locates and launches a bootloader); the controller firmware inside an SSD or hard drive, a router, a printer, a digital camera, a smart TV, a game controller, a keyboard, a car's engine control unit, a microwave. THE BOOT RELATIONSHIP on a PC: at power-on the FIRMWARE (UEFI/BIOS) runs FIRST, brings the hardware up to a usable state, and then hands control to a bootloader (GRUB, Windows Boot Manager) which loads the OPERATING SYSTEM kernel into RAM. So firmware is not merely "the boot code" — the boot firmware is just the most visible example of a much broader category of device-level embedded software.`,
    createdAt: Date.now(),
  },
  {
    id: 'kb-gap-cs-bigo-bigtheta-omega',
    title: "Big O vs Big Theta vs Big Omega Notation",
    category: 'Computer Science',
    keywords: [
      'what is the difference between big O and big Theta notation asymptotic upper bound versus tight bound both upper and lower big Omega lower bound not the same as worst case average case best case',
      'these are ASYMPTOTIC notations describing how a functions growth rate scales as input size n goes to infinity they are about BOUNDS ON A FUNCTION not about which input case you are analysing worst average best is a SEPARATE independent axis',
      'BIG O f(n) is O(g(n)) means g is an asymptotic UPPER BOUND f grows no faster than g up to a constant factor for large n it is a ceiling saying at most this it is correct to say insertion sort is O(n squared) and also technically O(n cubed) since O is just an upper bound though we prefer the tightest one',
      'BIG OMEGA f(n) is Omega(g(n)) means g is an asymptotic LOWER BOUND f grows at least as fast as g a floor saying at least this BIG THETA f(n) is Theta(g(n)) means g is a TIGHT bound f is BOTH O(g) and Omega(g) so f grows exactly at the rate of g merge sort is Theta(n log n) in the worst case its runtime is bounded above and below by n log n Theta is not the average case it is a two sided bound',
    ],
    content: `BIG O, BIG THETA (Θ), and BIG OMEGA (Ω) are ASYMPTOTIC notations from analysis of algorithms. They describe how a function (typically an algorithm's running time or space as a function of input size n) GROWS as n becomes large, ignoring constant factors and lower-order terms. A crucial point: these notations bound a FUNCTION; they are a SEPARATE question from which input scenario (worst case, average case, best case) that function represents. You can state a Θ, O, or Ω bound on the worst-case running time, on the average-case running time, etc. BIG O — "f(n) is O(g(n))" — means g(n) is an asymptotic UPPER BOUND on f(n): beyond some point, f(n) ≤ c·g(n) for a constant c. It says f grows "no faster than" g — a ceiling, "at most this." Because it is only an upper bound, it is technically true (if loose) to say an O(n²) algorithm is also O(n³); by convention we quote the tightest upper bound we can. BIG OMEGA — "f(n) is Ω(g(n))" — means g(n) is an asymptotic LOWER BOUND: beyond some point, f(n) ≥ c·g(n). It says f grows "at least as fast as" g — a floor, "at least this." (It is used, e.g., to say "any comparison sort is Ω(n log n)" — no comparison sort can beat that.) BIG THETA — "f(n) is Θ(g(n))" — means g(n) is a TIGHT bound: f(n) is BOTH O(g(n)) AND Ω(g(n)). The function is squeezed between c₁·g(n) and c₂·g(n) for large n, so f grows at EXACTLY the rate of g. Example: merge sort's running time is Θ(n log n) in the worst case — it is bounded both above and below by n log n. Θ is NOT "the average case" — it is a two-sided (upper + lower) bound. COMMON USAGE: people often write "O(...)" loosely where they really mean Θ (e.g. "quicksort is O(n log n) on average" usually means Θ). And note that quicksort is Θ(n log n) on average but Θ(n²) in the worst case — same algorithm, different input scenarios, each with its own tight bound.`,
    createdAt: Date.now(),
  },
  {
    id: 'kb-gap-cs-boolean-bitwise',
    title: "Boolean (Logical) Operators vs Bitwise Operators",
    category: 'Computer Science',
    keywords: [
      'what is the difference between a boolean logical operator and a bitwise operator operate on whole true false values with short circuit and or not versus operate on each individual bit of an integer AND OR XOR NOT shift',
      'BOOLEAN or LOGICAL operators in most languages written && for and || for or ! for not take whole TRUTH VALUES true or false and return a truth value they typically SHORT CIRCUIT the right operand of && is not evaluated if the left is false and of || if the left is true they are used in conditions if and while',
      'BITWISE operators written & for AND | for OR ^ for XOR ~ for NOT plus << and >> for left and right SHIFT operate on the INDIVIDUAL BITS of integer values applying the operation to each bit position in parallel 6 & 3 is 2 because 110 AND 011 is 010 they do NOT short circuit both operands are always evaluated',
      'bitwise operators are used for low level work flags packed into one integer bit masks setting clearing testing toggling individual bits fast multiply or divide by powers of two via shifts hashing and graphics a classic bug in C and JavaScript is writing & or | where you meant && or || which compiles but behaves subtly wrong',
    ],
    content: `BOOLEAN (LOGICAL) OPERATORS and BITWISE OPERATORS look similar (and in C-family languages the symbols are related: double-ampersand vs single-ampersand, double-pipe vs single-pipe) but they operate at completely different levels. BOOLEAN / LOGICAL OPERATORS work on whole BOOLEAN VALUES (true / false, or in C, zero / non-zero). In most languages: the double-ampersand is logical AND (true only if both operands are true), the double-pipe is logical OR (true if either operand is true), the exclamation mark is logical NOT (negates). They RETURN a boolean and are used to build the conditions in if, while, for, and ternary expressions. A key property: they SHORT-CIRCUIT — "a AND b" does not evaluate b at all if a is false (the result is already known to be false), and "a OR b" skips b if a is true. This is relied on for guard patterns like "if p is not null AND p.value is greater than 0". BITWISE OPERATORS work on the INDIVIDUAL BITS of integer values, applying the operation independently to each bit position: single-ampersand is bitwise AND, single-pipe is bitwise OR, caret is bitwise XOR (exclusive or — 1 if the two bits differ), tilde is bitwise NOT (flips every bit), plus the left-shift and right-shift operators. Example: 6 bitwise-AND 3 evaluates to 2, because 6 is binary 110 and 3 is binary 011, and ANDing bit-by-bit gives 010, which is 2. Bitwise operators do NOT short-circuit — both operands are always fully evaluated. USES of bitwise operators: packing many boolean flags into a single integer and then setting, clearing, toggling, or testing individual bits with masks; fast multiplication or division by powers of two via shifts (left-shifting by 3 multiplies by 8); low-level protocol and file-format parsing; hashing; cryptography; and graphics. A CLASSIC BUG in C, C++, Java, and JavaScript is writing a single-ampersand or single-pipe where you meant the doubled logical version (or vice versa): the code often still compiles and sometimes even gives the right answer for boolean-ish inputs, but it loses short-circuiting and can produce subtly wrong results (1 bitwise-AND 2 is 0, i.e. false, but 1 logical-AND 2 is true).`,
    createdAt: Date.now(),
  },
  {
    id: 'kb-gap-cs-hashtable-bst',
    title: "Hash Table vs Binary Search Tree",
    category: 'Computer Science',
    keywords: [
      'what is the difference between a hash table and a binary search tree average O(1) unordered lookup by exact key hash function collisions versus O(log n) ordered keeps elements sorted range queries in order traversal',
      'both store key value pairs and support insert delete and lookup they differ in average speed and crucially in whether they keep keys in ORDER',
      'a HASH TABLE hash map dictionary computes a HASH of the key to jump directly to a bucket giving O(1) AVERAGE time for insert delete and exact key lookup worst case O(n) if many keys collide it does NOT keep keys in any order so it CANNOT efficiently answer give me all keys between X and Y give me the smallest key or iterate in sorted order it also needs a good hash function and occasional resizing',
      'a BALANCED BINARY SEARCH TREE red black tree AVL tree keeps keys in SORTED order and guarantees O(log n) WORST case for insert delete and lookup because it is ordered it efficiently supports RANGE QUERIES successor predecessor min max and in order traversal that yields keys sorted use a hash table when you only need fast exact key access use a BST or a skip list when you need ordering C plus plus unordered_map is a hash table map is a balanced BST',
    ],
    content: `A HASH TABLE and a BINARY SEARCH TREE (BST) are both data structures for storing a collection of keys (or key-value pairs) with fast insert, delete, and lookup — but they differ in their time guarantees and, most importantly, in whether they maintain ORDER. A HASH TABLE (hash map, dictionary, associative array) stores entries in an array of "buckets." To find where a key goes, it runs the key through a HASH FUNCTION that maps it to a bucket index. Given a good hash function and a reasonable load factor, this gives O(1) AVERAGE-CASE time for insert, delete, and exact-key lookup — essentially constant, independent of the number of elements. Downsides: the WORST case is O(n) (if many keys hash to the same bucket — "collisions" — handled by chaining or open addressing); performance depends on a well-chosen hash function; the table must occasionally be resized and all entries rehashed (an amortised cost); and — the big one — a hash table stores keys in NO PARTICULAR ORDER. It cannot efficiently answer "what is the smallest key?", "what keys fall between X and Y?" (range query), "what is the next key after K?", or "iterate through all keys in sorted order." A BALANCED BINARY SEARCH TREE (e.g. a red-black tree or AVL tree) stores keys in a tree where, at every node, all keys in the left subtree are smaller and all in the right subtree are larger. Self-balancing keeps the height at O(log n), so insert, delete, and lookup are O(log n) in the WORST case (a firmer guarantee than the hash table's average-case O(1), though slower in typical use). The payoff of the ordering: a BST efficiently supports RANGE QUERIES, finding the minimum/maximum, finding a key's successor/predecessor, order statistics (with augmentation), and in-order traversal that yields the keys in sorted order — all things a hash table can't do without sorting everything first. RULE OF THUMB: need only fast lookup/insert by exact key, and don't care about order → hash table. Need keys kept sorted, range queries, or ordered iteration → balanced BST (or a skip list). In C++, std::unordered_map is a hash table and std::map is a balanced BST. In Java, HashMap versus TreeMap.`,
    createdAt: Date.now(),
  },
];
