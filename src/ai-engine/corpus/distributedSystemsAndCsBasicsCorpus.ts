import { KnowledgeItem } from '../../types';

// Code knowledge expansion (2026-09-14) — distributed systems fundamentals and remaining CS
// basics: number systems, bitwise operators, event-driven architecture, database scaling
// (sharding/replication), CAP theorem, containers/serverless. Plain neutral educational content,
// rounding out the coding corpus's depth.

const now = Date.now();
const k = (id: string, title: string, keywords: string[], content: string): KnowledgeItem => ({
  id,
  title,
  category: 'Programming',
  keywords,
  content,
  createdAt: now,
});

export const DISTRIBUTED_SYSTEMS_AND_CS_BASICS_CORPUS: KnowledgeItem[] = [
  k(
    'kb-code-binary-hex-number-systems',
    'Binary and hexadecimal number systems',
    ['binary number system', 'hexadecimal basics', 'base 2 base 16'],
    "Computers fundamentally store and process everything as binary (base 2) — sequences of 0s and 1s, corresponding to a transistor being off or on — since binary maps directly onto simple, reliable two-state electrical signals. A binary digit (\"bit\") represents one 0 or 1; a byte is a group of 8 bits, capable of representing 256 distinct values (0 through 255). Converting a binary number to decimal (base 10) involves summing powers of 2 for each 1-bit position: binary `1011` equals `1×8 + 0×4 + 1×2 + 1×1 = 11` in decimal. Hexadecimal (base 16, using digits 0-9 plus letters A-F for values 10-15) is widely used as a more human-readable, compact shorthand for binary data, precisely because each single hex digit corresponds EXACTLY to 4 binary bits (a \"nibble\"), so a full byte can always be represented cleanly with exactly 2 hex digits — this is why colors in CSS/web development are commonly written as hex codes like `#FF5733` (three bytes: red, green, blue channel values, each compactly represented as a 2-digit hex pair), and why memory addresses and low-level debugging output are conventionally shown in hexadecimal rather than the far longer, harder-to-scan equivalent binary or decimal representations.",
  ),
  k(
    'kb-code-bitwise-operators',
    'Bitwise operators',
    ['bitwise operators', 'and or xor shift bits'],
    "Bitwise operators manipulate the individual binary bits of a number directly, rather than treating it as a whole numeric value the way ordinary arithmetic operators do. `&` (AND) produces a 1 in each bit position only where BOTH operands have a 1 in that same position, commonly used for \"masking\" — isolating specific bits of interest while zeroing out the rest. `|` (OR) produces a 1 where EITHER operand has a 1, commonly used for combining multiple boolean flags together into one single combined value. `^` (XOR, exclusive or) produces a 1 only where the two operands DIFFER (exactly one of the two is 1, not both and not neither) — notably, XOR-ing a value with itself always produces 0, and applying the same XOR operation twice with the same second value returns the original value unchanged, a property historically exploited for lightweight, simple data encryption/obfuscation techniques. `<<` (left shift) shifts all bits left by a given number of positions, filling in zeros on the right, which is mathematically equivalent to multiplying by a power of 2; `>>` (right shift) does the reverse, roughly equivalent to integer division by a power of 2. Bitwise operations are commonly used for compact, memory-efficient storage of multiple boolean flags packed into a single integer (a \"bitmask\"), low-level networking and file-format protocol parsing, and performance-critical code where the underlying arithmetic operations they enable are typically extremely fast at the hardware level.",
  ),
  k(
    'kb-code-event-driven-architecture',
    'Event-driven architecture',
    ['event driven architecture', 'pub sub architecture pattern'],
    "Event-driven architecture structures a system around producing, detecting, and reacting to discrete \"events\" (something meaningful just happened — an order was placed, a file finished uploading, a user signed up) rather than components calling each other directly and synchronously in a fixed, predetermined sequence. A component that experiences an event PUBLISHES it (often to a message broker or event bus) without needing to know, or even care, exactly which other components — if any — are actually listening for and interested in that specific event. Any number of other components can independently SUBSCRIBE to specific event types and react however each one individually needs to, entirely independently of one another — when an \"order placed\" event fires, an inventory service might decrement stock, a notification service might send a confirmation email, and an analytics service might separately log the sale, all completely independently reacting to that exact same single published event, with none of them needing to know anything at all about the others' existence. This loose coupling between event producers and event consumers makes it comparatively easy to add entirely new functionality later (a new service can simply start listening for existing already-published events without requiring ANY changes at all to the original code that produces them) but can make a system's overall end-to-end behavior somewhat harder to trace and fully understand at a glance, since the full effective chain of what specifically happens in response to a given event isn't visible or traceable from any single one localized place in the code.",
  ),
  k(
    'kb-code-cap-theorem',
    'The CAP theorem',
    ['cap theorem', 'consistency availability partition tolerance'],
    "The CAP theorem states that a distributed data system can genuinely guarantee at most TWO of the following three properties simultaneously, never fully all three at once: Consistency (every node in the system always returns the exact same, most recent, correct data for a given read, no matter which specific node actually happens to answer that particular request), Availability (every request genuinely receives some response, even if a specific node is currently unable to fully guarantee it has the absolute latest, freshest data), and Partition tolerance (the system keeps working correctly even when a network failure genuinely splits it into multiple, mutually unreachable groups of nodes that temporarily can no longer communicate with each other at all). Since real-world networks WILL experience partitions at some point eventually, partition tolerance is generally treated in practice as effectively mandatory and non-negotiable for any genuinely distributed system, which means the real, practical everyday choice mostly comes down to trading consistency against availability specifically during an actual partition event: a CP system will refuse to answer certain requests during a partition rather than risk returning potentially stale or genuinely incorrect data, while an AP system will still answer every request during a partition, but can't fully guarantee every single node's answer is the absolute latest, most current version (this weaker but still useful guarantee is called \"eventual consistency\" — every node WILL eventually converge on the same final answer once the partition is fully resolved, just not necessarily instantly the moment a partition first begins).",
  ),
  k(
    'kb-code-database-sharding-replication',
    'Database sharding and replication',
    ['database sharding', 'database replication basics'],
    "Sharding and replication are two distinct, complementary strategies for scaling a database beyond what a single server can comfortably handle on its own. Sharding (also called horizontal partitioning) splits a single large dataset across MULTIPLE separate database servers, with each individual shard holding only a distinct PORTION of the overall total data — for example, users with IDs 1-1,000,000 on one specific shard, users 1,000,001-2,000,000 on a genuinely different shard — which lets total storage capacity and write throughput scale out roughly linearly by simply adding more shard servers, at the real cost of added query complexity (a query that legitimately needs data spanning multiple different shards at once, like a global aggregate count across the entire user base, now has to gather and combine results from several separate shards instead of running as one single simple query against just one server). Replication instead keeps multiple full, complete COPIES of the exact same entire dataset on several different servers — a common setup has one primary/\"leader\" node accepting all writes, which then propagates each change out to one or more read replicas, letting read-heavy traffic be spread out and scaled across many replica servers (since reads vastly outnumber writes in most typical real-world applications) while writes still ultimately funnel safely through the single primary node, and replicas also provide meaningful redundancy — if the primary node fails outright, a replica can typically be promoted relatively quickly to take over and become the new primary, minimizing overall downtime.",
  ),
  k(
    'kb-code-containers-orchestration',
    'Container orchestration basics',
    ['kubernetes basics', 'container orchestration'],
    "Running one container manually with `docker run` works fine for a single instance on a single machine, but a real production system typically needs many containers running across many different machines, needs to automatically restart a container that unexpectedly crashes, needs to automatically scale the number of running instances up or down based on current actual traffic load, and needs to gracefully handle rolling out new versions without any real user-facing downtime — an orchestration tool automates all of this. Kubernetes (often abbreviated \"K8s\") is by far the most widely used container orchestration platform: you declare the DESIRED overall state (\"I want 5 replicas of this specific container image running at all times, each with at least this much CPU and memory available\") in a configuration file, and Kubernetes continuously and automatically works to actually maintain that desired state — if a container crashes or an entire underlying node goes down, Kubernetes automatically detects that and starts new replacement containers elsewhere to restore the originally declared desired count. Core Kubernetes concepts include a Pod (the smallest deployable unit, typically wrapping one or a small handful of tightly-coupled containers together), a Deployment (manages a specified desired number of replica Pods and handles rolling updates smoothly between versions), and a Service (provides one single, stable network address that reliably routes to whichever specific set of Pods is currently actually healthy and running at any given moment, even as individual underlying Pods themselves are created and destroyed over time).",
  ),
  k(
    'kb-code-serverless-basics',
    'Serverless computing basics',
    ['serverless basics', 'what is serverless', 'functions as a service'],
    "Serverless computing (despite its somewhat misleading name — servers obviously still exist and run the code somewhere) lets developers deploy individual functions without provisioning, explicitly managing, or continuously running any dedicated server themselves — the cloud provider automatically runs the developer's uploaded code only in direct response to specific trigger events (an incoming HTTP request, a new file uploaded to storage, a scheduled timer), and critically, automatically scales the number of concurrent running instances up or down as needed, including all the way down to zero actively running instances (and correspondingly zero ongoing cost) when the function genuinely isn't being actively used at all at that moment. This model (commonly called \"Functions as a Service,\" or FaaS — AWS Lambda, Google Cloud Functions, and Azure Functions are well-known concrete examples) can meaningfully reduce operational overhead and cost for genuinely sporadic, bursty, or otherwise unpredictable workloads, since you generally only pay for the actual precise compute time your code genuinely uses, rather than continuously paying for an always-on server that sits mostly idle much of the time. The tradeoffs include \"cold starts\" (a noticeable, sometimes meaningful added delay the very first time a function runs after a period of genuine inactivity, since a fresh running instance needs to be freshly initialized from scratch before it can actually begin processing that specific request), individual execution time limits imposed by the specific provider, and generally reduced direct control over the underlying execution environment compared to managing one's own dedicated, fully self-controlled server.",
  ),
  k(
    'kb-code-stack-trace-reading',
    'How to read a stack trace',
    ['reading a stack trace', 'stack trace basics'],
    "A stack trace is the detailed report a program prints when an unhandled error/exception occurs, showing the exact sequence of function calls that were actively in progress at the precise moment the error happened — reading it correctly is one of the single most useful everyday debugging skills, yet many beginners initially skip past it, mistakenly assuming it's just unreadable, intimidating noise. The trace lists each active function call as a distinct \"frame,\" generally ordered from the innermost point where the actual error specifically occurred (often listed first or at the very top) outward through each successive calling function that led there, all the way out to the program's own original entry point. The very first line usually states the error TYPE and its specific accompanying message (e.g. `TypeError: Cannot read property 'name' of undefined`), which alone is often already enough information to correctly diagnose many common, simple bugs even before examining the rest of the full trace at all. Each subsequent frame typically shows the specific file name, exact line number, and function name where that particular step of the call chain occurred — the most immediately useful, actionable frame to start investigating first is usually the topmost one that actually points into YOUR OWN application code specifically, rather than deep into some third-party library's or the language runtime's own internals, since a bug is generally far more likely to be found and genuinely fixable in your own code than somewhere buried deep inside a well-tested, widely-used external library or the language's own core runtime.",
  ),
  k(
    'kb-code-try-catch-finally-patterns',
    'try/catch/finally patterns across languages',
    ['try catch finally', 'exception handling pattern comparison'],
    "Most mainstream languages (Java, JavaScript, C#, Python with its slightly different `except` keyword, C++) share broadly the same fundamental error-handling structure: a `try` block wraps code that might potentially fail, one or more `catch`/`except` blocks handle specific anticipated failure types if and when they actually occur, and an optional `finally` block runs unconditionally afterward — regardless of whether an exception was actually thrown at all, and regardless of whether it was ultimately successfully caught and handled or instead allowed to keep propagating further up the call stack uncaught. `finally` is specifically the correct, reliable place for essential cleanup code that absolutely must run no matter what happens overall — releasing a lock, closing an open file handle or network connection, since it's the one guaranteed execution point that reliably runs even if the `try` block itself returns early, or even if the exception ultimately ends up going fully unhandled and propagating further upward past this specific function entirely. A commonly recommended general best practice across virtually all of these languages: catch only the SPECIFIC exception types you actually genuinely know how to meaningfully handle and recover from, rather than reflexively catching an overly broad base exception type (like `Exception` or `Error`) and potentially silently swallowing a genuinely unrelated, unexpected bug that really should have been allowed to visibly propagate and be immediately noticed, rather than being accidentally hidden away and effectively ignored by an overly broad catch block that was really only ever intended for one specific, narrower kind of anticipated failure.",
  ),
  k(
    'kb-code-boolean-logic-basics',
    'Boolean logic basics',
    ['boolean logic', 'and or not truth table'],
    "Boolean logic, named after mathematician George Boole, operates specifically on just two possible values — true and false — and forms the fundamental underlying basis of every conditional statement in every programming language. The three basic boolean operators are AND (`&&` in most C-family languages), OR (`||`), and NOT (`!`). AND returns true only when BOTH of its operands are true (true if A is true AND B is true; false in every other case). OR returns true when AT LEAST ONE of its operands is true (false only when both A and B are false). NOT simply inverts a single value (`!true` is `false`, and vice versa). These combine to build arbitrarily complex conditional logic: `if (age >= 18 && hasLicense) { }` requires BOTH conditions to hold true simultaneously, while `if (isAdmin || isOwner) { }` requires just one or the other (or both) to hold true. Most languages also apply \"short-circuit evaluation\" specifically as a deliberate optimization and safety mechanism: in `a && b`, if `a` alone already evaluates to false, `b` is never even evaluated at all, since the overall final result is already fully determined regardless of what `b` would have evaluated to — this is commonly and deliberately exploited in real code as a concise safety-guard pattern, like `user && user.name`, which safely avoids ever attempting to access `.name` at all unless `user` has already first been separately confirmed to actually be truthy and genuinely exist.",
  ),
  k(
    'kb-code-arrays-vs-objects-dictionaries',
    'Arrays vs objects/dictionaries as general containers',
    ['array vs object as data structure', 'when to use an array vs a dictionary'],
    "Choosing between an array (ordered, indexed by consecutive integer position) and an object/dictionary (unordered or insertion-ordered, indexed instead by arbitrary named keys) as a general container for data comes down to how the individual items are naturally most meaningfully identified and accessed. An array is the natural fit when items are genuinely a SEQUENCE and their relative order actually matters, and when they're most naturally accessed either by numeric position (\"give me the third item\") or by simply iterating through every item in turn from start to finish — a list of chat messages in chronological order, a list of leaderboard scores, or the individual steps of a specific sequential process are all well-suited to an array. An object/dictionary is the natural fit instead when items are more naturally identified by a specific, meaningful, named identifier rather than by their numeric position — a mapping from user ID directly to that specific user's profile data, or a configuration object mapping named setting keys directly to their corresponding current values, are far more naturally and directly expressed as key-value pairs than forced awkwardly into a numerically-indexed array where you'd otherwise have to separately remember or somehow track which specific numeric index corresponds to which particular meaningful item. Many practical, real-world data structures genuinely combine and nest both together — an array of objects (a list of user records, where each individual user itself is represented as an object with clearly named fields like `name` and `email`) is an extremely common and natural shape for exactly this reason, since it lets each level use whichever specific access pattern is most natural and appropriate for that particular level of the data.",
  ),
  k(
    'kb-code-code-coverage-static-analysis',
    'Code coverage and static analysis',
    ['code coverage testing', 'static analysis vs dynamic analysis'],
    "Code coverage measures what PERCENTAGE of a codebase's actual lines (or branches, or individual statements) are genuinely executed at least once by running the existing automated test suite, expressed as a specific percentage — 80% line coverage means roughly 80% of all the code's lines were actually run by at least one test somewhere in the suite. High coverage is generally a genuinely good, reassuring signal, but it's crucially not a fully sufficient one on its own: a line of code being merely EXECUTED by some test doesn't necessarily mean that test actually meaningfully verified the resulting behavior was actually genuinely correct — a poorly written test could easily call a function and never actually check or assert anything meaningful whatsoever about its returned result, still fully counting as \"covering\" that code from a raw metric standpoint despite verifying essentially nothing useful about its real correctness. Static analysis examines source code directly WITHOUT ever actually running it at all, checking for potential bugs, security vulnerabilities, or style issues directly from the code's own structure alone (this is fundamentally what a linter does, among other more specialized static analysis tools). Dynamic analysis, by contrast, examines a program's actual real behavior specifically WHILE it's genuinely running — code coverage tools themselves are one clear example of dynamic analysis, as are memory-leak detectors and dedicated runtime performance profilers, all of which necessarily require the code to actually be running live in order to observe and measure anything meaningful about its real behavior at all.",
  ),
  k(
    'kb-code-interfaces-vs-abstract-classes-general',
    'Interfaces vs abstract classes (general, cross-language)',
    ['interface vs abstract class general', 'when to use interface vs abstract class'],
    "Across most mainstream object-oriented languages, an interface defines a CONTRACT — a specific set of method signatures a class commits to implementing — without itself providing any concrete shared implementation (some languages, like modern Java and Kotlin, do now allow interfaces to include limited default method implementations, but the CORE original, defining concept remains contract-first). An abstract class, by contrast, can combine both a defined contract AND real, concrete shared implementation together, and can also hold actual shared instance state (fields) that concrete subclasses directly inherit and share. Since most mainstream languages allow a class to implement multiple SEPARATE interfaces simultaneously but extend only ONE single parent class (interfaces themselves imposing no actual inheritance-related structural constraint the way extending a base class does), interfaces are generally best used specifically to describe a capability or role that fundamentally UNRELATED classes might all independently need to satisfy (both a `Duck` and an `Airplane` might both reasonably implement a shared `Flyable` interface despite being otherwise completely unrelated types), while abstract classes are generally better suited specifically for closely, genuinely related classes that legitimately share substantial common state and implementation logic in an actual meaningful \"is-a\" family hierarchy (multiple different `Shape` subtypes sharing a common `area()` calculation helper method and shared `color` field, for instance). A commonly cited practical rule of thumb: favor interfaces by default for defining and describing pure behavioral CAPABILITIES, and reach for an abstract class specifically when genuine shared implementation code or shared underlying state actually needs to be reused directly across multiple, closely-related concrete subclasses.",
  ),
];
