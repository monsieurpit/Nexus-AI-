import { KnowledgeItem } from '../../types';

// Batch 312 corpus fixes — coding-priority batch (2026-09-14). 23/25 misses.
// Topic: message queues & event-driven architecture patterns (Kafka vs RabbitMQ, delivery
// guarantees, consumer groups, dead-letter/retry queues, backpressure, CQRS, saga vs distributed
// transaction, outbox pattern, push/pull consumption, ordering, brokers, event vs command,
// choreography vs orchestration, idempotent producers/consumers, durable vs transient queues,
// ack/nack, topic partitions vs shards, event-driven vs request-response, Kafka retention), plus
// the Active Record vs Data Mapper ORM pattern — chosen because grep confirmed none of this was
// covered anywhere in the existing 300+ corpus files (only basic pub/sub, Observer-vs-pub/sub,
// and a one-line message-queue-basics entry existed; ORM basics existed but never contrasted
// Active Record vs Data Mapper specifically).
// Several severe wrong-domain hallucinations: "broker vs message queue" answered about securities
// brokers/dealers; "idempotent consumers/producers" answered about ecology (trophic levels, food
// webs); "ack vs nack" answered about Discord server-rules acknowledgment bots; "topic partition
// vs queue shard" answered entirely about database table/row partitioning and sharding, never
// touching Kafka; "competing consumers vs fan-out consumers" answered about economics/shoppers;
// "choreography vs orchestration" dumped generic Wikipedia text about IT orchestration and SOA,
// never explaining choreography at all. One severe factual inversion: "at-least-once vs
// at-most-once" and "at-least-once vs exactly-once" both got at-least-once backwards, describing
// it as fire-and-forget/may-get-lost (that's actually at-most-once) rather than guaranteed
// delivery with possible duplicates. One hallucination mid-answer: "message ordering in a queue
// vs topic partition" confused a FIFO queue with merge sort and O(n log n) complexity. Many
// one-sided cutoffs: dead-letter vs retry queue, backpressure vs rate limiting, CQRS vs normal
// API, durable vs transient queue, and Active Record vs Data Mapper (cut off before saying
// anything substantive about either pattern). "Event vs command" gave an imprecise/incorrect
// framing (commands described as "actions taken based on events" rather than as imperative
// requests to a specific handler that can be rejected). "Event-driven vs request-response"
// incorrectly conflated request-response with polling ("constantly ask... over and over").

const now = Date.now();
const k = (id: string, title: string, keywords: string[], content: string): KnowledgeItem => ({
  id,
  title,
  category: 'Programming',
  keywords,
  content,
  createdAt: now,
});

export const MESSAGE_QUEUES_EVENT_DRIVEN_ARCHITECTURE_GAPS: KnowledgeItem[] = [
  k(
    'kb-gap-code-mq-vs-pubsub-topic',
    'Message queue vs pub/sub topic',
    ['message queue vs pub sub topic', 'difference queue topic messaging', 'queue vs topic'],
    "Both let producers hand off work asynchronously, but they differ in how many consumers get each message. A QUEUE delivers each individual message to exactly ONE consumer — if three worker processes are all reading from the same queue, a given message goes to whichever one happens to pick it up first, and the others never see it; this is the natural model for distributing WORK across a pool of workers (e.g. resizing uploaded images), since you want each job done once, not repeated by every worker. A PUB/SUB TOPIC instead broadcasts each published message to EVERY subscriber currently listening — if three services subscribe to an 'order placed' topic, all three independently receive their own copy of that same message; this is the natural model for NOTIFYING multiple independent parts of a system about the same event (inventory, email, analytics all reacting to one order). Many real systems blur the line (Kafka topics support both: within one consumer group, messages are load-balanced like a queue; across different consumer groups, each group gets its own full copy like pub/sub) but the underlying design question is always the same. The key difference: a queue hands each message to exactly one consumer (competing for work), while a pub/sub topic broadcasts each message to every subscriber (fanning out the same event to many independent listeners).",
  ),
  k(
    'kb-gap-code-kafka-vs-rabbitmq',
    'Kafka vs RabbitMQ',
    ['kafka vs rabbitmq', 'difference kafka rabbitmq', 'rabbitmq vs kafka'],
    "Both are message brokers, but they were built around fundamentally different data models. RabbitMQ is a traditional MESSAGE BROKER built on the AMQP protocol: producers publish messages to an 'exchange', which routes each message to one or more queues based on routing rules (direct, topic, fanout), and once a consumer successfully processes and acknowledges a message it is typically REMOVED from the queue — RabbitMQ is optimized for complex routing logic, per-message priority, and low-latency task distribution to worker pools, and it's a natural fit for traditional task-queue workloads (background jobs, RPC-style request/reply). Kafka is fundamentally a DISTRIBUTED, APPEND-ONLY LOG: producers write messages to a 'topic' which is split into ordered partitions, and messages are RETAINED on disk for a configured period (hours to forever) regardless of whether they've been consumed — multiple independent consumer groups can each read the same topic from any offset they choose, replaying old messages if needed, which makes Kafka suited for very high-throughput event streaming, event sourcing, and feeding multiple downstream systems (analytics, search indexing, a live dashboard) from the same firehose of events. The key difference: RabbitMQ is a smart broker that routes and typically deletes messages once consumed, tuned for flexible task/work distribution, while Kafka is a durable, replayable distributed log built for very high-throughput event streaming where many independent consumers can read the same retained data at their own pace.",
  ),
  k(
    'kb-gap-code-at-least-once-vs-at-most-once',
    'At-least-once vs at-most-once message delivery',
    ['at least once vs at most once delivery', 'at most once vs at least once', 'message delivery guarantees'],
    "These are two of the three standard delivery guarantees for messaging systems, and they trade off duplicates against lost messages. AT-MOST-ONCE delivery sends a message a maximum of one time and never retries — if it's lost in transit, or the consumer crashes before finishing, the message is simply gone forever; nothing follows up. This is the 'fire and forget' option: fast and simple, with zero risk of processing the same message twice, but a real risk of silently losing messages, so it's only appropriate when occasional loss is acceptable (e.g. a non-critical metrics ping). AT-LEAST-ONCE delivery is the opposite trade: the sender keeps the message and RETRIES until it receives a positive acknowledgment (ack) from the consumer — if the ack is lost, or the consumer crashes after processing but before acking, the sender will resend, meaning the SAME message can genuinely be delivered and processed more than once. This guarantees no message is ever silently dropped, but it pushes the responsibility for handling duplicates onto the consumer, which is why at-least-once systems are usually paired with idempotent processing (so reprocessing the same message twice causes no extra harmful side effect). The key difference: at-most-once never retries and can silently lose messages but never duplicates them, while at-least-once retries until acknowledged and never silently loses messages but can deliver the same message more than once.",
  ),
  k(
    'kb-gap-code-at-least-once-vs-exactly-once',
    'At-least-once vs exactly-once message delivery',
    ['at least once vs exactly once delivery', 'exactly once vs at least once', 'exactly once semantics'],
    "AT-LEAST-ONCE delivery guarantees a message is never silently lost — the sender retries until it gets an acknowledgment — but that retry logic means the SAME message can genuinely be delivered and processed more than once (e.g. the consumer processed it fine but the ack got lost, so the sender resends), so the receiving application must be built to tolerate duplicates, typically by making its processing idempotent (applying the same message twice has no extra effect, e.g. using a unique message ID to skip ones already handled). EXACTLY-ONCE delivery is the stronger — and far harder to actually achieve — guarantee that each message is processed EXACTLY one time: never lost, and never duplicated, from the application's point of view. True exactly-once semantics generally require the messaging system to combine at-least-once delivery (retries, so nothing is lost) with deduplication or transactional processing at the consumer side (so a redelivered message is detected and effectively ignored rather than double-applied) — Kafka's 'exactly-once semantics' (EOS) achieves this via idempotent producers plus transactional writes across the log and any downstream state, and it comes at a real cost in throughput and complexity compared to plain at-least-once. The key difference: at-least-once can redeliver and reprocess the same message (so the app must handle duplicates itself), while exactly-once guarantees each message has effect exactly one time, which requires extra machinery (deduplication or transactions) and is significantly more expensive to implement correctly.",
  ),
  k(
    'kb-gap-code-kafka-consumer-group-vs-single-consumer',
    'Consumer group vs single consumer (Kafka)',
    ['kafka consumer group vs single consumer', 'what is a kafka consumer group', 'consumer group'],
    "A single, standalone consumer just reads messages off a Kafka topic on its own — simple, but limited to whatever throughput one process can handle, and if it goes down, nothing is consuming until it comes back. A CONSUMER GROUP is a named set of consumer instances that cooperate to consume a topic together: Kafka automatically divides the topic's partitions among the group's members, so each partition is read by exactly ONE consumer within that group at a time — with a topic split into 6 partitions and 3 consumers in a group, each consumer typically gets 2 partitions, letting the group process messages in parallel far faster than any single consumer could alone. If a consumer in the group crashes, Kafka automatically 'rebalances,' reassigning its partitions to the remaining members so processing continues with no manual intervention. Crucially, groups are also how Kafka supports both queue-like and pub/sub-like behavior from the same topic: consumers WITHIN one group share the work (each message goes to only one of them, like a queue), while DIFFERENT consumer groups reading the same topic each independently get their own full copy of every message (like pub/sub) — an analytics service and a notifications service can both consume the exact same topic from entirely separate consumer groups without interfering with each other at all. The key difference: a single consumer processes a topic alone with no automatic parallelism or failover, while a consumer group lets multiple consumer instances split a topic's partitions for parallel processing, with Kafka automatically rebalancing partition assignments if a member joins or fails.",
  ),
  k(
    'kb-gap-code-competing-vs-fanout-consumers',
    'Competing consumers vs fan-out consumers',
    ['competing consumers vs fan out consumers', 'competing consumer pattern', 'fan out messaging pattern'],
    "These are two opposite messaging patterns for how multiple consumers relate to the same stream of messages, NOT about shoppers or economic competition. The COMPETING CONSUMERS pattern puts several consumer instances against a single shared queue, all racing to pull the next available message — each individual message is handled by only ONE of them, so adding more competing consumers increases total processing throughput by spreading the workload, which is the standard way to scale out a background job/worker system (e.g. five worker processes all pulling from one 'resize this image' task queue, each job done exactly once by whichever worker grabs it first). The FAN-OUT pattern instead broadcasts a single message out to MULTIPLE independent consumers, each of which receives and processes its OWN full copy of that same message — commonly implemented by having a producer publish to an exchange/topic that fans out to several separate queues, one per interested consumer, so (for example) one 'order placed' event triggers a completely independent inventory-update consumer, a completely independent email consumer, and a completely independent analytics consumer, each seeing the whole message rather than splitting it up. The key difference: competing consumers split a workload so each message is processed once by whichever consumer picks it up (scaling throughput), while fan-out broadcasts the same message to multiple independent consumers who each process their own full copy (scaling reactions to an event).",
  ),
  k(
    'kb-gap-code-dead-letter-vs-retry-queue',
    'Dead-letter queue vs retry queue',
    ['dead letter queue vs retry queue', 'dlq vs retry queue', 'difference dead letter retry queue'],
    "Both are ways messaging systems handle messages that fail to process, but they represent different stages of giving up. A RETRY QUEUE holds a message that failed temporarily, so it can be attempted again — usually after a delay (often with exponential backoff, waiting progressively longer between attempts) to give whatever caused the failure (a brief network blip, a downstream service that was momentarily overloaded) a chance to clear up before trying once more; the assumption is the failure is likely transient and the message is still worth processing. A DEAD-LETTER QUEUE (DLQ) is where a message ends up once it has failed too many times, or failed in a way that's clearly not going to succeed no matter how many times you retry it (the message itself is malformed, or references data that no longer exists) — it is deliberately NOT retried automatically anymore; instead it's set aside for a human or a separate process to inspect, fix, or discard, so a genuinely broken message doesn't get stuck endlessly retrying and clogging the main processing pipeline forever. A typical pipeline chains both: a failed message goes to a retry queue for a few attempts with backoff, and only after exhausting those retries does it finally get routed to the DLQ for manual investigation. The key difference: a retry queue re-attempts a message automatically (assuming the failure might be transient), while a dead-letter queue is the final resting place for messages that have exhausted retries or are known unprocessable, held for manual handling instead of endless automatic retrying.",
  ),
  k(
    'kb-gap-code-backpressure-vs-rate-limiting',
    'Backpressure vs rate limiting',
    ['backpressure vs rate limiting', 'difference backpressure rate limiting', 'what is backpressure'],
    "Both slow down the flow of requests or data, but they differ in who decides the limit and why. RATE LIMITING is an externally imposed, fixed cap set in advance — 'this client may make at most 100 requests per minute' — enforced regardless of whether the system is actually struggling right now; it's a policy decision (often for fairness between clients, cost control, or abuse prevention) applied uniformly whether the server is idle or already at capacity, and requests over the limit are typically just rejected outright (HTTP 429). BACKPRESSURE is a dynamic, self-protective mechanism where a system that is genuinely being overwhelmed signals UPSTREAM (back toward whoever is sending it work) to slow down or pause — a consumer that's falling behind can stop pulling new messages off a queue until it catches up, or a stream can push back on its source rather than letting an internal buffer grow unboundedly and eventually run out of memory or crash. Backpressure reacts to actual real-time load and internal capacity, propagating the slowdown signal back through the chain of producers, whereas rate limiting enforces a predetermined ceiling regardless of current load. The key difference: rate limiting is a fixed, predetermined cap applied to protect a resource or enforce fairness regardless of current load, while backpressure is a dynamic signal from an overwhelmed component telling upstream producers to slow down specifically because it's currently struggling to keep up.",
  ),
  k(
    'kb-gap-code-cqrs-vs-normal-api',
    'CQRS vs a normal read/write API',
    ['cqrs vs normal api', 'what is cqrs', 'command query responsibility segregation'],
    "A normal read/write API uses the SAME model (the same classes/tables/endpoints) for both reading data and writing it — a `User` object and its underlying table are used both when you fetch a user's profile and when you update it, which is simple and familiar, but that one shared model has to be a reasonable compromise for both very different jobs (writes care about validation and consistency; reads often care about speed and convenient shape for display). CQRS (Command Query Responsibility Segregation) deliberately SPLITS the two: a 'command' side handles all writes, enforcing business rules and validation and updating the source-of-truth data, while a completely separate 'query' side handles all reads, often against its own independently optimized read model (which might be a denormalized, pre-joined, even differently-structured copy of the data built specifically for fast querying, kept in sync with the write side — sometimes via events published whenever a write happens). This lets each side be scaled, optimized, and even modeled completely independently — the read side can use a fast cache or search index while the write side stays strict and transactional — at the cost of real added complexity (two models to maintain, and the read model is often only EVENTUALLY consistent with the write side, meaning a read immediately after a write might briefly return stale data). The key difference: a normal API uses one shared model for both reading and writing, while CQRS splits reads and writes into separate models/pathways that can be independently optimized and scaled, at the cost of extra complexity and often eventual (rather than immediate) consistency between them.",
  ),
  k(
    'kb-gap-code-saga-vs-distributed-transaction',
    'Saga pattern vs distributed transaction',
    ['saga pattern vs distributed transaction', 'saga vs two phase commit', 'what is the saga pattern'],
    "Both coordinate an operation that spans multiple services/databases so it either all succeeds or is properly undone, but they take opposite approaches to achieving that. A DISTRIBUTED TRANSACTION (classically implemented via two-phase commit, 2PC) tries to make the whole operation ATOMIC the traditional way: a coordinator asks every participant to 'prepare' (lock its resources and confirm it CAN commit) and only once every single participant says yes does the coordinator tell them all to actually commit together — this gives strong, immediate all-or-nothing consistency, but it requires holding locks across every participant for the whole duration, doesn't scale well across many services or across network/service boundaries (a single slow or unreachable participant blocks everyone), and is largely avoided in modern distributed/microservice systems for exactly that reason. The SAGA PATTERN instead breaks the overall operation into a sequence of separate LOCAL transactions, each in one service, with NO cross-service locking at all — each step commits independently and immediately; if a later step fails, the saga doesn't roll back a lock, it runs explicit COMPENSATING actions for each already-completed step to semantically undo them (e.g. a 'refund payment' compensating action for an earlier 'charge card' step), moving backward through what already succeeded. This trades strict, immediate atomicity for much better scalability and availability, accepting that the system passes through temporarily inconsistent intermediate states along the way. The key difference: a distributed transaction (2PC) holds locks across all participants to commit everything together atomically in one shot, while a saga runs a sequence of independent local transactions with no cross-service locking, using compensating actions to undo already-completed steps if a later step fails.",
  ),
  k(
    'kb-gap-code-outbox-pattern-vs-direct-publish',
    'Outbox pattern vs publishing an event directly',
    ['outbox pattern vs direct event publish', 'transactional outbox pattern', 'what is the outbox pattern'],
    "Publishing an event directly means a service updates its own database and THEN, as a separate follow-up step, sends a message to a broker — the problem is these are two independent operations against two independent systems (the database and the broker) with no shared atomicity between them: this is the classic 'dual write' problem. If the service crashes (or the broker call simply fails) after the database commit but before the message is actually published, the data change happened but the event was silently never sent, leaving downstream systems permanently out of sync with no error raised anywhere. The OUTBOX PATTERN solves this by never doing a truly separate write: instead of publishing directly, the service writes the event as a ROW in an 'outbox' table in the SAME database, as part of the SAME database transaction as the actual business data change — since both writes are in one transaction, they succeed or fail together atomically, so it's impossible for the data change to be committed without the corresponding outbox row also being committed. A separate background process (or a change-data-capture tool reading the database's write-ahead log) then reads new rows from the outbox table and actually publishes them to the message broker, retrying as needed until it succeeds, then marks/deletes them from the outbox once confirmed published. The key difference: publishing directly is two unsynchronized writes to two different systems that can fail independently (risking a silently lost or missing event), while the outbox pattern writes the event to the SAME database transaction as the business data change, guaranteeing atomicity, and relies on a separate reliable relay process to actually deliver it to the broker afterward.",
  ),
  k(
    'kb-gap-code-push-vs-pull-consumption',
    'Push-based vs pull-based message consumption',
    ['push based vs pull based message consumption', 'push vs pull messaging consumer', 'push consumer vs pull consumer'],
    "These describe who initiates the transfer of a message from broker to consumer. In PUSH-based consumption, the message broker itself actively sends (pushes) each new message to the consumer the moment it's available, without the consumer having to ask — the consumer just registers a callback/handler and the broker calls it whenever there's work, which gives very low latency (messages are delivered essentially instantly) but risks overwhelming a slow consumer if the broker pushes messages faster than that consumer can actually keep up (requiring the broker to implement its own flow control to avoid this). In PULL-based consumption, the consumer itself actively reaches out and asks the broker 'give me the next batch of messages' whenever it is ready for more — the consumer controls its own pace entirely, only ever pulling as much as it can currently handle, which naturally prevents it from being overwhelmed (a form of built-in backpressure) at the cost of some added latency (a message might sit waiting until the consumer's next pull) and the overhead of the consumer having to poll or maintain a pull loop. Kafka consumers are a classic pull-based design specifically so each consumer can control its own throughput; RabbitMQ supports both push (via `basic.consume`) and pull (via `basic.get`) styles. The key difference: push-based delivery has the broker actively send messages to the consumer the instant they're available (lower latency, risk of overwhelming a slow consumer), while pull-based delivery has the consumer actively request messages at its own pace (consumer controls throughput, at the cost of some added latency).",
  ),
  k(
    'kb-gap-code-queue-vs-partition-ordering',
    'Message ordering: a queue vs a topic partition',
    ['message ordering queue vs topic partition', 'ordering guarantees message queue', 'kafka partition ordering'],
    "A simple, single FIFO queue naturally preserves the exact order messages were enqueued: message A published before message B will always be delivered before B, because there's only one ordered line and (with a single consumer) messages are handled strictly in that same sequence — this is a basic property of a first-in-first-out data structure, unrelated to sorting algorithms like merge sort. The moment you add MULTIPLE competing consumers to that same single queue, though, strict overall ordering is no longer guaranteed across consumers, since two messages could be picked up and processed by two different consumers running at different speeds, finishing in either order. A Kafka-style TOPIC PARTITION handles this differently and more deliberately: Kafka only guarantees ordering WITHIN a single partition (messages written to the same partition are always read back in that exact order by any one consumer), but a topic is typically split across MANY partitions, and there is no ordering guarantee AT ALL across different partitions — two messages sent to different partitions of the same topic can be read in either order relative to each other. To get ordering for related messages, producers assign them the same partition key (e.g. all events for a given user ID go to the same partition based on a hash of that key), guaranteeing those specific related messages stay in order while still letting the topic scale out across many partitions and parallel consumers overall. The key difference: a plain single-consumer queue guarantees global order for everything in it, while a partitioned topic guarantees order only within each individual partition (not across partitions), trading strict global ordering for the ability to parallelize across many partitions/consumers, with producers using a partition key to keep specifically related messages in order.",
  ),
  k(
    'kb-gap-code-broker-vs-message-queue',
    'Message broker vs message queue',
    ['message broker vs message queue', 'what is a message broker', 'difference broker queue messaging'],
    "In a messaging-systems context (not securities/stock trading), a MESSAGE BROKER is the overall middleware SOFTWARE/SERVER that receives, routes, stores, and delivers messages between producers and consumers — RabbitMQ, Kafka, and ActiveMQ are all message brokers; the broker is the whole system that runs as a service, handles connections, applies routing logic, persists messages to disk, and manages delivery. A MESSAGE QUEUE is one specific DATA STRUCTURE (or named channel) that the broker manages — a broker typically hosts many separate queues (or topics), and each queue is just the ordered buffer of pending messages waiting for a particular consumer or group of consumers to process. So a broker can contain, route between, and manage many different individual queues; a queue is a component that lives inside a broker, not the broker itself. An analogy: the broker is like the whole post office building and its sorting/routing operations, while a specific queue is like one particular mail slot or PO box within it, holding letters (messages) addressed to one recipient until they're picked up. The key difference: a message broker is the full middleware system that manages routing, delivery, and storage of messages, while a message queue is one specific ordered channel/buffer of pending messages that the broker manages — a broker typically hosts and coordinates many individual queues.",
  ),
  k(
    'kb-gap-code-event-vs-command',
    'Event vs command in event-driven architecture',
    ['event vs command event driven architecture', 'difference event command messaging', 'command vs event'],
    "Both are messages sent between parts of a system, but they differ in intent, tense, and who's allowed to say no. A COMMAND is an imperative INSTRUCTION telling a specific handler to DO something — named in the imperative ('PlaceOrder', 'ChargeCard', 'CancelSubscription') — and it is addressed to exactly one intended recipient/handler that owns that responsibility. Critically, a command CAN be rejected: the handler is allowed to refuse or fail it (insufficient stock, invalid card, business rule violation), and the sender generally expects to learn whether it succeeded or failed. An EVENT, in contrast, is a statement of FACT about something that has ALREADY happened — named in the past tense ('OrderPlaced', 'CardCharged', 'SubscriptionCancelled') — and it is broadcast to whoever happens to be listening, with the publisher not caring, or even knowing, who (if anyone) reacts to it. Crucially, an event CANNOT be rejected, because it isn't a request for permission — it's simply reporting that something already, definitely occurred; any number of independent subscribers (zero, one, or many) can react to that same event however they individually choose, entirely independently of one another. A single command is often what actually CAUSES an event to later be published once it succeeds (a `PlaceOrder` command, once successfully handled, results in an `OrderPlaced` event being broadcast). The key difference: a command is an imperative request to one specific handler that can be rejected, while an event is a past-tense statement of fact broadcast to any number of independent listeners that cannot be rejected because it already happened.",
  ),
  k(
    'kb-gap-code-choreography-vs-orchestration',
    'Choreography vs orchestration in microservices',
    ['choreography vs orchestration microservices', 'saga choreography vs orchestration', 'difference choreography orchestration'],
    "Both are ways of coordinating a multi-step business process across several independent microservices, but they differ in whether there's a central conductor. In an ORCHESTRATION approach, one dedicated central component (an 'orchestrator') explicitly directs the whole process: it calls each service in turn, telling it exactly what to do next, tracks the overall state of the workflow, and decides what happens on failure (e.g. which compensating steps to trigger) — this keeps the overall flow easy to see and reason about in one place, since the entire sequence of steps is explicit in the orchestrator's own logic, but it also creates a central point of coordination (and potential single point of failure/bottleneck) that every step has to go through, and the orchestrator ends up knowing a lot about every other service's responsibilities. In a CHOREOGRAPHY approach, there is NO central coordinator at all — each service instead independently listens for events published by other services and reacts on its own, publishing its own resulting events in turn, so the overall workflow emerges purely from this decentralized chain of independent event reactions (an 'OrderPlaced' event triggers the inventory service to react and publish 'StockReserved,' which triggers the shipping service to react and publish 'Shipped,' with no single component ever knowing the 'whole' process end-to-end). This avoids a central bottleneck and keeps services more loosely coupled and independently deployable, but the overall workflow becomes genuinely harder to trace, monitor, and reason about, since it's scattered implicitly across many separate services' independent event handlers rather than being explicit anywhere in one place. The key difference: orchestration uses one central component that explicitly directs every step of the workflow, while choreography has no central coordinator at all — the workflow emerges from services independently reacting to each other's published events.",
  ),
  k(
    'kb-gap-code-idempotent-consumer-vs-producer',
    'Idempotent consumers vs idempotent producers',
    ['idempotent consumer vs idempotent producer', 'idempotent producer messaging', 'idempotent consumer pattern'],
    "In a messaging-systems context (not ecology/food webs), both terms describe defenses against duplicate messages, applied at opposite ends of the pipeline. An IDEMPOTENT CONSUMER is designed so that processing the exact SAME message more than once produces no additional or different effect beyond processing it the first time — since at-least-once delivery can genuinely redeliver a message the consumer already handled, an idempotent consumer typically tracks which message IDs it has already processed (e.g. in a database 'already handled' table) and simply skips ones it recognizes as duplicates, so being sent the same message five times still only results in the underlying action (charging a card, incrementing a counter, sending an email) happening once. An IDEMPOTENT PRODUCER, in contrast, prevents duplicates from being introduced from the SENDING side in the first place — if a producer's write to the broker fails to get a confirmed acknowledgment (maybe the write actually succeeded but the ack was lost over the network) and the producer's retry logic then resends the same message, an idempotent producer (a feature Kafka producers can enable) attaches a unique sequence number/ID per message so the broker itself can detect and silently discard a message it already actually received, rather than storing it a second time. The key difference: an idempotent consumer defends against processing a duplicate it received (skipping ones it's already handled), while an idempotent producer prevents the broker from ever actually storing a duplicate in the first place, even if the producer's own retry logic resends the same message after an ambiguous/lost acknowledgment.",
  ),
  k(
    'kb-gap-code-durable-vs-transient-queue',
    'Durable queue vs transient queue',
    ['durable queue vs transient queue', 'difference durable transient queue', 'persistent vs non persistent queue'],
    "Both describe whether a queue (and the messages sitting in it) survive a broker restart or crash. A DURABLE queue is declared to persist its metadata and (when messages are also marked persistent) its contents to disk — if the message broker process restarts or the underlying server crashes and comes back up, a durable queue still exists afterward with its pending messages intact, ready to be delivered once consumers reconnect; this is essential for anything where losing a message would be a real problem (a payment to process, a welcome email that must be sent), at the cost of extra disk I/O overhead for every message written, which makes durable queues somewhat slower than non-durable ones. A TRANSIENT (non-durable) queue exists only in the broker's in-memory state — it's faster, since there's no disk write on every message, but if the broker restarts or crashes, the queue itself and anything still sitting in it are simply gone, with no way to recover them; this is acceptable for genuinely low-stakes, short-lived, or easily-regenerated data (ephemeral real-time status updates, a temporary reply-to queue for a single request/response exchange) where losing a message on a rare restart is a non-issue. The key difference: a durable queue and its messages are persisted to disk and survive a broker restart, at the cost of extra write overhead, while a transient queue lives only in memory, is faster, but is completely lost if the broker restarts or crashes.",
  ),
  k(
    'kb-gap-code-ack-vs-nack',
    'Message acknowledgment (ack) vs negative acknowledgment (nack)',
    ['message ack vs nack', 'what is a nack messaging', 'acknowledgment negative acknowledgment queue'],
    "In messaging systems (not Discord server-rules bots), an ACK (acknowledgment) and NACK (negative acknowledgment) are how a consumer tells the broker the outcome of handling a delivered message, which the broker uses to decide whether to consider that message safely delivered or to try again. When a consumer receives a message and successfully finishes processing it, it sends an ACK back to the broker — this tells the broker 'I'm done with this, you can safely remove/mark-consumed this message and never redeliver it,' which is what actually makes it safe (assuming ack-based delivery) since the broker won't discard a message until it gets that explicit confirmation. If processing instead FAILS — an exception is thrown, a downstream dependency the consumer needed is unavailable, the message data is invalid for this consumer to handle right now — the consumer sends a NACK (or simply never acks and lets a delivery timeout fire, which most brokers treat the same way), telling the broker 'I could not process this.' Depending on configuration, a nacked message is typically either requeued for another delivery attempt (often to a retry queue with backoff) or, if requeuing keeps failing, ultimately routed to a dead-letter queue rather than being retried forever. The key difference: an ack tells the broker a message was successfully processed and can be safely discarded, while a nack (or a missing ack) tells the broker processing failed, triggering a redelivery attempt or eventual routing to a dead-letter queue.",
  ),
  k(
    'kb-gap-code-topic-partition-vs-queue-shard',
    'Topic partition vs queue shard (messaging, not database sharding)',
    ['kafka topic partition vs queue shard', 'messaging topic partition', 'topic partition messaging systems'],
    "This is specifically about MESSAGING system scalability units, not database table/row partitioning or database sharding (though the underlying idea of splitting data to parallelize work is conceptually similar). A Kafka-style TOPIC PARTITION is an ordered, append-only sub-log that one topic is split into — a topic with 12 partitions can be consumed in parallel by up to 12 consumers within one consumer group (each handling a different subset of partitions), since Kafka only guarantees message ordering WITHIN a single partition, not across the whole topic; partitions are also how a topic scales its total write and read throughput beyond what any single log/broker could handle alone, and how the topic's data gets distributed and replicated across multiple broker machines in the cluster. A QUEUE SHARD, in messaging systems that shard queues (some cloud queue services, and horizontally-scaled RabbitMQ-style setups), similarly splits ONE logical queue's messages across multiple underlying physical queues/nodes, each handling its own slice of the traffic, again to spread load and throughput beyond one node's capacity — but a sharded queue generally does NOT preserve any ordering guarantee across shards, and messages are typically distributed round-robin or by hashing a shard/partition key, similar in spirit to how a topic partition key works. The key difference: both split one logical stream of messages into multiple physical units to parallelize throughput across machines, and both only guarantee ordering within a single unit (one partition, or one shard) rather than across the whole topic/queue — the specific terminology and guarantees differ by which messaging system you're using (Kafka calls its units partitions; other systems call theirs shards), but the underlying idea is the same.",
  ),
  k(
    'kb-gap-code-active-record-vs-data-mapper',
    'Active Record pattern vs Data Mapper pattern',
    ['active record vs data mapper', 'active record pattern orm', 'data mapper pattern orm', 'difference active record data mapper'],
    "Both are architectural patterns for how an ORM maps objects to database rows, but they place the database-awareness in different places. In the ACTIVE RECORD pattern, a single class represents both the business data AND the logic to save/load/delete itself — the object itself knows how to talk to the database: `user.save()`, `user.delete()`, and `User.find(5)` are called directly on the model object/class itself, which typically maps one-to-one to a single database table, with the object's own fields matching that table's columns. This is simple and fast to get started with (very little boilerplate — the model IS the data access layer), which is why it's the default in Ruby on Rails' ActiveRecord and Laravel's Eloquent, but it tightly couples your domain/business objects directly to the database schema and to persistence concerns, which can get awkward as business logic grows more complex or when a domain object's shape genuinely needs to diverge from its underlying table structure. In the DATA MAPPER pattern, the domain object itself knows NOTHING about the database at all — it's a plain object holding just data and business logic, with no `save()` or `find()` methods of its own; a completely SEPARATE 'mapper' class/layer is responsible for moving data back and forth between that plain domain object and the database (`userMapper.save(user)`, `userMapper.findById(5)`). This keeps the domain model fully decoupled from persistence — the domain object doesn't even need to have a matching table shape, and you can test business logic with zero database involved at all — at the cost of more boilerplate/indirection to set up. Hibernate (Java), Doctrine (PHP), and SQLAlchemy's classic mapping mode (Python) follow the Data Mapper approach. The key difference: Active Record bakes save/load/delete logic directly into the domain object itself (simple, but tightly coupled to the database), while Data Mapper keeps the domain object completely persistence-ignorant and uses a separate mapper layer to handle all database interaction (more decoupled and testable, but more boilerplate).",
  ),
  k(
    'kb-gap-code-event-driven-vs-request-response',
    'Event-driven architecture vs request-response architecture',
    ['event driven architecture vs request response', 'difference event driven request response', 'request response vs event driven'],
    "REQUEST-RESPONSE is the classic synchronous model: a client sends ONE request directly to a specific server/service and then waits for that same service to send back exactly ONE response before continuing (a typical REST API call, or a database query) — this is simple, predictable, and easy to trace (the caller knows exactly who it's talking to and gets an immediate result or error back), but it also means the caller is tightly coupled to knowing which specific service to call, and is often blocked/waiting on that call before it can proceed. Note: request-response does NOT inherently mean 'constantly asking again and again' — a single request-response exchange is just one request paired with one response; REPEATEDLY re-requesting to check for updates is a separate, distinct technique called polling, which is one possible (often inefficient) USE of request-response, not what request-response itself means. EVENT-DRIVEN architecture instead structures the system around components publishing 'events' (facts that something happened) without knowing or caring who, if anyone, is listening, while any number of other components independently subscribe to and react to those events on their own — there's no single caller waiting on a single specific response; the publisher fires the event and moves on immediately, and reactions happen asynchronously, potentially by multiple completely independent listeners. This makes it much easier to add new functionality later (a new service just starts listening to already-published events, with zero changes needed to the code that publishes them) and avoids the publisher being blocked waiting on any specific downstream reaction, but the overall end-to-end behavior of the system becomes harder to trace at a glance, since what happens in response to any given event is scattered across many independent listeners rather than visible from one caller's code. The key difference: request-response is a direct, synchronous exchange between a specific caller and a specific responder (one request, one response), while event-driven architecture decouples publishers from any specific listener entirely, letting any number of independent components react asynchronously to a published event.",
  ),
  k(
    'kb-gap-code-kafka-retention-vs-delete-on-consume',
    "Kafka log retention vs a traditional queue's delete-on-consume behavior",
    ['kafka log retention vs delete on consume', 'kafka message retention', 'traditional queue deletes message after consumed'],
    "A traditional message queue (like RabbitMQ in its default task-queue usage) treats a message as disposable once it's been successfully handled: a consumer receives the message, processes it, sends an acknowledgment back to the broker, and the broker then REMOVES that message from the queue for good — the message existed only to be delivered once and is gone immediately after that consumer confirms it's done, with no way for a second, later consumer to come along and read that same message again. Kafka works completely differently: it treats a topic as a durable, APPEND-ONLY LOG rather than a disposable inbox, and it retains every message for a configured retention period (a fixed amount of time, like 7 days, or a fixed total size, or in some setups indefinitely) regardless of whether any consumer has actually read it yet — consuming a message in Kafka does NOT delete it; instead, each consumer group simply tracks its own 'offset' (a bookmark of how far through the log it has read), so a brand-new consumer group can start reading a topic from the very beginning and replay all the retained history, and an existing consumer group can even deliberately reset its offset backward to reprocess old messages (useful for fixing a bug in a downstream consumer and reprocessing past events correctly, or backfilling an entirely new consumer that needs the full history). The key difference: a traditional queue deletes a message once it's been consumed and acknowledged, so it can only ever be delivered once, while Kafka retains messages for a configured period regardless of consumption, letting any number of independent consumer groups read (and even replay) the same retained messages at their own pace.",
  ),
];
