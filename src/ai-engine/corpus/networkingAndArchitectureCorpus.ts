import { KnowledgeItem } from '../../types';

// Code knowledge expansion (2026-09-14) — networking fundamentals and software architecture
// concepts relevant to backend/full-stack development. Plain neutral educational content.

const now = Date.now();
const k = (id: string, title: string, keywords: string[], content: string): KnowledgeItem => ({
  id,
  title,
  category: 'Programming',
  keywords,
  content,
  createdAt: now,
});

export const NETWORKING_AND_ARCHITECTURE_CORPUS: KnowledgeItem[] = [
  k(
    'kb-code-tcp-ip-basics',
    'TCP/IP basics',
    ['tcp ip basics', 'how the internet works networking'],
    "TCP/IP is the foundational suite of protocols underlying virtually all internet communication, organized in layers, each handling a different concern. IP (Internet Protocol) handles addressing and routing — every device gets an IP address, and IP is responsible for getting individual packets of data from a source address to a destination address, potentially hopping across many intermediate routers, but IP itself makes no guarantee that packets arrive in order, or arrive at all. TCP (Transmission Control Protocol) sits on top of IP and adds reliability: it breaks data into packets, numbers them, retransmits any that go missing, reassembles them in the correct order at the destination, and establishes a connection between two endpoints (the well-known \"three-way handshake\") before any actual data is exchanged — this reliability comes at the cost of some overhead and latency. UDP (User Datagram Protocol) is a simpler, connectionless alternative to TCP that skips the reliability guarantees entirely (no retransmission, no ordering guarantee) in exchange for lower latency and overhead, which is why it's preferred for use cases like live video/voice calls or games, where a dropped or late packet is better skipped than waited on and retransmitted.",
  ),
  k(
    'kb-code-dns-basics',
    'DNS basics',
    ['dns basics', 'domain name system'],
    "DNS (Domain Name System) translates human-readable domain names (like `example.com`) into the numeric IP addresses computers actually use to route network traffic — functioning conceptually like the internet's phone book. When a browser needs to reach `example.com`, it queries a DNS resolver, which (if it doesn't already have the answer cached from a recent prior lookup) works its way through a hierarchy: root DNS servers point to servers for the top-level domain (`.com`), which point to the specific authoritative name servers for `example.com`, which finally return the actual IP address to connect to. Common DNS record types include an A record (maps a domain directly to an IPv4 address), an AAAA record (maps to an IPv6 address), a CNAME record (an alias pointing one domain name to another domain name rather than directly to an IP), and an MX record (specifies which mail servers handle email for that domain). DNS results are cached at multiple levels (the browser, the operating system, intermediate resolvers) for a duration controlled by each record's TTL (time to live) value, which is why DNS changes (like pointing a domain to a new server) don't take effect everywhere instantly — cached copies elsewhere continue serving the old, stale answer until their TTL expires.",
  ),
  k(
    'kb-code-http-https',
    'HTTP vs HTTPS',
    ['http vs https', 'what does https do'],
    "HTTP (HyperText Transfer Protocol) is the application-level protocol web browsers and servers use to exchange requests and responses, but by itself, HTTP traffic travels in plain text — anyone who can intercept the network traffic between a client and server (on a shared public WiFi network, or at any point along the route) can read exactly what's being sent, including passwords or sensitive form data if the connection is unencrypted. HTTPS adds a layer of encryption on top of HTTP, using TLS (Transport Layer Security, the modern successor to the older SSL protocol) to encrypt the data in transit, so an eavesdropper intercepting the traffic sees only unreadable ciphertext rather than the actual content. HTTPS also verifies the server's identity via a certificate issued by a trusted certificate authority, protecting against a malicious actor impersonating the real server (a \"man-in-the-middle\" attack) by presenting a certificate the browser wouldn't trust. Browsers now widely mark plain HTTP sites as \"Not Secure,\" and virtually all modern production websites use HTTPS by default, both for security and because major browsers and search engines actively penalize or discourage plain unencrypted HTTP.",
  ),
  k(
    'kb-code-load-balancing',
    'Load balancing basics',
    ['load balancing', 'what is a load balancer'],
    "A load balancer sits in front of multiple servers running the same application and distributes incoming requests across them, rather than every request hitting a single server directly — this both improves performance (spreading work across multiple machines instead of overloading one) and improves reliability (if one server fails, the load balancer can detect this via health checks and stop routing traffic to it, while the remaining healthy servers keep serving requests without any visible downtime to users). Common distribution strategies include round-robin (requests are sent to each server in the pool in simple rotating order), least-connections (a new request goes to whichever server currently has the fewest active connections, useful when requests take varying amounts of time to process), and IP hash (a given client's requests are consistently routed to the same server based on hashing their IP address, useful when a server needs to remember some client-specific state locally between requests, called \"session affinity\" or \"sticky sessions\"). Load balancing is a core building block of horizontal scaling — the strategy of handling more total traffic by adding MORE servers running in parallel, rather than \"vertical scaling,\" which means making a single server more powerful (more CPU/RAM), which eventually hits a hard physical ceiling that horizontal scaling doesn't.",
  ),
  k(
    'kb-code-caching-strategies',
    'Caching strategies',
    ['caching strategies', 'cache basics programming'],
    "Caching stores a copy of expensive-to-compute or slow-to-fetch data somewhere faster to access, so a repeated request for the same data can be served quickly from the cache instead of redoing the original expensive work every single time. A cache can exist at many different layers simultaneously: browser caching stores static assets (images, CSS, JS files) locally on a user's own device, entirely avoiding a network request on their next visit; a CDN (Content Delivery Network) caches content at servers physically distributed close to users around the world, reducing latency versus always reaching back to one single, potentially distant, origin server; an application-level cache (like Redis or Memcached, often used to cache frequent, expensive database query results) sits between the application and the database. Cache invalidation — knowing precisely WHEN a cached copy has gone stale and needs to be refreshed or discarded — is famously one of the hardest problems in caching (jokingly called one of the two hardest problems in computer science, alongside naming things and off-by-one errors), since serving stale cached data can show users incorrect or outdated information if invalidation isn't handled correctly and promptly enough after the underlying data actually changes.",
  ),
  k(
    'kb-code-database-transactions-acid',
    'Database transactions and ACID',
    ['database transactions', 'acid properties database'],
    "A database transaction groups multiple operations together so they either ALL succeed together or ALL fail together, with no possibility of only some of them partially completing and leaving the database in an inconsistent, half-finished state — critical for operations like transferring money between two accounts, where debiting one account but failing to credit the other (due to a crash or error in between the two operations) would be a serious, unacceptable bug. ACID is the acronym for the four properties a properly transactional database guarantees. Atomicity: a transaction's operations are all-or-nothing — if any part fails, the entire transaction is rolled back as if none of it had ever happened. Consistency: a transaction takes the database from one valid state to another valid state, never violating defined rules like constraints or foreign keys along the way. Isolation: concurrently running transactions don't interfere with or see each other's uncommitted, in-progress changes, behaving (from each transaction's own perspective) roughly as if they ran one at a time in some sequential order, even though they may actually be executing concurrently under the hood for performance. Durability: once a transaction successfully commits, its changes are permanently saved and will survive even a subsequent crash or power failure, typically ensured by writing changes to durable, persistent storage rather than only keeping them in volatile memory before confirming success.",
  ),
  k(
    'kb-code-sql-vs-nosql',
    'SQL vs NoSQL databases',
    ['sql vs nosql', 'relational vs non relational database'],
    "SQL (relational) databases like PostgreSQL and MySQL organize data into structured tables with a predefined schema — every row in a table must conform to the same fixed set of columns and types, defined up front — and they excel at expressing complex relationships between different types of data via JOINs, plus strong ACID transactional guarantees, making them a strong default choice for data with clear, stable structure and relationships (financial data, most typical business application data). NoSQL is a broad umbrella term for several different non-relational alternatives, each suited to different needs: document databases (like MongoDB) store flexible, semi-structured JSON-like documents that don't need a rigid predefined schema, well suited to data whose shape varies or evolves over time; key-value stores (like Redis) offer extremely fast, simple lookups by a single unique key, ideal for caching and session storage; wide-column stores (like Cassandra) and graph databases (like Neo4j, optimized specifically for traversing richly interconnected relationships) serve still other specialized needs. The general tradeoff: SQL databases prioritize strong consistency and structured, well-defined relationships; many NoSQL databases instead prioritize horizontal scalability and flexible schemas, often relaxing some of the strict consistency guarantees a traditional relational database provides in exchange for that scalability and flexibility.",
  ),
  k(
    'kb-code-orm-basics',
    'ORM basics',
    ['orm object relational mapping', 'what is an orm'],
    "An ORM (Object-Relational Mapper) is a library that lets application code interact with a relational (SQL) database using the programming language's own native objects and method calls, instead of writing raw SQL strings directly. Instead of `db.query(\"SELECT * FROM users WHERE id = 5\")`, an ORM lets you write something like `User.find(5)` in the application's own language, and the ORM translates that call into the appropriate underlying SQL, then maps the resulting database rows back into instances of the application's own `User` class/objects. Popular examples include SQLAlchemy and Django's ORM for Python, ActiveRecord for Ruby on Rails, Entity Framework for C#/.NET, Hibernate for Java, and Prisma or TypeORM for TypeScript/JavaScript. Benefits include less repetitive boilerplate SQL, some protection against SQL injection by default (since query parameters are handled safely under the hood rather than manually string-concatenated), and being able to work at a higher, more language-native level of abstraction that fits naturally with the rest of an object-oriented or otherwise idiomatic codebase. The main tradeoff: an ORM can generate inefficient SQL for particularly complex queries compared to skillfully hand-written SQL, and can obscure exactly what's actually happening at the database level, which is why many real-world systems still drop down to raw SQL for their most performance-critical or complex specific queries even while using an ORM for the majority of everyday, simpler database interactions.",
  ),
  k(
    'kb-code-monolith-vs-microservices',
    'Monolith vs microservices architecture',
    ['monolith vs microservices', 'microservices architecture basics'],
    "A monolithic architecture builds an entire application as one single, unified codebase and deployable unit — all of its features (user accounts, orders, payments, notifications) live together and are deployed together as one piece. This is simpler to develop, test, and deploy initially, since there's only one codebase and one deployment pipeline to manage, and it avoids the overhead of network calls between different parts of the same overall application, since everything runs together in the same process. A microservices architecture instead splits an application into many small, independently deployable services, each typically owning one specific business capability (an \"orders service,\" a \"payments service,\" a \"notifications service\") and communicating with each other over the network (commonly via REST APIs or a message queue). This allows different teams to work, deploy, and even scale each service independently — the orders service can be scaled up separately from the notifications service if it's getting disproportionately more traffic — but it introduces significant new complexity: network calls between services can fail in ways an in-process function call within a monolith simply can't, and debugging a problem that spans multiple independent services is generally harder than debugging within one unified codebase. Many teams deliberately start with a monolith and only split out microservices later, once a genuine, concrete need for independent scaling or independent team ownership actually emerges in practice.",
  ),
  k(
    'kb-code-message-queues',
    'Message queues basics',
    ['message queue basics', 'what is a message queue', 'async job processing'],
    "A message queue lets different parts of a system communicate asynchronously by having one part (a \"producer\") place a message onto a queue, and another part (a \"consumer\") process that message later, independently and at its own pace — decoupling the two so the producer doesn't need to wait around for the consumer to actually finish processing before it can move on and continue doing other work. A common use case: when a user signs up, sending them a welcome email is a good candidate to hand off to a queue rather than doing synchronously as part of the signup request itself — the actual signup request can complete quickly and respond to the user right away, while a separate background worker process picks the \"send welcome email\" message up off the queue and handles it independently, without making the user wait for that email to actually be sent before their signup finishes. This also improves resilience: if the email-sending service happens to be temporarily down when the message arrives, the message can simply stay safely in the queue and be retried automatically later, rather than causing the whole original signup request to fail outright. Popular message queue systems include RabbitMQ, Apache Kafka (which additionally supports very high-throughput event streaming beyond simple queuing), and cloud-managed offerings like AWS SQS.",
  ),
  k(
    'kb-code-logging-levels',
    'Logging levels',
    ['logging levels', 'debug info warn error log levels'],
    "Most logging libraries support several severity levels, letting developers categorize log messages by importance and then filter which ones are actually recorded or displayed, typically in this increasing order of severity: DEBUG (very detailed diagnostic information, useful mainly while actively developing/troubleshooting, generally too noisy and voluminous to keep on by default in a production environment), INFO (general, routine information about normal program operation — a request was handled, a scheduled job completed), WARN/WARNING (something unexpected or worth attention happened, but it isn't necessarily a full failure — a deprecated function was called, a retry was needed), ERROR (an actual failure occurred that needs attention, like a failed database connection or an unhandled exception), and sometimes FATAL/CRITICAL (a severe error the application likely can't meaningfully recover from and may need to shut down entirely after logging). Production systems commonly set a MINIMUM level to actually record (e.g. only INFO and above), which suppresses noisy DEBUG-level detail during normal day-to-day operation while still capturing everything genuinely important — and that minimum level can often be temporarily lowered on demand when actively troubleshooting a specific live issue, without needing to change and redeploy the application's actual code just to get more detailed logs.",
  ),
  k(
    'kb-code-feature-flags',
    'Feature flags',
    ['feature flags', 'feature toggles'],
    "A feature flag (also called a feature toggle) is a configuration switch that lets a team turn a specific piece of functionality on or off without needing to deploy new code — the code for both the old and new behavior already exists in the deployed application simultaneously, and the flag's current value simply decides which path actually runs at request time: `if (featureFlags.isEnabled(\"newCheckoutFlow\")) { /* new behavior */ } else { /* old behavior */ }`. This decouples DEPLOYING code (getting it running on production servers) from actually RELEASING a feature to users, which enables several useful practices: gradually rolling a new feature out to just a small percentage of users first to monitor for problems before a full rollout, instantly \"killing\" a newly launched feature that turns out to have a serious bug without needing an emergency rollback deployment (which takes real time and carries its own risk), and A/B testing two different versions of a feature against different user segments to compare outcomes. The tradeoff is added code complexity, since supporting both an old and new code path simultaneously (even temporarily) makes the codebase's logic more branching and harder to reason about — well-run teams generally aim to remove a feature flag (and its now-unused old code path) once a feature has been fully, confidently rolled out and is no longer expected to be toggled off again.",
  ),
  k(
    'kb-code-technical-debt',
    'Technical debt',
    ['technical debt', 'what is technical debt'],
    "Technical debt is a metaphor for the implied future cost of choosing an easier, faster, but ultimately more limited or messier solution now, instead of investing the extra time needed for a better, more sustainable one — much like taking on a real financial debt, it can be a perfectly reasonable, deliberate tradeoff in the moment (shipping a working feature sooner has genuine real value), but it accrues ongoing \"interest\" in the form of ongoing extra effort every single time that area of the code needs to be touched again later, until the underlying debt is eventually paid down through refactoring. Technical debt isn't inherently a mistake or a sign of bad engineering — deliberately taking some on to hit an important deadline, learn from real early user feedback sooner, or validate an uncertain idea before over-investing in it can be a genuinely smart, deliberate tradeoff. The risk comes specifically from debt that's taken on carelessly or never acknowledged and never revisited, which tends to compound over time as more and more new code gets built on top of the shaky, poorly-suited foundation — eventually making even small, simple-seeming changes disproportionately slow, risky, and expensive to make safely. Well-run teams typically track technical debt explicitly (rather than letting it stay silent and invisible) and deliberately budget some ongoing time specifically for paying it down periodically, rather than only ever adding new features and never revisiting the accumulating debt underneath them.",
  ),
  k(
    'kb-code-clean-code-naming',
    'Naming things well in code',
    ['clean code naming', 'variable naming best practices'],
    "Good naming is one of the highest-leverage, lowest-cost things a developer can do for code readability, since names are read far more often than they're written, by other people (and by your own future self) who won't have the exact same context in their head that you did the moment you originally wrote the code. A variable, function, or class name should clearly communicate its PURPOSE, not just its type or a generic placeholder — `daysSinceLastLogin` communicates far more useful information at a glance than a bare `d` or an overly generic `temp` or `data`, even though all technically \"work\" from the compiler's indifferent perspective. Function names, by widely followed convention, are usually verbs or verb phrases describing an action (`calculateTotal()`, `sendEmail()`, `isValid()` for something returning a boolean), while variable and class names are usually nouns or noun phrases describing a thing or a state. Consistency also matters considerably — if one part of a codebase calls something a `user` and another part of the exact same codebase calls the conceptually identical thing a `customer` or an `account`, readers are left wondering whether these are actually meaningfully different concepts or just accidentally inconsistent naming for the exact same underlying idea, which is a subtle but real recurring source of confusion and even outright bugs when the wrong assumption gets made.",
  ),
  k(
    'kb-code-comments-vs-self-documenting',
    'When to use comments vs self-documenting code',
    ['code comments best practices', 'self documenting code'],
    "A comment explaining WHAT a piece of code does is often a sign the code itself could instead be made clearer through better naming or restructuring — `// increment counter by one` above `count += 1` adds no real information a reader couldn't already get instantly from the code itself, and worse, such comments tend to silently go stale and become actively misleading once the underlying code changes later but nobody remembers (or bothers) to also update the now-outdated comment sitting right next to it. Comments earn their real keep explaining WHY something is done a certain way, especially when that reason genuinely isn't obvious from the code alone — a non-obvious workaround for a specific external bug, a deliberate but unusual performance tradeoff, a business rule or legal/regulatory requirement that isn't self-evident just from reading the logic itself. Rather than a comment restating what a chunk of code visibly does, extracting that chunk into a well-named function often communicates the same intent more durably and more usefully: `calculateShippingCost(order)` is inherently self-explanatory at every call site going forward, unlike an inline comment sitting above a raw block of arithmetic that a reader has to specifically notice, read, and trust is still accurate. The general guiding principle many experienced developers converge on: prefer writing code that's clear enough to need few comments, and reserve the comments you DO write specifically for context that genuinely can't be expressed through the code's own structure and naming alone.",
  ),
  k(
    'kb-code-linters-formatters',
    'Linters and code formatters',
    ['linters code formatters', 'eslint prettier basics'],
    "A linter is a tool that analyzes source code to flag potential bugs, stylistic inconsistencies, and patterns known to commonly cause problems, without actually running the code — for example, flagging an unused variable, a variable used before it's ever assigned, or (depending on configured rules) stylistic issues like inconsistent naming conventions. Common examples include ESLint for JavaScript/TypeScript, Pylint or Ruff for Python, and RuboCop for Ruby. A code formatter, by contrast, focuses specifically on consistent visual STYLE rather than catching logical problems — automatically fixing indentation, spacing, line length, quote style, and similar purely cosmetic concerns, so an entire team's code looks visually consistent regardless of each individual developer's personal formatting preferences and habits. Prettier is a widely used formatter across the JavaScript/TypeScript ecosystem; Black serves a similar role for Python. Many teams run both a linter and a formatter automatically as part of a pre-commit hook (running automatically right before each Git commit is created) or as a required, automated check in their CI pipeline, which catches basic issues and normalizes style automatically and consistently, freeing up code review discussions to focus on genuinely substantive logic and design questions instead of spending time debating purely stylistic nitpicks like tabs versus spaces.",
  ),
  k(
    'kb-code-code-review-basics',
    'Code review basics',
    ['code review best practices', 'what is code review'],
    "Code review is the practice of having another developer examine a proposed code change (typically via a pull/merge request) before it's merged into the shared main codebase, serving several purposes at once: catching actual bugs a second pair of eyes may spot that the original author missed (having stared at the same code for a while, it's easy to become effectively blind to certain kinds of mistakes in your own work), spreading knowledge of the codebase across more than just one single person, maintaining reasonable consistency with the rest of the existing codebase's established conventions and patterns, and giving less experienced developers useful, concrete feedback and mentorship tied directly to real code they actually wrote. Constructive code review feedback is generally specific, actionable, and focused on the code itself rather than the person who wrote it — explaining not just WHAT should change but WHY it matters helps the author (and future readers of the same review thread) actually learn from the specific feedback, rather than just mechanically complying with the individual requested change without understanding the underlying reasoning behind it. Most teams distinguish between blocking feedback (a genuine bug, a real security issue, a significant design problem that must be addressed before the change can be safely merged) and optional suggestions or personal style preferences that the original author is free to take or leave as they see fit, since treating every single piece of feedback as equally mandatory tends to slow reviews down considerably without a correspondingly proportional gain in actual code quality.",
  ),
  k(
    'kb-code-static-vs-dynamic-typing',
    'Static vs dynamic typing',
    ['static typing vs dynamic typing', 'statically typed vs dynamically typed'],
    "In a statically-typed language (Java, C, Rust, TypeScript), every variable's type is checked at COMPILE time, before the program ever actually runs — assigning a string to a variable declared as an integer is caught immediately as a compile error, never even reaching runtime. This catches a meaningful class of bugs early and lets tooling (IDEs, autocomplete) understand a codebase's shapes precisely, but requires more upfront ceremony (explicit type annotations, though modern statically-typed languages increasingly infer many types automatically rather than requiring every single one spelled out by hand). In a dynamically-typed language (Python, JavaScript, Ruby), a variable's type is determined and checked only at RUNTIME, as the program actually executes — the same variable can freely be reassigned from a string to a number to a list over its lifetime, and a type-related error (like calling a string-only method on a value that turns out to actually be a number at that point) only surfaces when that specific line of code actually runs, not necessarily every single time the program is run overall (if that particular code path happens not to execute on a given run, the type error stays silently latent and undiscovered). This generally makes dynamically-typed code faster and more flexible to prototype quickly, at some real cost to catching type-related bugs as early and as reliably as a statically-typed language's compiler does automatically before the code ever ships.",
  ),
  k(
    'kb-code-compiled-vs-interpreted',
    'Compiled vs interpreted languages',
    ['compiled vs interpreted languages', 'compiler vs interpreter'],
    "A compiled language (C, C++, Rust, Go) is translated by a compiler, ahead of time, directly into native machine code specific to a target CPU architecture, which can then be run directly by the operating system without needing the original compiler or source code present anymore at run time — this ahead-of-time translation step generally produces faster-running programs, since all the translation work happens once, up front, rather than being repeated every single time the program runs, but any change to the source code requires a fresh recompilation step before it can actually run again. An interpreted language (classic Python, Ruby, PHP) is instead read and executed line by line by an interpreter program at run time, without first producing a separate standalone machine-code executable — this makes development iteration faster (there's no separate compile step needed before you can immediately try running a change) but is traditionally somewhat slower at actual execution time, since the translation work happens repeatedly, every single time the code runs, rather than being done just once ahead of time. Many modern languages genuinely blur this classic distinction: Java and C# compile to an intermediate bytecode format that then runs on a virtual machine (partway between fully compiled and fully interpreted), and JavaScript engines like V8 use Just-In-Time (JIT) compilation, compiling frequently-executed \"hot\" code paths to native machine code on the fly, DURING execution, to combine much of an interpreter's flexibility with performance approaching that of genuinely ahead-of-time compiled code.",
  ),
  k(
    'kb-code-garbage-collection',
    'Garbage collection',
    ['garbage collection programming', 'what is garbage collection'],
    "Garbage collection is an automatic memory management technique used by many languages (Java, Python, JavaScript, Go, C#) where the language runtime itself periodically identifies memory that's no longer reachable or usable by the running program — meaning nothing in the program still holds any reference that could ever access it again — and automatically frees that memory for reuse, without the developer ever needing to manually call something like C's `free()`. This eliminates entire categories of manual-memory bugs common in languages like C — use-after-free (accessing memory that's already been freed) and memory leaks caused by simply forgetting to free something the program is genuinely done using. The tradeoff is some amount of runtime performance overhead: the garbage collector itself has to periodically run and do real work (scanning for unreachable objects), which can introduce brief, sometimes-unpredictable pauses in program execution — a real concern for latency-sensitive applications like real-time audio processing or certain classes of games, which is a meaningful part of why systems languages like C, C++, and Rust (Rust specifically achieves memory safety through its compile-time ownership system instead, entirely without needing any garbage collector or its associated runtime overhead at all) remain preferred for that particular category of extremely performance- and latency-sensitive work.",
  ),
  k(
    'kb-code-xss-sql-injection',
    'XSS and SQL injection basics',
    ['cross site scripting xss', 'sql injection basics', 'web security vulnerabilities'],
    "SQL injection happens when untrusted user input is inserted directly into a SQL query string without proper sanitization, letting a malicious user craft input that changes the query's actual intended meaning — for example, entering `' OR '1'='1` into a naively-built login form's password field could, if the query is built via unsafe raw string concatenation, turn a query intended to check for a specific matching user into one that matches every single row in the table instead, potentially bypassing authentication entirely. The standard, reliable defense is using PARAMETERIZED queries (also called prepared statements), where user input is passed as a separate, clearly-delineated parameter to the database driver rather than being directly spliced into the raw query string itself — the database then correctly and safely treats that value purely as DATA, never as part of the executable query's own structure, no matter what characters the input actually contains. Cross-Site Scripting (XSS) is a related but distinct web vulnerability where an attacker manages to inject malicious JavaScript into a page that other, unsuspecting users then view — for instance, a comment field that echoes a user's raw, unescaped input directly back into the page's HTML without escaping it lets an attacker submit a `<script>` tag that then executes in every other visitor's browser who later views that comment. The standard defense is properly escaping (or otherwise safely encoding) any user-supplied content before rendering it as HTML, so special HTML/script-triggering characters are displayed literally as harmless plain text rather than being interpreted and executed by the browser as live, active code.",
  ),
  k(
    'kb-code-hashing-vs-encryption',
    'Hashing vs encryption',
    ['hashing vs encryption', 'password hashing basics'],
    "Hashing and encryption both transform data, but for fundamentally different purposes and with a crucial structural difference. Hashing is a ONE-WAY transformation — a hash function takes an input and produces a fixed-size output (the \"hash\"), and there's deliberately no way to reverse that process to recover the original input from the hash alone, by design. This makes hashing the correct, standard choice for storing passwords securely: a system stores only the HASH of a user's password, never the actual plaintext password itself, and checks a login attempt by hashing the freshly submitted password and comparing that fresh hash against the stored one — if the server database is ever breached, an attacker gets only the unreversible hashes, not the actual usable passwords themselves (assuming a suitably strong, slow, purpose-built hashing algorithm like bcrypt or Argon2 was used, specifically NOT a fast general-purpose hash like plain MD5 or SHA-256 alone, which are comparatively far too easy to brute-force at scale for this specific security-sensitive purpose). Encryption, by contrast, is explicitly a TWO-WAY transformation — encrypted data (ciphertext) is specifically MEANT to be decrypted back into its original readable form later, using an appropriate corresponding key, which is exactly why encryption (not hashing) is the right, appropriate choice for data that genuinely needs to be recovered and read again later, like a message that needs to eventually be displayed back to its legitimate intended recipient.",
  ),
  k(
    'kb-code-recursion-vs-iteration',
    'Recursion vs iteration',
    ['recursion vs iteration', 'when to use recursion vs a loop'],
    "Recursion and iteration (loops) can both express repeated computation, and many problems can genuinely be solved with either approach, but they suit different situations. A loop (`for`/`while`) repeats a block of code using explicit loop-control variables, generally uses constant extra memory regardless of how many iterations it runs (aside from whatever memory the actual work itself needs), and is usually the more naturally efficient, straightforward choice for simple, flat, linear repetition, like summing every number in a list. Recursion instead has a function call itself with progressively smaller inputs until it reaches a base case, and tends to be the clearer, more natural fit specifically for problems with an inherently recursive, self-similar, or tree-like structure — traversing a nested folder structure, walking a tree data structure, or certain divide-and-conquer algorithms like merge sort, where the recursive structure closely mirrors the actual shape of the underlying problem itself. The real cost of recursion is that each recursive call adds a new frame onto the call stack, consuming real memory proportional to the recursion's depth, and very deep recursion (potentially thousands of levels) risks a stack overflow error in many languages — a plain iterative loop doesn't have this particular limitation, since it doesn't grow the call stack with every additional iteration the way a recursive call genuinely does. Some languages optimize a specific pattern called \"tail recursion\" (where the recursive call is the very last operation performed in the function) to reuse the same stack frame rather than growing it further, though not every language or runtime actually implements this optimization.",
  ),
  k(
    'kb-code-sorting-insertion-selection-heap',
    'Insertion sort, selection sort, and heap sort',
    ['insertion sort', 'selection sort', 'heap sort'],
    "Insertion sort builds up a sorted portion of the array one element at a time, taking each next unsorted element and inserting it directly into its correct position within the already-sorted portion built up so far, shifting other elements over as needed to make room — it's O(n²) in the average and worst case, but performs very well specifically on data that's already mostly sorted or nearly sorted (where each new element usually only needs to move a very short distance, close to its final correct position), and its simplicity and genuinely low overhead make it a common practical choice for very small arrays, sometimes even used as an internal optimization within otherwise more sophisticated hybrid sorting algorithms once a sub-array being processed becomes small enough. Selection sort repeatedly scans the entire remaining unsorted portion of the array to find its overall minimum (or maximum) value, then swaps that found value into its correct final sorted position, one position at a time — also O(n²) in essentially all cases, and generally considered less efficient in practice than insertion sort for typical real-world data, though it does have the specific advantage of making the minimum possible number of actual element swaps (at most n-1 total), which can matter specifically when each individual swap operation happens to be unusually expensive to perform for some reason. Heap sort first builds a max-heap out of the entire array, then repeatedly extracts the current maximum value (always sitting at the heap's root) and places it correctly at the end of the array, shrinking the heap by one each time — this reliably achieves a guaranteed O(n log n) time complexity in every case, and does its sorting genuinely in-place with no extra array needed (unlike merge sort), though it's generally somewhat slower in practice than quicksort due to less favorable, less predictable memory access patterns for modern CPU caches.",
  ),
];
