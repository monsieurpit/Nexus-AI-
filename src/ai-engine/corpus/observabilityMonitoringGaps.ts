import { KnowledgeItem } from '../../types';

// Batch 316 corpus fixes — coding-priority batch (2026-09-14). 22/25 misses.
// Topic: observability & monitoring (logs vs metrics vs traces, SLI/SLO/SLA, spans/trace IDs,
// white-box vs black-box monitoring, synthetic vs real-user monitoring, counter/gauge/histogram
// metric types, push vs pull collection, MTTR/MTBF, error budgets, alerting vs monitoring,
// postmortems, log rotation/retention, uptime vs availability, on-call runbooks/playbooks) —
// chosen because grep confirmed observability was completely uncovered anywhere in the existing
// 300+ corpus files: only a one-line "logging levels" entry and a passing APM disambiguation
// existed, with zero coverage of SLI/SLO/SLA, distributed tracing spans, or metric types.
// Several severe wrong-domain hallucinations: "SLI vs SLO vs SLA" answered entirely about SLA
// 3D-printing resin printers; "counter metric vs gauge metric" answered about over-the-counter
// medication and knitting gauge/tension; "metric vs log" answered about the metric MEASUREMENT
// SYSTEM (centimetres/kilograms) vs logarithms; "span vs trace" answered about a structural
// engineering beam span; "postmortem vs root cause analysis" answered entirely about forensic
// livor mortis (corpse lividity); "error budget vs SLO" answered about baseball scoring errors
// and personal finance budgeting; "uptime vs availability" answered about the cognitive-bias
// availability heuristic; "histogram metric vs summary metric" answered about photography
// histograms; "log sampling vs trace sampling" answered about statistical sampling and music
// sampling; "RED method vs USE method" refused/failed entirely, hallucinating red dwarf stars,
// red cards, and the NFL red zone; "health check liveness probe vs readiness probe" triggered a
// full off-topic web search returning COVID-19 pandemic and Gaza war Wikipedia summaries; "logs
// vs metrics vs traces" (the "three pillars of observability") also triggered an off-topic web
// search returning British/American spelling differences and an unrelated Quillen metric math
// article. "Log rotation vs log retention" and "white-box vs black-box monitoring" both answered
// with an adjacent but WRONG specific concept (Kafka log retention; software testing's white/
// black-box terminology) rather than the actual monitoring-domain meaning. Several one-sided
// cutoffs that explained only the first term and never addressed the second: structured vs
// unstructured logging, synthetic monitoring vs RUM, alerting vs monitoring, dashboard vs alert,
// trace ID vs span ID, runbook vs playbook, log aggregation vs log parsing, MTTR vs MTBF (never
// explained MTTR at all). "Incident vs outage" reframed the whole answer around a security
// incident/breach rather than the SRE operational sense. Passed: distributed tracing vs stack
// trace, horizontal/vertical scaling of an observability pipeline, APM tool vs log aggregator,
// application logs vs audit logs, anomaly detection vs threshold alerting, on-call rotation vs
// escalation policy — all answered correctly and were left alone.

const now = Date.now();
const k = (id: string, title: string, keywords: string[], content: string): KnowledgeItem => ({
  id,
  title,
  category: 'Programming',
  keywords,
  content,
  createdAt: now,
});

export const OBSERVABILITY_MONITORING_GAPS: KnowledgeItem[] = [
  k(
    'kb-gap-code-obs-monitoring-vs-observability',
    'Monitoring vs observability',
    ['monitoring vs observability', 'difference monitoring observability', 'what is observability'],
    "MONITORING means watching a predefined, finite set of metrics and dashboards you already decided were important ahead of time, and alerting when one of those known signals crosses a threshold — it answers questions you thought to ask in advance (\"is CPU usage above 90%?\", \"is the checkout endpoint returning 500s?\"). OBSERVABILITY is a broader property of a system: how well you can understand its INTERNAL state and diagnose NEW, previously unanticipated problems purely from its external outputs (logs, metrics, and traces), without having to ship new code or add new instrumentation first. The key distinction is about UNKNOWN UNKNOWNS: a well-monitored system can tell you when something you already anticipated goes wrong, while a well-OBSERVABLE system lets you ask arbitrary new questions on the fly (\"why are only Canadian users on this specific browser seeing slow checkouts since 2pm?\") and actually get an answer from the data you're already collecting, even though nobody set up a dashboard for that exact scenario in advance. The key difference: monitoring watches known signals for known failure modes, while observability is the system's underlying capacity to be interrogated and understood for failure modes nobody predicted ahead of time — monitoring is one thing you DO, observability is a property the system HAS.",
  ),
  k(
    'kb-gap-code-obs-three-pillars',
    'Logs vs metrics vs traces (the three pillars of observability)',
    ['logs vs metrics vs traces', 'three pillars of observability', 'difference log metric trace'],
    "These are the three core, complementary data types ('pillars') that together make a system observable, and each answers a different kind of question. A LOG is a timestamped, discrete text record of one specific event that happened ('user 4821 logged in at 14:32:01', 'payment failed: card declined') — logs are rich in detail and great for understanding exactly what happened at one specific point, but expensive to store at scale and slow to search across millions of entries. A METRIC is a numeric measurement aggregated over time (CPU usage, request count, average latency) — metrics are cheap to store, fast to query, and ideal for dashboards, trends, and alerting thresholds, but they're aggregated summaries: a metric can tell you 'error rate spiked to 5%' but not WHICH specific requests failed or why. A TRACE follows a single request as it travels through multiple services in a distributed system, recording how long each step (each 'span') took and how the steps relate to each other — traces are ideal for pinpointing exactly WHERE in a multi-service call chain a slow or failed request got stuck, something neither logs nor metrics alone can show clearly. The key difference: logs give rich detail about individual discrete events, metrics give cheap aggregated numeric trends over time, and traces show the step-by-step journey of one specific request across multiple services — a mature observability setup uses all three together, since each covers a blind spot the others have.",
  ),
  k(
    'kb-gap-code-obs-structured-vs-unstructured-logging',
    'Structured logging vs unstructured logging',
    ['structured logging vs unstructured logging', 'structured vs unstructured logs', 'difference structured unstructured logging'],
    "UNSTRUCTURED logging writes free-form, human-readable text lines with no fixed format — e.g. `User john.doe logged in from 192.168.1.5 at 14:32` — which is easy for a person to read directly in a terminal, but hard for software to reliably parse, since every developer might phrase similar events slightly differently, and pulling out a specific field (like the IP address) requires fragile regex or string-splitting that breaks if the wording ever changes. STRUCTURED logging instead writes each log entry as a well-defined, machine-parseable format (almost always JSON), with explicit named key-value fields: `{\"timestamp\": \"2026-09-14T14:32:00Z\", \"level\": \"info\", \"event\": \"user_login\", \"user_id\": 1234, \"ip\": \"192.168.1.5\"}` — every field is unambiguous and directly queryable by log-aggregation tools (find every login from this specific IP, or every event with `level: error`) without any fragile text-parsing at all. The tradeoff: structured logs are less pleasant to read raw in a terminal (more visual noise per line) but dramatically easier for machines to filter, aggregate, and search at scale, which is why virtually every serious production logging setup at any real scale uses structured (JSON) logging rather than unstructured free text. The key difference: unstructured logging is human-readable free text that's hard for software to reliably parse, while structured logging uses a fixed machine-parseable format (typically JSON) with named fields that log-aggregation tools can query directly.",
  ),
  k(
    'kb-gap-code-obs-sli-slo-sla',
    'SLI vs SLO vs SLA',
    ['SLI vs SLO vs SLA', 'difference SLI SLO SLA', 'service level indicator objective agreement'],
    "These three terms are NOT about 3D printing (that's a coincidental acronym collision with the SLA 3D-printing resin process) — in reliability/SRE terms they form a chain from raw measurement to formal promise. An SLI (Service Level INDICATOR) is the actual, raw, measured metric itself — a specific number you track, like 'the percentage of HTTP requests that returned successfully in the last 5 minutes' or 'the 95th-percentile response latency.' An SLO (Service Level OBJECTIVE) is an internal TARGET or goal set for that SLI — a specific threshold the team aims to hit, like '99.9% of requests should succeed over a rolling 30-day window.' SLOs are typically internal engineering goals, used to guide decisions like whether it's currently safe to ship a risky new release (if you're comfortably within your SLO, you have 'error budget' to spend; if you're close to breaching it, you slow down and focus on reliability instead). An SLA (Service Level AGREEMENT) is a formal, external, often legally binding CONTRACT with a customer that specifies a service level (often built directly on top of an SLO) and, critically, the CONSEQUENCES (usually financial credits or penalties) if that level isn't met. The key difference: an SLI is the raw measurement, an SLO is the internal target set for that measurement, and an SLA is the external contractual promise (with real consequences for breaking it) typically built on top of an SLO — SLOs are usually set stricter than the corresponding SLA specifically to leave a safety margin before contractual penalties would actually kick in.",
  ),
  k(
    'kb-gap-code-obs-metric-vs-log-term',
    'A "metric" (observability) vs the metric measurement system',
    ['metric vs log observability programming', 'what is a metric in monitoring', 'metric term software engineering not measurement system'],
    "This is a software-observability concept, NOT the metric measurement SYSTEM (centimetres, kilograms, litres) — the word 'metric' has two completely unrelated meanings, and in a monitoring/observability context it always means the numeric-measurement sense. In this context, a METRIC is a numeric value tracked over time, usually collected at regular intervals and aggregated (e.g. requests per second, average CPU usage, error rate percentage) — metrics are cheap to store, fast to query, and are what dashboards, graphs, and alerting thresholds are built from. A LOG, by contrast, is a discrete, timestamped text record of one specific individual event ('request X failed with error Y at time Z') — logs are far richer in per-event detail but much more expensive to store and search at scale than metrics. The key difference: a metric is an aggregated NUMBER tracked over time (cheap, great for trends and dashboards), while a log is a detailed TEXT RECORD of one specific event (expensive at scale, great for deep investigation of exactly what happened) — and neither has anything to do with the metric vs imperial measurement system used for physical units.",
  ),
  k(
    'kb-gap-code-obs-span-vs-trace',
    'Span vs trace (distributed tracing) and trace ID vs span ID',
    ['span vs trace distributed tracing', 'trace ID vs span ID', 'difference span trace opentelemetry', 'what is a span in tracing'],
    "This is a distributed-tracing concept, NOT structural engineering (a beam's 'span,' the distance between its supports) — in tracing, a TRACE represents the entire end-to-end journey of one single request as it travels through a distributed system, potentially touching many different services (an API gateway, an auth service, a database, a payment processor). A SPAN represents ONE individual unit of work within that journey — typically one operation in one specific service, like 'the auth service validated this token' or 'the database ran this specific query' — with its own start time, end time, and duration. A trace is made up of MANY spans, nested and connected in a parent-child tree reflecting which operation called which (the initial span that kicks off the whole request is the 'root span'). Each trace is identified by a single TRACE ID, generated once at the very start of the request and passed along (propagated) to every single service the request touches, so all of that request's spans — even ones happening in completely different services and processes — can later be grouped back together and displayed as one connected timeline. Each individual SPAN additionally has its own unique SPAN ID, plus a reference to its PARENT span ID, which is what lets a tracing tool reconstruct the exact call hierarchy (which operation triggered which) rather than just a flat unordered list of events. The key difference: a trace ID identifies the entire multi-service request as a whole, while a span ID identifies just one individual operation within that request — a single trace typically contains many spans, each with its own span ID but sharing the same overall trace ID.",
  ),
  k(
    'kb-gap-code-obs-whitebox-vs-blackbox-monitoring',
    'White-box monitoring vs black-box monitoring',
    ['white-box monitoring vs black-box monitoring', 'difference white box black box monitoring', 'internal external monitoring'],
    "This is specifically about MONITORING a running system (not the separate concept of white-box vs black-box software TESTING, which is about testing code before release). BLACK-BOX monitoring observes a system purely from the OUTSIDE, the way an external user or client would experience it — e.g. a synthetic check that periodically hits a public URL and confirms it returns a 200 status within an acceptable time — it tells you WHETHER something is broken (a symptom) without any visibility into WHY, since it has no access to the system's internals. WHITE-BOX monitoring instead relies on the application itself being instrumented to expose its own internal state directly — custom metrics the code emits about its own internal behavior (queue depth, cache hit rate, how many items are in an internal retry buffer, which specific internal function is slow) — giving deep insight into WHY something is failing, but only for the specific internal signals someone thought to instrument in advance. The key difference: black-box monitoring checks a system's externally visible behavior with no knowledge of its internals (good for confirming user-facing symptoms), while white-box monitoring uses instrumentation built into the application itself to expose internal state directly (good for diagnosing root causes) — a mature monitoring setup uses both together, since black-box catches things you forgot to instrument and white-box explains what black-box alone can't.",
  ),
  k(
    'kb-gap-code-obs-synthetic-vs-rum',
    'Synthetic monitoring vs real user monitoring (RUM)',
    ['synthetic monitoring vs real user monitoring', 'RUM vs synthetic monitoring', 'difference synthetic RUM'],
    "SYNTHETIC monitoring uses automated scripts or scheduled checks (a scripted browser, or a simple periodic ping/HTTP request) that simulate a user action against your application on a fixed schedule, from a fixed set of locations — it runs constantly whether or not any real person is using the app, gives you an early, consistent, controlled signal the moment something breaks (since it doesn't depend on real traffic actually happening), and is great for catching outages during low-traffic periods (like 3am) when almost no real users would otherwise be around to notice. REAL USER MONITORING (RUM) instead captures performance and behavior data directly from ACTUAL live users as they use the real application — real page-load times, real click paths, real error rates, measured on real devices, real network conditions, and real geographic locations, giving a true picture of what your actual audience is actually experiencing (which can vary wildly by device, connection speed, and region in ways a synthetic check from one fixed test location would never reveal). The key difference: synthetic monitoring is proactive and consistent, using scripted fake traffic on a schedule to catch problems even with zero real visitors, while RUM is reactive and authentic, measuring the real experience of real users but only for the traffic that's actually happening — most mature setups use both, since synthetic monitoring catches an outage instantly during quiet periods while RUM reveals real-world experience issues synthetic checks would never simulate.",
  ),
  k(
    'kb-gap-code-obs-alerting-vs-monitoring',
    'Alerting vs monitoring',
    ['alerting vs monitoring', 'difference alerting monitoring', 'what is alerting in observability'],
    "MONITORING is the broad, continuous, passive activity of COLLECTING and displaying data about a system's health over time — dashboards, logs, metrics being gathered and stored so a human can look at them, whether proactively investigating an issue or just checking in on overall system health at any given moment. ALERTING is a specific, narrower, ACTIVE mechanism built on top of that monitored data: automatically evaluating collected metrics against predefined rules or thresholds ('if error rate exceeds 5% for more than 2 minutes'), and when a rule is triggered, PROACTIVELY notifying a human (via page, Slack message, email, or phone call) WITHOUT that person needing to be actively watching a dashboard at that exact moment. The key relationship: alerting depends entirely on monitoring data existing first (you can't alert on a metric nobody is collecting), but monitoring can exist perfectly well with zero alerting configured on top of it (a dashboard nobody has wired any alert rules to still shows useful data, it just requires a human to actively go look at it rather than being proactively notified). The key difference: monitoring is the ongoing collection and display of system data for humans to inspect, while alerting is the automated rule-based layer on top of that data that proactively pushes a notification to a human the moment a specific problem condition is detected, rather than requiring someone to be actively watching.",
  ),
  k(
    'kb-gap-code-obs-counter-vs-gauge',
    'Counter metric vs gauge metric',
    ['counter metric vs gauge metric', 'difference counter gauge metric', 'metric types monitoring'],
    "This is a monitoring METRIC TYPE distinction (used by systems like Prometheus), not over-the-counter medication or knitting tension. A COUNTER is a metric that can only ever go UP (or reset to zero when the process restarts) — it's used for cumulative totals that only accumulate, like 'total number of HTTP requests served' or 'total number of errors thrown since startup.' A counter is never directly useful as a raw number by itself (knowing 'there have been 4,502,391 requests total' tells you little) — it's almost always queried as a RATE of change over a time window instead ('requests per second over the last 5 minutes'), which is what actually reveals current load or error trends. A GAUGE is a metric that can freely go both UP AND DOWN, representing a value that fluctuates and can be read directly at any single point in time, like 'current memory usage,' 'number of items currently in a queue,' or 'current number of active connections' — a gauge's raw current value is directly meaningful on its own without needing to compute a rate first, unlike a counter. The key difference: a counter only ever increases (or resets to zero) and is typically analyzed as a rate of change over time, while a gauge can go up or down freely and its raw current value is directly meaningful at any single moment — using the wrong type (e.g. treating a fluctuating queue depth as a counter) breaks the metric's math entirely.",
  ),
  k(
    'kb-gap-code-obs-histogram-vs-summary-metric',
    'Histogram metric vs summary metric (monitoring)',
    ['histogram metric vs summary metric', 'difference histogram summary metric monitoring', 'metric type latency buckets'],
    "This is about monitoring metric TYPES (used for measuring distributions like request latency), not a photography tonal-range histogram or a generic English-language 'brief overview' summary. Both a HISTOGRAM metric and a SUMMARY metric exist specifically to capture the DISTRIBUTION of a measured value (like request latency) rather than just a single aggregate number, because an average alone can badly hide problems — e.g. an average latency of 100ms could hide the fact that 5% of requests are actually taking 3 full seconds. A HISTOGRAM metric works by counting how many observations fall into a set of predefined value ranges ('buckets') — e.g. how many requests took 0-10ms, 10-50ms, 50-100ms, 100-500ms, and so on — and critically, this bucketing happens on the CLIENT/application side and the raw bucket counts are what get sent to the monitoring server, meaning percentiles (like 'the 95th percentile latency') can be calculated and even AGGREGATED later across multiple different application instances/servers, at the cost of only approximate precision (limited by how the buckets were chosen). A SUMMARY metric instead calculates specific quantiles/percentiles (like p50, p95, p99) directly on the CLIENT side in real time and streams those already-calculated precise percentile values to the server — giving more PRECISE percentiles for that one single instance, but those pre-calculated percentiles generally CANNOT be meaningfully aggregated or averaged together across multiple different server instances afterward (you can't average two different p95 values from two different servers and get a valid combined p95). The key difference: a histogram sends bucketed raw counts that can be aggregated across many instances later (less precise per-instance, but combinable), while a summary calculates precise percentiles on the client itself (more precise for that one instance, but not meaningfully combinable across instances afterward).",
  ),
  k(
    'kb-gap-code-obs-push-vs-pull-metrics',
    'Push-based vs pull-based metrics collection',
    ['push-based vs pull-based metrics collection', 'push vs pull monitoring', 'difference push pull metrics collection prometheus statsd'],
    "In PUSH-based metrics collection, each individual application/service is responsible for actively sending its own metrics OUT to a central metrics-collection server on its own schedule, typically over UDP or a lightweight network call (StatsD is the classic example) — this works well for very short-lived processes (like a serverless function that might only run for a few hundred milliseconds and could be gone before anything else gets a chance to come ask it for data), but the central collector has no direct way to distinguish 'this service is healthy and just has nothing new to report' from 'this service has actually crashed and can no longer push anything,' since silence looks the same either way. In PULL-based metrics collection (the model Prometheus popularized), the central monitoring system instead actively reaches OUT and 'scrapes' (requests) metrics from each service at a regular configured interval, typically via an HTTP endpoint the service exposes (like `/metrics`) — the monitoring system controls the scrape schedule directly and centrally, can immediately tell a service is down if a scrape attempt itself outright fails or times out (a much clearer failure signal than the silence problem push has), and it's easier to see exactly which specific targets are currently being monitored at any given time, though it's poorly suited to extremely short-lived processes that may not exist long enough to ever get scraped. The key difference: push has each service proactively send its own metrics out on its own schedule (works for short-lived processes, but can't clearly distinguish 'healthy and quiet' from 'crashed'), while pull has the central monitoring system actively request metrics from each service on a controlled schedule (clearer down-detection and centralized scheduling, but ill-suited to very short-lived processes).",
  ),
  k(
    'kb-gap-code-obs-mttr-vs-mtbf',
    'MTTR vs MTBF',
    ['MTTR vs MTBF', 'difference MTTR MTBF', 'mean time to recovery mean time between failures'],
    "Both are reliability metrics used to measure how a system fails and recovers over time, but they measure opposite phases. MTBF (Mean Time Between Failures) measures RELIABILITY — the average length of time a system runs successfully BETWEEN one failure ending and the next failure starting; a HIGHER MTBF is better, since it means the system fails less often. MTTR (Mean Time To Recovery, sometimes 'Mean Time To Repair' or 'Mean Time To Resolution') measures RESPONSIVENESS — the average length of time it takes to fully detect, diagnose, and fix a failure once it has already happened and get the system back to healthy; a LOWER MTTR is better, since it means outages get resolved faster once they occur. A useful way to see the difference: MTBF is about how often bad things happen at all, while MTTR is about how quickly the team recovers once a bad thing HAS happened — a system could have a relatively low MTBF (fails somewhat often) but still deliver good overall availability if its MTTR is very low (each failure gets fixed almost immediately), and conversely a system that rarely fails (high MTBF) but takes days to recover on the rare occasion it does (very high MTTR) can still suffer serious cumulative downtime. The key difference: MTBF measures the average gap between failures (bigger is better — how often things break), while MTTR measures the average time to recover from a failure once it happens (smaller is better — how fast the team fixes it).",
  ),
  k(
    'kb-gap-code-obs-error-budget-vs-slo',
    'Error budget vs SLO',
    ['error budget vs SLO', 'difference error budget SLO', 'what is an error budget'],
    "This is an SRE/reliability-engineering concept, NOT baseball fielding-error scoring and NOT personal-finance budgeting. An SLO (Service Level Objective) is the actual reliability TARGET a team sets for a service, like '99.9% of requests succeed over a rolling 30-day window.' An ERROR BUDGET is the direct mathematical INVERSE of that target, converted into a concrete, spendable allowance of allowed failure: if the SLO is 99.9% success, the error budget is the remaining 0.1% of requests that are ALLOWED to fail (or be slow/erroring) over that same period before the SLO itself is actually breached. The error budget's real purpose is as a shared, objective DECISION-MAKING TOOL between reliability-focused and feature-focused teams: as long as there's still meaningful budget remaining (the service has been more reliable than the bare minimum required), the team is free to take on more risk — ship new features faster, deploy more aggressively, run more experiments — but once the error budget is close to fully spent (the service is right at the edge of breaching its SLO), the team is expected to pause new risky releases and prioritize reliability work instead until the budget replenishes. The key difference: the SLO is the reliability TARGET itself (e.g. 99.9% uptime), while the error budget is the remaining ALLOWANCE of acceptable failure calculated as the gap between 100% and that target — the error budget is what actually gets 'spent' by real-world incidents and is used to gate how much release risk a team can currently afford to take.",
  ),
  k(
    'kb-gap-code-obs-log-sampling-vs-trace-sampling',
    'Log sampling vs trace sampling',
    ['log sampling vs trace sampling', 'difference log trace sampling observability', 'sampling in observability'],
    "Both are ways of deliberately NOT capturing every single log line or trace in a very high-traffic system, specifically to control storage/processing cost and volume — but they apply to different data types and typically use different strategies. LOG sampling means only capturing/storing a fraction of the log lines generated (e.g. keeping only 1 in every 100 routine 'request succeeded' INFO-level logs) while still capturing essentially ALL error/warning-level logs regardless of sampling rate, since routine successful events are individually low-value at huge volume but a rare error is exactly the kind of thing you don't want to accidentally drop. TRACE sampling means only capturing full, complete traces (the whole multi-service span tree) for a fraction of all requests, rather than tracing every single request end-to-end — common strategies include a fixed sampling RATE (trace 1% of all requests, chosen up front before the request even starts, 'head-based' sampling) or, more sophisticated, 'tail-based' sampling, which waits until a request has actually finished and then decides whether to KEEP that specific completed trace based on what happened (always keep traces that were slow, or that had an error, even if they'd have otherwise been randomly dropped by the sampling rate) — tail-based sampling requires buffering the full trace data temporarily before deciding, but ensures the traces you actually keep are disproportionately the interesting/problematic ones rather than a purely random slice. The key difference: log sampling thins out the volume of individual log lines (while still keeping virtually all errors), while trace sampling decides which entire request traces to keep tracking end-to-end at all — and trace sampling specifically can be either decided upfront (head-based) or after the fact based on what actually happened (tail-based), a choice log sampling doesn't really have in the same way.",
  ),
  k(
    'kb-gap-code-obs-dashboard-vs-alert',
    'Dashboard vs alert (observability tooling)',
    ['dashboard vs alert observability', 'difference dashboard alert monitoring tool', 'what is an alert vs a dashboard'],
    "A DASHBOARD is a PULL-based, human-driven tool: a visual collection of graphs, charts, and numbers showing the current and historical state of chosen metrics, which a person has to actively choose to open and look at in order to get any information from it — dashboards are great for exploring trends over time, comparing multiple related metrics side by side, and investigating a problem once you already suspect something's wrong, but they provide zero value if nobody happens to be looking at them at the exact moment something breaks. An ALERT is a PUSH-based, automated tool: a predefined rule that continuously evaluates one specific metric or condition in the background, and when that condition is met (e.g. 'error rate above 5% for 2+ minutes'), automatically and proactively notifies a specific human or team (via page, Slack, SMS, phone call) WITHOUT anyone needing to be actively watching anything at that moment. The key difference: a dashboard requires a human to actively go look at it to learn anything (great for exploration and investigation, but passive), while an alert proactively pushes a notification to a human the instant a predefined problem condition is met, with no one needing to be watching at all (great for fast incident detection, but only for conditions someone specifically thought to configure an alert rule for in advance) — a mature setup uses dashboards heavily during active investigation of a known issue, and alerts specifically to make sure the team finds out about a new issue in the first place.",
  ),
  k(
    'kb-gap-code-obs-red-vs-use-method',
    'The RED method vs the USE method',
    ['RED method vs USE method monitoring', 'difference RED USE method', 'what is the RED method what is the USE method'],
    "These are two widely-referenced, complementary frameworks for choosing WHICH metrics to actually monitor for a given part of a system, and they apply to different kinds of components. The RED method (Rate, Errors, Duration) is aimed at REQUEST-DRIVEN services — anything that handles incoming requests, like a web service or an API: RATE is how many requests per second it's currently handling, ERRORS is how many of those requests are failing, and DURATION is how long requests are taking to complete (typically tracked as percentiles, like p95/p99 latency, not just a plain average, since an average can badly hide a slow tail). The USE method (Utilization, Saturation, Errors) is aimed instead at RESOURCES — the underlying infrastructure a service runs on, like CPU, memory, disk, or a network link: UTILIZATION is the percentage of time/capacity that resource was actively busy doing work, SATURATION is how much extra demand is currently queued up waiting for that resource because it's already fully busy (a queue depth, essentially — a resource can be under 100% utilized but still badly saturated if requests are piling up faster than it can drain them), and ERRORS is the count of actual error events that resource itself generated (disk I/O errors, dropped network packets). The key difference: the RED method is built for monitoring request-driven SERVICES (rate, errors, duration of the requests they handle), while the USE method is built for monitoring underlying RESOURCES (utilization, saturation, and errors of the infrastructure those services actually run on) — a complete monitoring setup typically applies RED to every service and USE to every piece of infrastructure underneath it.",
  ),
  k(
    'kb-gap-code-obs-incident-vs-outage',
    'Incident vs outage (SRE/operations sense)',
    ['incident vs outage SRE operations', 'difference incident outage software', 'what counts as an incident vs an outage'],
    "In an SRE/operations context (as distinct from the separate cybersecurity sense of 'incident,' which specifically concerns a security event), an INCIDENT is the broad umbrella term for ANY unplanned event that degrades, or risks degrading, a service's expected behavior enough to require an active human response — this includes full outright failures, but also partial degradations (elevated error rates, unusually slow response times, a single non-critical feature broken) that never actually take the whole service fully down. An OUTAGE is a specific, more severe SUBSET of incidents: a period where the service (or some specific critical part of it) is genuinely UNAVAILABLE or completely unusable for its users, not merely degraded or slower than usual. Put another way: every outage IS an incident (since it's an unplanned event that needed a response), but not every incident is an outage — a service running noticeably slower than normal, or one specific minor feature being broken while everything else works fine, is an incident but would typically NOT be classified as a full outage. The key difference: incident is the broad category covering any unplanned issue serious enough to need a response (degradations included), while outage specifically means the service (or a critical part of it) was genuinely unavailable — outages are always incidents, but many incidents never rise to the level of a true outage.",
  ),
  k(
    'kb-gap-code-obs-postmortem-vs-rca',
    'Postmortem vs root cause analysis',
    ['postmortem vs root cause analysis software incident', 'difference postmortem RCA', 'what is an incident postmortem'],
    "In a software/SRE context, this has nothing to do with forensic pathology (livor mortis, autopsies) — a POSTMORTEM here is the broader written DOCUMENT/PROCESS a team produces after an incident is resolved, covering the FULL story: a timeline of what happened and when, the overall impact (how many users affected, for how long), how the team detected and responded to it, what went well and what didn't in the response itself, and a list of concrete action items to prevent recurrence or improve future response — critically, a healthy 'blameless' postmortem culture focuses on what in the SYSTEM or PROCESS allowed the failure to happen, explicitly avoiding blaming the specific individual who, say, pushed a bad deploy, since punishing individuals just teaches people to hide mistakes rather than surface them. A ROOT CAUSE ANALYSIS (RCA) is one specific, narrower COMPONENT that typically lives inside a postmortem: the focused technical investigation into the actual underlying cause(s) of the failure itself, often using a structured technique like the '5 Whys' (repeatedly asking 'why did that happen?' to drill down past a surface-level trigger to the deeper systemic cause) — a good RCA usually finds not just one single root cause but a chain of contributing factors. The key difference: a postmortem is the complete document covering the full incident story (timeline, impact, response quality, and action items), while a root cause analysis is the specific, narrower technical investigation into WHY the failure actually happened that's typically embedded as one section within that larger postmortem — you can't meaningfully write a full postmortem without doing at least some root cause analysis, but an RCA alone, without the surrounding timeline/impact/process review, isn't a complete postmortem.",
  ),
  k(
    'kb-gap-code-obs-log-rotation-vs-retention',
    'Log rotation vs log retention (application/system log files)',
    ['log rotation vs log retention application log files', 'difference log rotation retention logrotate', 'what is log rotation'],
    "This is about ordinary application/system LOG FILES on disk (managed by a tool like `logrotate` on Linux), NOT Kafka's distributed-log retention feature, which is a different, unrelated concept about a messaging system keeping a replayable stream of events. LOG ROTATION is the process of periodically closing off the CURRENT log file once it hits a size or age limit, renaming/archiving it (often compressing it, e.g. `app.log` becomes `app.log.1.gz`), and starting a brand-new empty log file for new entries to be written into going forward — its purpose is purely practical: preventing a single ever-growing log file from eventually filling up the entire disk and crashing the server, and keeping individual log files at a manageable size that's actually fast to open and search. LOG RETENTION is a separate, later decision: how LONG to actually keep those rotated/archived log files around before permanently deleting them for good (e.g. 'keep the last 30 days of rotated logs, then delete older ones automatically') — retention policy is often driven by a mix of practical storage-cost limits and, in regulated industries, legal/compliance requirements dictating a minimum retention period. The key difference: log rotation is about SPLITTING an ever-growing log file into manageable, periodically-archived chunks so nothing grows unbounded, while log retention is the separate policy governing how long those already-rotated archived files get kept before being permanently deleted — a system can rotate logs every hour while still retaining 90 days of rotated history, or rotate daily while only retaining a week; the two settings are independent of each other.",
  ),
  k(
    'kb-gap-code-obs-uptime-vs-availability',
    'Uptime vs availability',
    ['uptime vs availability', 'difference uptime availability SLA', 'what does 99.9 percent availability mean'],
    "This is about system reliability, NOT the cognitive-bias 'availability heuristic' from psychology. UPTIME is the simplest, most literal measure: the actual amount of TIME a system was physically running and not crashed/down — often expressed as a raw duration ('this server has been up for 47 days straight') or a simple percentage of elapsed time the process was alive. AVAILABILITY is a related but more precise and more commonly used metric in reliability engineering: the percentage of time a system was actually able to successfully serve legitimate requests as expected, which can differ meaningfully from raw uptime — a server process can technically be 'up' (not crashed) while still being functionally UNAVAILABLE to real users, for example if it's overloaded and timing out on every request, disconnected from the network, or returning errors for every call despite the process itself still technically running. Availability is almost always expressed as a percentage of a defined time period, famously described in terms of 'nines': 99% availability ('two nines') allows roughly 3.65 days of downtime per year, 99.9% ('three nines') allows about 8.76 hours per year, and 99.99% ('four nines') allows only about 52.6 minutes per year — each additional 'nine' represents roughly a 10x reduction in allowed downtime and is dramatically harder and more expensive to actually achieve. The key difference: uptime is the raw, literal measure of whether a process is running at all, while availability is the more meaningful measure of whether the system was actually successfully serving real requests as expected — a system can have high raw uptime while still having poor real availability if it's technically running but functionally broken.",
  ),
  k(
    'kb-gap-code-obs-runbook-vs-playbook',
    'Runbook vs playbook (incident response)',
    ['runbook vs playbook incident response', 'difference runbook playbook operations', 'what is a runbook what is a playbook'],
    "A RUNBOOK is a very specific, detailed, step-by-step set of instructions for handling ONE particular, well-understood, recurring operational task or known failure scenario — e.g. exact commands and steps to restart a specific stuck service, roll back a specific bad deployment, or fail over a specific database to its replica — written precisely enough that someone reasonably familiar with the system (even a newer on-call engineer at 3am) can follow it directly without needing deep expertise in that specific subsystem, and many runbook steps are eventually automated into scripts entirely, since they're repetitive and well-defined. A PLAYBOOK is broader and less prescriptive: a higher-level guide for handling a whole CATEGORY or CLASS of situation (e.g. 'how to run an incident response for any severity-1 outage') rather than one exact known failure — a playbook typically covers roles and responsibilities during an incident (who's the incident commander, who communicates with customers, who actually investigates), communication protocols and escalation paths, and general decision-making guidance, but deliberately leaves room for judgment since the specific situation triggering it wasn't fully known in advance. The key difference: a runbook gives precise, literal step-by-step instructions for one specific, previously-known scenario (and is often eventually automated), while a playbook gives broader process/role/communication guidance for handling an entire category of situation where the specific details couldn't be fully anticipated in advance — during a real incident, the on-call engineer typically follows the PLAYBOOK for overall process (who does what, how to communicate) while reaching for a specific RUNBOOK if the actual failure matches one they already have a documented procedure for.",
  ),
  k(
    'kb-gap-code-obs-log-aggregation-vs-parsing',
    'Log aggregation vs log parsing',
    ['log aggregation vs log parsing', 'difference log aggregation parsing', 'what is log aggregation'],
    "LOG PARSING is the process of taking one individual raw log line (which might be free-form text or already-structured JSON) and extracting specific meaningful FIELDS out of it — turning `2026-09-14 14:32:01 ERROR payment failed for order 4821: card declined` into distinct labeled data like `{timestamp, level: 'ERROR', order_id: 4821, reason: 'card declined'}` — parsing happens per individual log line and is what makes a log line's contents actually queryable/filterable rather than just an opaque block of text. LOG AGGREGATION is a separate, larger-scale operation: collecting log entries (already parsed or not) from MANY different sources at once — potentially hundreds or thousands of individual servers, containers, and services scattered across a distributed system — and centralizing them all into one single searchable place (a tool like Elasticsearch, Loki, or Splunk), so an engineer doesn't have to manually SSH into dozens of individual machines one at a time to piece together what happened across a multi-service incident. The key difference: log parsing operates on ONE log line at a time, extracting structured fields out of its raw content, while log aggregation operates at the scale of an ENTIRE distributed system, centrally collecting and unifying logs from many separate sources into one searchable place — a real production log pipeline typically does both together: each log line gets parsed into structured fields as it's collected, and all of those parsed logs from every service get aggregated into one central searchable system.",
  ),
];
