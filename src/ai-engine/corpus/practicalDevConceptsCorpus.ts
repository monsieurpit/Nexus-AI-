import { KnowledgeItem } from '../../types';

// Code knowledge expansion (2026-09-14) — additional practical development concepts: CORS,
// webhooks, rate limiting, pagination, migrations, encoding, deployment strategies, and testing
// vocabulary. Plain neutral educational content, rounding out the coding corpus's breadth.

const now = Date.now();
const k = (id: string, title: string, keywords: string[], content: string): KnowledgeItem => ({
  id,
  title,
  category: 'Programming',
  keywords,
  content,
  createdAt: now,
});

export const PRACTICAL_DEV_CONCEPTS_CORPUS: KnowledgeItem[] = [
  k(
    'kb-code-cors-basics',
    'CORS basics',
    ['cors basics', 'cross origin resource sharing'],
    "CORS (Cross-Origin Resource Sharing) is a browser security mechanism that, by default, blocks a web page loaded from one origin (a specific combination of protocol, domain, and port) from making requests to a DIFFERENT origin's API — this is called the \"same-origin policy,\" and it exists to prevent a malicious website from silently making authenticated requests on a user's behalf to some other unrelated site the user happens to be logged into, using the user's own browser cookies without their knowledge or consent. When a legitimate cross-origin request genuinely IS needed (a frontend hosted on `app.example.com` calling an API hosted on `api.example.com`), the SERVER being called must explicitly opt in by returning specific CORS response headers, most importantly `Access-Control-Allow-Origin`, listing which origins are permitted to receive its responses. For more sensitive request types (like ones using custom headers or methods beyond simple GET/POST), the browser first automatically sends a \"preflight\" `OPTIONS` request asking the server for permission before sending the actual real request, and only proceeds with the real request if the server's preflight response indicates it's allowed. A common beginner confusion: CORS errors are enforced entirely by the BROWSER, not the server — a server's API still technically processes and can be called just fine via non-browser tools like `curl` or a backend script, even in situations where the exact same request would be blocked when attempted directly from within browser-based JavaScript.",
  ),
  k(
    'kb-code-webhooks-vs-polling',
    'Webhooks vs polling',
    ['webhooks basics', 'webhooks vs polling'],
    "Polling means a client repeatedly asks a server \"has anything new happened yet?\" at fixed intervals (every few seconds or minutes), even when the honest answer is usually \"no\" — this is simple to implement but wastes resources on both sides with a stream of mostly-empty requests, and inherently introduces some delay, since a genuine new event is only actually noticed the next time the client happens to poll again, not the instant it actually occurs. A webhook flips this relationship: instead of the client repeatedly asking, the SERVER itself proactively sends an HTTP request (typically a `POST`) to a URL the client registered in advance, the moment a relevant event actually happens — a payment provider calling a webhook URL the instant a payment succeeds, rather than the client having to repeatedly poll \"is my payment done yet?\" over and over. This is far more efficient (no wasted, mostly-empty requests) and delivers near-instant notification of genuine events, but it requires the receiving client to expose a publicly reachable endpoint the sending server can actually call, and requires reliably handling the real possibility of a webhook delivery failing or arriving more than once (many providers automatically retry a webhook delivery that didn't get a successful acknowledgment response, so a well-built webhook receiver needs to be safely idempotent about processing the same event twice).",
  ),
  k(
    'kb-code-api-rate-limiting',
    'API rate limiting',
    ['api rate limiting', 'what is rate limiting'],
    "Rate limiting restricts how many requests a client (identified by API key, IP address, or user account) can make to an API within a given time window, protecting the service from being overwhelmed — whether by a genuine traffic spike, an unintentional runaway bug in a client's own code hammering the API in a tight loop, or a deliberate malicious attack. A common approach returns an HTTP 429 (\"Too Many Requests\") status code once a client exceeds its allotted limit, often alongside headers communicating the current limit, how many requests remain in the current window, and when the limit will reset, so well-behaved clients can proactively slow down before actually hitting the hard limit. A frequently used underlying algorithm is the \"token bucket\": each client has a bucket that holds a maximum number of tokens, one token is consumed per request, and tokens refill at a steady, fixed rate over time — this naturally allows brief, legitimate short bursts of activity (as long as tokens are currently available in the bucket) while still enforcing a firm long-run average rate once the bucket runs dry. Rate limiting is typically implemented as middleware sitting in front of an API's actual business logic, often backed by a fast, shared, in-memory store like Redis to efficiently track each individual client's current request counts, especially across multiple separate server instances that all need to share and stay consistent on the very same rate-limit counts.",
  ),
  k(
    'kb-code-api-pagination',
    'API pagination',
    ['api pagination', 'offset vs cursor pagination'],
    "Pagination breaks a large result set into smaller, more manageable pages returned one request at a time, rather than a single API response trying to return potentially millions of rows all at once, which would be far too slow and memory-intensive for both the server generating it and the client actually receiving and processing it. Offset-based pagination uses a page number or numeric offset plus a page size, e.g. `GET /users?offset=40&limit=20` requests the third page of 20 users — simple to understand and implement, but can behave inconsistently in practice if the underlying data changes between page requests (a row inserted or deleted earlier in the overall sorted result set shifts every subsequent row's numeric offset, which can cause a client paging through results to accidentally skip a row or see the exact same row twice across two consecutive page requests). Cursor-based pagination instead returns an opaque \"cursor\" pointing to a specific exact position in the result set (often, under the hood, a unique row identifier plus its sort value), and the next page is requested by asking for results \"after this specific cursor\" rather than by a numeric offset — this remains correct and stable even as the underlying data changes concurrently between page requests, which is why cursor-based pagination is generally preferred for large or frequently-changing, actively-updated datasets, at the cost of not directly supporting jumping straight to an arbitrary specific page number the way offset-based pagination naturally allows.",
  ),
  k(
    'kb-code-database-migrations',
    'Database migrations',
    ['database migrations', 'what is a database migration'],
    "A database migration is a version-controlled, incremental script describing a specific change to a database's schema (adding a column, creating a new table, adding an index) — rather than manually running ad-hoc SQL commands directly against a production database by hand, migrations are checked into the same version control system as the application's own source code, letting schema changes be reviewed, tracked, and applied consistently and repeatably across every environment (a developer's own local machine, a staging server, production) in the exact same order every single time. Each migration typically defines both an \"up\" operation (the actual forward change to apply, like `ADD COLUMN email VARCHAR(255)`) and a corresponding \"down\" operation (how to safely reverse that specific change if it later needs to be rolled back, like `DROP COLUMN email`), and a migration tool tracks precisely which migrations have already been applied to a given specific database, so running migrations is safely idempotent — re-running the same migration command doesn't try to reapply a migration that's already been applied previously. Popular migration tools include Django's built-in migrations for Python, Flyway and Liquibase for Java, ActiveRecord migrations for Ruby on Rails, and Prisma Migrate for the TypeScript/JavaScript ecosystem. A generally well-followed best practice is making migrations backward-compatible where realistically possible (e.g. adding a new nullable column rather than an immediately-required one), so that an application still actively running the PREVIOUS version of the code doesn't unexpectedly break during the brief window while a deployment is gradually rolling out and both old and new application versions may briefly coexist.",
  ),
  k(
    'kb-code-floating-point-precision',
    'Floating-point precision issues',
    ['floating point precision', '0.1 + 0.2 javascript', 'floating point rounding error'],
    "A famous, widely encountered programming surprise: in most languages, `0.1 + 0.2` doesn't equal exactly `0.3` — it actually evaluates to something like `0.30000000000000004`. This isn't a specific bug in any one particular language; it's an inherent, fundamental consequence of how standard IEEE 754 floating-point numbers represent decimal values in binary. Just as some simple fractions like 1/3 can't be represented exactly and finitely in base-10 decimal notation (0.333... repeating forever), many everyday decimal fractions (including 0.1 and 0.2) can't be represented exactly and finitely in binary floating-point either, so they're stored as the closest possible binary APPROXIMATION instead, and small rounding errors from that approximation can accumulate and surface during arithmetic operations performed on them. This matters practically for anything involving precise decimal math, most notably money — many financial applications deliberately avoid plain floating-point numbers entirely for currency values, instead either working in the smallest whole-number unit (storing amounts as an integer count of cents rather than a fractional number of dollars) or using a dedicated arbitrary-precision decimal type specifically designed to avoid this exact class of representation error (like Python's `decimal.Decimal`, or a database's native `DECIMAL`/`NUMERIC` column type) rather than an ordinary binary floating-point type.",
  ),
  k(
    'kb-code-character-encoding-utf8',
    'Character encoding and UTF-8',
    ['character encoding basics', 'utf-8 basics', 'unicode basics'],
    "Computers ultimately store all text as raw numbers, and a character encoding defines the specific, agreed-upon mapping between those numbers and actual human-readable characters. ASCII, an early and highly influential encoding, covers only 128 characters — the basic unadorned English alphabet, digits, and a handful of common punctuation and control characters — which is nowhere near sufficient for the vast majority of the world's languages and scripts. Unicode is a much larger, far more comprehensive standard assigning a unique numeric \"code point\" to essentially every character across virtually every written language in use today, plus emoji and a wide range of symbols. UTF-8 is by far the most widely used way of actually ENCODING those Unicode code points into concrete bytes for storage or transmission — it's a variable-width encoding, meaning different characters take up different numbers of bytes: basic ASCII characters still take exactly 1 byte each (making UTF-8 fully backward-compatible with plain existing ASCII text), while many other characters (accented letters, non-Latin scripts, emoji) take 2, 3, or up to 4 bytes each. A very common real-world bug source is a mismatch between the encoding a file or data stream was actually written in and the encoding some other tool later assumes when reading it back — this produces visibly garbled or corrupted-looking text (sometimes informally called \"mojibake\"), which is exactly why explicitly specifying UTF-8 consistently everywhere (in HTML meta tags, HTTP response headers, file I/O calls, and database column/connection settings) is such a strongly and widely recommended default practice.",
  ),
  k(
    'kb-code-enums',
    'Enums (enumerated types)',
    ['enum programming', 'enumerated type basics'],
    "An enum (enumerated type) defines a named type with a small, fixed, explicitly limited set of possible values, used to make code more readable and considerably safer than using loose, untyped raw strings or arbitrary numeric constants scattered throughout a codebase to represent the exact same limited set of meaningful options. In TypeScript: `enum Status { Pending, Active, Completed }`, then a variable typed `Status` can only ever legitimately hold one of those three specific defined values, and the compiler will flag an attempt to assign or compare against anything else entirely — versus using a bare, unconstrained string like `\"pending\"` directly, where a typo (`\"pnding\"`) would silently compile and run without any error at all, only failing much later and far less obviously at actual runtime, once the mismatched string genuinely fails to match anywhere it was expected to. Enums also serve as helpful, self-documenting communication for anyone reading the code later — `Status.Active` is immediately self-explanatory in a way an opaque bare numeric constant like `1` scattered through the code never quite is on its own, without cross-referencing some separate comment or piece of external documentation explaining what that particular number is actually supposed to represent. Java, C#, Kotlin, Swift, and Rust all have similarly robust, well-integrated native enum support; Python added a proper, dedicated `Enum` class to its standard library, and plain JavaScript notably has no built-in native enum construct at all, so JS code commonly simulates the same underlying pattern using a plain frozen object or a TypeScript enum instead.",
  ),
  k(
    'kb-code-null-vs-undefined-js',
    'null vs undefined in JavaScript',
    ['javascript null vs undefined', 'null undefined difference'],
    "JavaScript has two distinct values representing \"nothing,\" which frequently confuses newcomers coming from languages with just one such concept. `undefined` is what JavaScript automatically gives a variable that has been declared but never explicitly assigned a value (`let x; console.log(x); // undefined`), and it's also what a function implicitly returns if it has no explicit `return` statement, and what accessing a genuinely nonexistent object property returns. `null` is instead an explicit, deliberate value a developer assigns themselves specifically to represent \"intentionally no value here\" — the language itself never automatically produces `null` on its own the way it automatically produces `undefined`; a developer has to deliberately choose and write `null` somewhere for it to appear at all. Comparing them: `null == undefined` is `true` (using loose equality, since JavaScript specifically treats them as loosely equal to each other in this one particular comparison), but `null === undefined` is `false` (using strict equality, since they remain genuinely two distinct, different types under the hood — `typeof null` famously and rather confusingly returns `\"object\"`, a widely acknowledged long-standing historical quirk/bug in the language that's now permanently baked in and can never realistically be fixed without breaking a large amount of existing code that has come to depend on that exact long-standing behavior). Many style guides recommend picking one of the two consistently for representing intentional absence in your own application-level code (commonly `null`), reserving `undefined` specifically for its narrower, more automatic \"this was never actually set at all\" language-level meaning.",
  ),
  k(
    'kb-code-mocks-stubs-fakes',
    'Test doubles: mocks, stubs, and fakes',
    ['mock vs stub vs fake', 'test doubles testing'],
    "\"Test double\" is the general umbrella term for any object used in place of a real dependency specifically for testing purposes, and the specific terms mock, stub, and fake describe meaningfully different flavors of that same general idea. A stub is a simple test double that returns predetermined, hardcoded canned responses when called, with no real internal logic behind it and no verification of exactly how it was called — it exists purely to let the code under test run at all, by supplying the specific fixed data that code needs from that dependency to proceed. A mock goes further than a stub by additionally verifying HOW it was actually used during the test — a test might assert that a particular mocked method was called exactly once, or called with the specific expected arguments, failing the test if that specific expected interaction pattern didn't actually happen as expected. A fake is a genuinely simplified but still real, actually-working implementation of the dependency — for example, using a fast in-memory list-backed database implementation in place of a real full production database during a test, rather than either merely simulating fixed canned answers (a stub) or specifically verifying particular call patterns (a mock) — a fake actually behaves correctly, just via a much simpler, faster, and lighter-weight internal implementation better suited for quick automated tests than the full real thing would be. Choosing the right kind of test double for the given situation keeps tests both fast (avoiding genuinely slow, heavy real dependencies like an actual network call or a full real database) and appropriately focused specifically on the exact behavior the test is actually trying to verify, rather than incidentally also testing unrelated other dependencies along the way at the same time.",
  ),
  k(
    'kb-code-blue-green-canary-deployment',
    'Blue-green and canary deployments',
    ['blue green deployment', 'canary deployment', 'zero downtime deployment'],
    "Blue-green deployment maintains two identical, complete, fully separate production environments (\"blue\" and \"green\") — at any given moment, only ONE of the two is actually receiving live real user traffic. To release a new version, it's deployed entirely to the currently-idle environment (say, green, while blue is currently live), and thoroughly tested there in a fully realistic production-like setting without affecting any real users at all, and once it's confirmed working correctly, traffic is switched over from blue to green essentially instantly (often just by updating a load balancer or router's configuration) — if something unexpected still goes wrong even after that switch, traffic can be instantly switched right back to blue, giving a very fast, low-risk, low-drama rollback path. Canary deployment instead gradually shifts traffic to a new version incrementally — first routing just a small percentage of real users (say, 5%) to the new version while the vast majority continue on the proven, stable old version, closely monitoring for any errors or concerning metric regressions specifically among that small canary group, then progressively increasing that percentage over time (10%, 25%, 50%, 100%) as confidence in the new version steadily grows — this limits the actual real-world blast radius of a previously-undetected bug to just a small fraction of total users at any given point, rather than every single user being affected all at once immediately upon deployment, the way a traditional all-at-once deployment inherently would.",
  ),
  k(
    'kb-code-monorepo-vs-polyrepo',
    'Monorepo vs polyrepo',
    ['monorepo vs polyrepo', 'monorepo basics'],
    "A monorepo stores the code for multiple distinct projects (or multiple separate services within one larger overall system) together in a single shared Git repository, whereas a polyrepo (the more traditional, historically default approach) gives each individual project or service its own genuinely separate, independent repository. A monorepo makes sharing code between projects considerably easier (a shared internal utility library can just live as an ordinary directory right there in the same repository, with no separate publishing/versioning step needed to actually consume it elsewhere), makes large sweeping cross-cutting changes spanning multiple projects at once far simpler to review and land safely together as a single atomic, coordinated commit, and gives everyone full, complete visibility into the entire codebase from one single unified place. The tradeoffs: a monorepo can grow extremely large over time, which can meaningfully slow down certain common tooling operations (cloning, full-repository searching, some CI processes) without deliberately investing in specialized additional tooling designed specifically to handle that scale gracefully (tools like Bazel, Nx, or Turborepo exist largely to address exactly this specific problem), and it requires more careful, deliberate access-control consideration if genuinely different teams working within the same monorepo shouldn't necessarily all have equally unrestricted access to every other part of it. Large, well-known tech companies like Google and Meta famously use enormous internal monorepos at massive scale, while many other equally successful organizations instead deliberately prefer polyrepos specifically for the cleaner project isolation and simpler, more independent per-project permission boundaries that approach naturally provides.",
  ),
  k(
    'kb-code-space-complexity',
    'Space complexity',
    ['space complexity big o', 'algorithm memory usage'],
    "While time complexity (commonly expressed using Big-O notation) describes how an algorithm's RUNNING TIME grows as input size grows, space complexity uses that exact same Big-O notation to instead describe how much extra MEMORY an algorithm needs, again as a function of input size n. An algorithm with O(1) space complexity uses a constant, fixed amount of extra memory regardless of how large the input actually is — for example, an in-place iterative algorithm that only tracks a small handful of fixed variables (like an index and a running sum) while working directly on the original input in place, without ever allocating any new memory proportional to the input's own size. An algorithm with O(n) space complexity uses memory that grows directly, roughly proportionally with the input size — for example, merge sort's classic implementation, which allocates a genuinely separate additional temporary array during its merging step, distinctly unlike quicksort's typical in-place implementation, which sorts directly within the original array using only O(log n) extra space specifically for its own recursive call stack, not for any separate additional working array. There's frequently a meaningful, real time-space tradeoff at play in algorithm design — a memoized (cached) recursive solution to a problem often trades additional memory (to actually store all those previously-computed intermediate results) specifically in exchange for a substantially faster overall running time, compared to a naive equivalent recursive approach that instead needlessly, repeatedly recomputes the exact same overlapping subproblem results over and over again from scratch every single time they're needed.",
  ),
  k(
    'kb-code-string-interning',
    'String interning',
    ['string interning', 'string pool programming'],
    "String interning is an optimization technique where a language's runtime stores only ONE single shared copy of each distinct string value in memory (commonly called a \"string pool\" or \"intern pool\"), and reuses that exact same single shared copy every time the identical string content is needed again elsewhere in the program, rather than allocating a brand new separate, redundant copy in memory every single time an identical string literal happens to appear. Java famously interns string LITERALS (fixed strings written directly in the source code) automatically by default — two separately written string literals with identical content, like `\"hello\"` appearing in two completely different places in the code, will actually be `==` (reference-)equal to each other because they both point to the very same single interned pooled object under the hood, whereas a string explicitly built dynamically at runtime via `new String(\"hello\")` deliberately creates a genuinely separate, distinct new object instance instead, bypassing that automatic pooling behavior. Python similarly interns many short string literals and identifier-like strings automatically as a routine, largely invisible internal optimization. String interning meaningfully saves memory specifically when the exact same string content legitimately repeats very frequently throughout a program's actual data (a good practical example: many repeated identical category or status label strings scattered throughout a large in-memory dataset), and it can also meaningfully speed up equality comparisons in some specific cases, since comparing whether two interned strings are the exact same underlying shared object (a fast, cheap reference-identity check) can sometimes be done instead of the generally slower, full character-by-character content comparison that's otherwise needed to properly compare two arbitrary, non-interned strings for equality.",
  ),
  k(
    'kb-code-immutable-data-across-languages',
    'Immutability conventions across languages',
    ['immutability across programming languages', 'const final readonly comparison'],
    "Many languages provide a keyword specifically for declaring an immutable, non-reassignable binding, though the exact underlying guarantee each one actually provides differs meaningfully and is worth understanding precisely rather than assuming they're all fully equivalent. JavaScript/TypeScript's `const` prevents REASSIGNING the variable itself to point to a different value entirely, but if that value is an object or array, its own internal contents can still be freely mutated afterward (`const arr = [1,2]; arr.push(3);` is perfectly legal and works fine, since `arr` itself is never actually reassigned to a different array — only its existing internal contents change). Java's `final` on a variable works essentially the same way — it prevents reassignment of the reference itself, but not mutation of a mutable object that reference happens to point to. Swift's and Kotlin's `let`/`val` behave the same way as these. Rust's default immutability (`let x = 5;`, requiring an explicit `mut` keyword specifically to opt IN to mutability instead) is comparatively closer to Clojure's genuinely deep, pervasive immutability specifically for its own core built-in data structures — though even Rust's plain `let` binding is still fundamentally the same general \"can't be reassigned\" kind of binding-level immutability as JavaScript's `const`, unless the underlying data type itself has additionally been specifically designed by its author to be deeply, structurally immutable all the way through, which is a genuinely separate design property from the mere reassignment-immutability of the binding/variable itself.",
  ),
  k(
    'kb-code-null-safety-comparison',
    'How different languages handle null safety',
    ['null safety comparison languages', 'null pointer exception prevention'],
    "Handling the absence of a value (\"nothing here\") safely is one of the most common, genuinely long-standing sources of runtime bugs across programming languages generally, and different languages take meaningfully different approaches to managing that specific risk. Older or more permissive languages like Java (pre-Optional) and JavaScript allow essentially ANY reference-typed variable to potentially be null/undefined at literally any point, with the type system itself doing nothing at all to specifically flag or prevent that particular possibility — a `NullPointerException` (Java) or \"Cannot read properties of undefined\" (JavaScript) can occur at almost any point in a codebase where a value that was silently assumed to definitely be present unexpectedly turns out to actually be null/undefined instead. Modern languages increasingly build null-safety directly into their own type systems instead: Kotlin, Swift, and Dart all distinguish nullable types (`String?`) from genuinely non-nullable ones (`String`) at the type-system level itself, and the compiler actively forces an explicit check or safe-unwrap before a nullable value can be used anywhere a non-null one is actually required. Rust and Haskell go even further, having no dedicated `null` concept baked into the language at all — instead representing optional absence explicitly through an ordinary value-level type (`Option<T>` in Rust, `Maybe` in Haskell) that must be explicitly, deliberately pattern-matched or unwrapped before its potential inner value can actually be used, which makes forgetting to handle the specific \"nothing here\" case a genuine, hard compile-time type error rather than a silent, easy-to-overlook runtime crash waiting to happen unpredictably much later, potentially in production.",
  ),
  k(
    'kb-code-first-class-functions',
    'First-class and higher-order functions',
    ['first class functions', 'higher order functions'],
    "A language has \"first-class functions\" when functions can be treated just like any other ordinary value — assigned to a variable, passed as an argument into another function, returned as the actual result FROM another function, and stored inside a data structure like a list, exactly the same as a number or a string could be. Most modern mainstream languages (Python, JavaScript, Ruby, Kotlin, Swift, Rust, Go) support this directly; some older or more restrictive languages historically didn't, at least not without noticeably more ceremony and boilerplate involved. A \"higher-order function\" is specifically any function that either takes another function as one of its own arguments, or returns a function as its own result (or genuinely both at once) — `.map()`, `.filter()`, and `.reduce()` are all classic, extremely common everyday examples, since each one takes a caller-supplied function as an argument describing exactly what specific transformation or check to actually apply to each individual element. First-class functions are precisely what enables this style of higher-order-function-based, more declarative programming, and they're also the essential foundation underlying closures, decorators, and most other core functional-programming patterns generally — without functions being genuinely first-class citizen VALUES in their own right within the language, none of those related, closely-connected patterns and techniques would even be structurally possible to express at all in the first place.",
  ),
];
