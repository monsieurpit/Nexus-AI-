import { KnowledgeItem } from '../../types';

export const PROGRAMMING_CORPUS: KnowledgeItem[] = [
  {
    id: 'kb-prog-python-basics',
    title: 'Python: The Basics Every Beginner Needs to Know',
    category: 'Programming',
    keywords: ['python', 'programming', 'coding', 'beginner', 'syntax', 'variables', 'functions', 'lists', 'loops', 'library', 'pip', 'data science', 'scripting', 'automation'],
    content: `Python is the most popular programming language for beginners, data science, automation, AI/ML, and scripting. It uses readable, English-like syntax with significant indentation (spaces or tabs define code blocks, not braces). Variables are dynamically typed — no need to declare types. Data types: int (42), float (3.14), str ("hello"), bool (True/False), list ([1,2,3]), tuple ((1,2,3) — immutable), dict ({"key": "value"}), set ({1,2,3} — unique values). Control flow: if/elif/else for conditionals; for loops iterate over sequences; while loops run until a condition is false. Functions are defined with def: def greet(name): return f"Hello {name}". f-strings (f"Hello {variable}") are the modern way to format strings. List comprehensions provide concise list creation: squares = [x**2 for x in range(10)]. Modules and libraries extend Python: import math, import random, import os. The pip package manager installs third-party packages: pip install requests. Virtual environments (python -m venv env) isolate project dependencies. Essential libraries: NumPy (numerical computing), Pandas (data manipulation), Matplotlib (visualisation), Requests (HTTP), Flask/FastAPI (web), Scikit-learn (machine learning). Python runs scripts from the terminal: python script.py. The REPL (interactive interpreter) lets you test code line by line. Error types: SyntaxError (typo in code structure), TypeError (wrong data type), IndexError (out-of-range access), KeyError (missing dictionary key). Debugging: print() statements, breakpoint(), or an IDE debugger. Learning path: variables, control flow, functions, lists/dicts, file I/O, then a project.`,
    createdAt: Date.now(),
  },
  {
    id: 'kb-prog-javascript-web',
    title: 'JavaScript and Web Development Fundamentals',
    category: 'Programming',
    keywords: ['javascript', 'web development', 'coding', 'HTML', 'CSS', 'Node.js', 'React', 'DOM', 'async', 'fetch', 'npm', 'discord.js', 'programming', 'frontend', 'typescript'],
    content: `JavaScript (JS) is the only programming language that runs natively in web browsers, making it essential for front-end web development. It is also used server-side via Node.js. Variables: let (block-scoped, reassignable), const (block-scoped, not reassignable), var (function-scoped, legacy — avoid). Data types: number, string, boolean, null, undefined, object, symbol, BigInt. Functions can be declared (function add(a,b){return a+b}) or expressed as arrow functions (const add = (a,b) => a+b). Arrays and objects are reference types. DOM manipulation: JavaScript can modify HTML and CSS dynamically — document.getElementById(), querySelector(), addEventListener(). This is what makes pages interactive. Events: click, keydown, submit, load — attach handlers with addEventListener. Asynchronous JS: JavaScript is single-threaded but handles async operations through the event loop. Promises represent future values. async/await makes async code look synchronous. fetch() is the standard way to make HTTP requests. The frontend ecosystem: HTML provides structure, CSS provides styling, JS provides behaviour. React, Vue, and Angular are JS frameworks for building complex UIs. npm (Node Package Manager) installs libraries: npm install express. Node.js enables JS on the server, powering APIs and Discord bots (discord.js library). TypeScript adds static typing to JavaScript — catches bugs at compile time. Browser DevTools (F12) allow live debugging, network monitoring, and performance profiling. The modern JS ecosystem moves fast — focus on fundamentals first, frameworks second.`,
    createdAt: Date.now(),
  },
  {
    id: 'kb-prog-git-version-control',
    title: 'Git and Version Control: The Fundamentals',
    category: 'Programming',
    keywords: ['git', 'version control', 'programming', 'GitHub', 'commit', 'branch', 'merge', 'pull request', 'repository', 'coding', 'developer', 'open source', 'push', 'clone'],
    content: `Git is the industry-standard version control system — it tracks every change to every file in a project, allows multiple people to work simultaneously, and enables rolling back mistakes. Created by Linus Torvalds in 2005 for Linux kernel development. Core concepts: Repository (repo) — the project folder tracked by git. Commit — a snapshot of the project at a point in time, with a message describing the change. Branch — an independent line of development; the default is called main or master. Branching allows working on features without affecting the main codebase. Merge — combining one branch into another. Essential commands: git init — create a new repo. git clone <url> — copy an existing repo. git status — see modified/untracked files. git add <file> — stage a file for commit (or git add . for all). git commit -m "message" — save a snapshot. git push — upload commits to a remote repo (GitHub, GitLab). git pull — download and merge remote changes. git branch <name> — create a branch. git checkout <branch> — switch branches. git merge <branch> — merge a branch in. git log — view commit history. Merge conflicts occur when two branches edit the same line — you must manually resolve them and then commit the resolution. GitHub is the most popular remote hosting platform for git repos. Pull Requests (PRs) on GitHub are proposals to merge a branch — they enable code review and discussion before integration. .gitignore specifies files git should not track (passwords, build artifacts, node_modules). Commit messages should be descriptive and in imperative mood: "Add user authentication" not "Added stuff".`,
    createdAt: Date.now(),
  },
  {
    id: 'kb-prog-dsa-essentials',
    title: 'Data Structures and Algorithms: The Essentials',
    category: 'Programming',
    keywords: ['algorithms', 'data structures', 'programming', 'array', 'linked list', 'hash map', 'tree', 'graph', 'big O', 'sorting', 'binary search', 'recursion', 'dynamic programming', 'interview'],
    content: `Data structures and algorithms (DSA) form the theoretical foundation of computer science and are the primary topic in technical interviews at major tech companies. Core data structures: Array — ordered collection, O(1) random access, O(n) search. Linked List — nodes with pointers to next node; O(1) insert/delete at head, O(n) access. Stack — Last In First Out (LIFO); operations: push, pop, peek. Queue — First In First Out (FIFO); operations: enqueue, dequeue. Hash Map/Dictionary — key-value pairs with O(1) average lookup via hashing. Tree — hierarchical structure; Binary Search Tree (BST) enables O(log n) search if balanced. Graph — nodes connected by edges; can model networks, maps, social connections. Heap — tree-based structure where parent is always larger (max-heap) or smaller (min-heap) than children; enables O(1) access to min/max element. Algorithm complexity — Big O notation describes how runtime or space grows with input size n: O(1) constant, O(log n) logarithmic, O(n) linear, O(n log n) linearithmic (merge sort), O(n²) quadratic (bubble sort). Sorting: Merge Sort — divide and conquer, O(n log n) guaranteed, stable. Quick Sort — O(n log n) average, O(n²) worst case, in-place. Heap Sort — O(n log n), in-place. Searching: Binary Search — O(log n) on sorted arrays. Graph traversal: BFS (Breadth-First Search) — explores level by level; DFS (Depth-First Search) — explores as deep as possible first. Recursion — a function that calls itself; requires a base case to terminate. Dynamic Programming — breaking problems into overlapping subproblems and caching results.`,
    createdAt: Date.now(),
  },
  {
    id: 'kb-prog-apis-rest-internet',
    title: 'APIs, REST, and How the Internet Communicates',
    category: 'Programming',
    keywords: ['API', 'REST', 'HTTP', 'JSON', 'programming', 'web development', 'authentication', 'OAuth', 'JWT', 'webhook', 'discord', 'rate limit', 'fetch', 'requests', 'endpoint'],
    content: `An API (Application Programming Interface) is a defined set of rules for how software components communicate. REST (Representational State Transfer) is the dominant architectural style for web APIs. A REST API communicates over HTTP using these methods: GET — retrieve data (no side effects). POST — create a new resource. PUT/PATCH — update a resource. DELETE — remove a resource. Resources are identified by URLs: GET /users returns all users; GET /users/42 returns user with ID 42. HTTP status codes communicate results: 200 OK, 201 Created, 400 Bad Request, 401 Unauthorised, 403 Forbidden, 404 Not Found, 500 Internal Server Error. Data is typically exchanged as JSON (JavaScript Object Notation) — a lightweight, human-readable format using key-value pairs, arrays, and nested objects. Authentication: API keys (sent in headers) authenticate automated requests. OAuth 2.0 is the standard for user-delegated access (logging in with Google, GitHub). JWT (JSON Web Token) is a common token format for stateless authentication. Rate limiting restricts how many requests a client can make in a time window — respect these or your IP gets blocked. Webhooks are the reverse of polling: instead of repeatedly asking "any new data?", the server POSTs to your URL when something happens. Critical for Discord bots. Tools: curl tests APIs from the terminal. Postman is a GUI for API testing. The fetch() function in JavaScript and the requests library in Python are standard HTTP clients. API documentation (Swagger/OpenAPI) describes all available endpoints, parameters, and response formats.`,
    createdAt: Date.now(),
  },
];
