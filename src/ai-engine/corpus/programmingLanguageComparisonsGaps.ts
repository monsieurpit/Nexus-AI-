import { KnowledgeItem } from '../../types';

// Batch 308 corpus fixes — sixth coding-priority batch (2026-09-14), coding again after
// batch 307 (cocktails/spirits, non-coding). Topic: direct pairwise "difference between X and Y"
// comparisons of programming languages. Grep confirmed zero existing "vs"/"difference between"
// style entries for any language pair anywhere in the corpus, despite systemsLanguagesCorpus.ts,
// jvmAndCSharpCorpus.ts, programmingLanguagesOverview.ts, pythonFundamentalsCorpus.ts,
// javascriptTypescriptCorpus.ts, otherLanguagesCorpus.ts, additionalLanguagesCorpus.ts and
// webAndDataLanguagesCorpus.ts each covering individual languages in depth — the gap was
// specifically the head-to-head contrast angle, not the languages themselves.
// 10/25 misses. Several severe wrong-domain hallucinations: Go vs Rust answered with the German
// MUNICIPALITY of Rust (Baden-Württemberg, Europa-Park) and the tabletop RPG setting "Scarred
// Lands", never touching either language; Swift vs Objective-C dumped an unrelated native-app-vs-
// web-app corpus entry, never explaining either language; Zig vs Rust defined Zig then derailed
// into a generic "option type" type-theory Wikipedia dump, never mentioning Rust; Groovy vs Java
// dumped a raw Wikipedia definition of Groovy followed by a "Java topical outline" trivia dump,
// never actually contrasting the two languages. Several one-sided cutoffs that explained only one
// side of the pair and never reached the other: C vs C++ (explained C only), Rust vs C++
// (mockingly described C++'s messiness but never explained Rust's own ownership/borrow-checker
// approach in contrast), Java vs C# (explained the JVM/bytecode side of Java only, cut off before
// ever mentioning C#), C++ vs Java (same JVM/bytecode fragment, cut off before C++), Visual Basic
// vs C# (explained VB's history only, never reached C#), MATLAB vs Python (explained MATLAB's
// matrix-first design only, never reached Python).

const now = Date.now();
const k = (id: string, title: string, keywords: string[], content: string): KnowledgeItem => ({
  id,
  title,
  category: 'Programming',
  keywords,
  content,
  createdAt: now,
});

export const PROGRAMMING_LANGUAGE_COMPARISONS_GAPS: KnowledgeItem[] = [
  k(
    'kb-gap-go-vs-rust',
    'Go vs Rust',
    ['go vs rust', 'golang vs rust', 'difference between go and rust', 'rust vs golang'],
    "Go and Rust are both modern, compiled, statically-typed languages built with performance and reliability in mind, but they make almost opposite tradeoffs to get there. Go prioritizes simplicity and fast compilation: it has a small language spec, garbage collection (so the runtime automatically frees memory you're done with, no manual bookkeeping), and lightweight built-in concurrency via goroutines (cheap, runtime-managed green threads) and channels for passing data between them, summed up in the Go proverb \"don't communicate by sharing memory, share memory by communicating.\" This makes Go fast to learn and fast to build things in, which is why it's the language behind Docker, Kubernetes, and most cloud infrastructure tooling. Rust prioritizes maximum control and safety without a garbage collector: memory is managed through its ownership system, enforced at compile time by the borrow checker — every value has exactly one owner, and the compiler tracks when values are borrowed or move, catching use-after-free, double-free, and data-race bugs before the program ever runs, with zero runtime overhead. This makes Rust harder to learn (\"fighting the borrow checker\" is a rite of passage) but gives performance on par with C/C++ with strong safety guarantees, which is why it's favored for systems programming, browser engines (Firefox's Servo), and performance-critical infrastructure (parts of the Linux kernel now accept Rust). The key difference: Go trades some runtime performance for simplicity and easy concurrency via a garbage collector and goroutines, while Rust trades a steeper learning curve for C-level performance and compile-time-enforced memory/thread safety with no garbage collector at all.",
  ),
  k(
    'kb-gap-c-vs-cpp',
    'C vs C++',
    ['c vs c++', 'difference between c and c++', 'c++ vs c'],
    "C is a small, procedural language from the early 1970s that gives extremely direct, low-level control over memory and hardware, but nothing more — there are no classes, no built-in objects, no exceptions, no templates/generics, no standard containers beyond raw arrays; you write plain functions that operate on plain data (structs), and you manage all memory manually with malloc/free. C++, created as an extension of C in the early 1980s (originally called \"C with Classes\"), keeps C's low-level control and adds a large layer of higher-level features on top: object-oriented programming (classes, inheritance, virtual functions/polymorphism), templates for writing generic, type-safe code once and reusing it for many types, exceptions for structured error handling, operator overloading, and the Standard Template Library (STL) — ready-made generic containers like `std::vector` and `std::map` plus generic algorithms that work over them. C++ is very close to being a superset of C (most valid C compiles as C++, with some exceptions), so C++ code can still drop down to raw pointers and manual memory management exactly like C when needed, but it also offers RAII (tying resource cleanup to object lifetime) and smart pointers (`std::unique_ptr`, `std::shared_ptr`) as safer alternatives to manual malloc/free. The key difference: C is a minimal procedural language offering only manual, low-level control, while C++ builds object-oriented programming, generics, exceptions, and a rich standard library of containers/algorithms on top of that same low-level foundation.",
  ),
  k(
    'kb-gap-rust-vs-cpp',
    'Rust vs C++',
    ['rust vs c++', 'difference between rust and c++', 'c++ vs rust'],
    "Rust and C++ are both compiled languages capable of C-level performance with no garbage collector, and both are used for systems programming, game engines, and performance-critical infrastructure — but they take fundamentally different approaches to memory safety. C++ gives the programmer full manual control over memory (raw pointers, `new`/`delete`, or safer RAII/smart-pointer patterns like `std::unique_ptr`) but nothing in the language stops you from writing a use-after-free, a double-free, a dangling reference, or a data race between threads — these bugs are only caught (if ever) at runtime, by tools like sanitizers, or in production as crashes/security vulnerabilities. Rust's ownership system and borrow checker enforce memory and thread safety at COMPILE time instead: every value has one owner, references are checked so that a value can't be freed while something still borrows it, and the compiler statically rejects code with potential data races — code that would compile fine in C++ (and might crash later) simply fails to compile in Rust until the unsafe pattern is fixed. This makes C++ more flexible and mature (a much older ecosystem, more libraries, more legacy codebases, an easier time expressing certain patterns the borrow checker resists) but historically more prone to memory-safety bugs and security CVEs, while Rust is stricter and has a steeper initial learning curve (fighting the borrow checker) in exchange for eliminating whole categories of memory bugs before the program ever runs, with an `unsafe` keyword available as an explicit escape hatch when you truly need C-like manual control. The key difference: C++ trusts the programmer with manual memory management and catches unsafe memory/thread bugs (if at all) at runtime, while Rust's ownership system and borrow checker enforce memory and thread safety at compile time, with no garbage collector in either language.",
  ),
  k(
    'kb-gap-java-vs-csharp',
    'Java vs C#',
    ['java vs c#', 'difference between java and c#', 'c# vs java', 'java vs csharp'],
    "Java and C# are close cousins — both are statically-typed, garbage-collected, object-oriented languages that compile to an intermediate bytecode (Java bytecode for the JVM, Common Intermediate Language for C#'s .NET runtime, the CLR) rather than directly to machine code, which is what gives both their \"write once, run anywhere\" portability. C# was created by Microsoft in the early 2000s partly in response to Java, and it deliberately improved on several rough edges: C# has built-in properties (getters/setters without boilerplate methods), first-class events and delegates (function references) baked into the language, LINQ (a built-in query syntax for filtering/transforming collections and data inline), and nullable reference types to help catch null-reference bugs at compile time — features Java either lacks natively or only added much later and less elegantly (Java added lambdas/streams in Java 8, years after C#'s LINQ). Historically Java was platform-independent by design from day one (the JVM runs on virtually any OS) while C# and its .NET runtime were originally Windows-only, though modern .NET (since .NET Core, .NET 5+) is now fully cross-platform too, narrowing that historical gap. Ecosystem-wise, Java dominates enterprise backend systems, Android development (via Kotlin/Java), and big data tooling (Hadoop, Kafka are JVM-based), while C# is the default choice for Windows desktop apps, ASP.NET web backends, and is the primary scripting language for the Unity game engine. The key difference: they're both JVM/CLR-style bytecode-compiled OOP languages, but C# generally offers more modern built-in language features (properties, LINQ, delegates) that Java added later or not at all, while Java has broader historical platform independence and a larger footprint in enterprise/Android/big-data ecosystems.",
  ),
  k(
    'kb-gap-swift-vs-objc',
    'Swift vs Objective-C',
    ['swift vs objective-c', 'difference between swift and objective-c', 'objective-c vs swift', 'objc vs swift'],
    "Objective-C is Apple's original language for building Mac and (later) iOS apps, dating back to the 1980s — it's essentially the C language with an Smalltalk-style object-oriented layer bolted on top, using square-bracket \"message-sending\" syntax like `[myArray addObject:item]` rather than the dot-method syntax most modern languages use. Because it's built directly on C, it inherits C's manual pointer handling and verbose syntax, uses separate header (`.h`) and implementation (`.m`) files like C, and historically required manual memory management (retain/release counts) before Automatic Reference Counting (ARC) was added in 2011 to handle that automatically. Swift, introduced by Apple in 2014, was designed from scratch specifically to replace Objective-C: it has modern, concise syntax closer to languages like Kotlin or Python (dot-syntax method calls, type inference so you rarely have to write out types explicitly, no separate header files needed), and it bakes safety directly into the type system with Optionals — a value's type explicitly states whether it can be `nil`/absent (`String?`) or never can (`String`), which the compiler enforces, eliminating a huge class of the null-pointer crashes that were common in Objective-C. Swift is also significantly faster to execute than Objective-C in most benchmarks (closer to C++ performance) while still interoperating directly with existing Objective-C code and Apple's Objective-C-based frameworks, which is why large existing Objective-C codebases don't need a full rewrite to start adopting Swift. The key difference: Objective-C is C with an older, verbose Smalltalk-style object layer bolted on and no compile-time nil-safety, while Swift is a modern, concise, type-safe language built from scratch by Apple with Optionals to catch null-related bugs at compile time, and better performance, while still interoperating with existing Objective-C code.",
  ),
  k(
    'kb-gap-cpp-vs-java',
    'C++ vs Java',
    ['c++ vs java', 'difference between c++ and java', 'java vs c++'],
    "C++ and Java are both object-oriented languages with C-family syntax (curly braces, semicolons), but they sit at very different points on the control-vs-safety spectrum. C++ compiles directly to native machine code and gives the programmer manual control over memory — you allocate with `new` and must free with `delete` (or use RAII/smart pointers to automate that safely), and it supports low-level features like raw pointers, pointer arithmetic, and multiple inheritance (a class can directly inherit from more than one base class). Java compiles instead to portable bytecode that runs on the Java Virtual Machine (JVM), which is what gives it \"write once, run anywhere\" — the same compiled `.class` file runs unmodified on Windows, Mac, or Linux as long as a JVM is installed, whereas C++ must generally be recompiled per target platform/architecture. Java also uses automatic garbage collection (the JVM tracks object references and frees memory for you, with no manual free/delete ever needed) instead of C++'s manual/RAII-based management, has no raw pointers exposed to the programmer at all (only references, which can't be manipulated like pointer arithmetic), and restricts a class to inheriting from only one parent class directly (using interfaces instead of multiple inheritance to share behavior across unrelated class hierarchies). The tradeoff: C++'s manual control and native compilation generally make it faster and give it a smaller runtime footprint, useful for game engines, embedded systems, and performance-critical software, while Java's garbage collection, portability, and simpler inheritance model make it easier to write safely and deploy consistently across platforms, which is why it's a mainstay of enterprise backend systems and Android development. The key difference: C++ compiles to native code with manual memory management and multiple inheritance, while Java compiles to portable JVM bytecode with automatic garbage collection and single class inheritance (plus interfaces).",
  ),
  k(
    'kb-gap-zig-vs-rust',
    'Zig vs Rust',
    ['zig vs rust', 'difference between zig and rust', 'rust vs zig'],
    "Zig and Rust are both modern systems-programming languages positioned as safer/better alternatives to C, but they take almost opposite philosophies to get there. Zig deliberately keeps the language small and simple, with a stated goal of being basically \"a better C\": no hidden control flow, no hidden memory allocations (a function that allocates memory must be explicitly passed an allocator, making allocation always visible at the call site), no garbage collector, no exceptions, and — notably — no borrow checker at all; memory safety in Zig comes from simpler mechanisms like optional types (`?T`) to force handling of absent values, explicit error unions (`!T`) for error handling, and runtime safety checks (like bounds-checking and overflow-checking) that are on by default in debug/safe builds but can be stripped for release performance. Rust instead adds a much more sophisticated compile-time ownership system and borrow checker that statically proves memory safety and the absence of data races before the program can even compile, at the cost of a notably steeper learning curve and a much larger, more complex language (traits, lifetimes, generics with trait bounds). Zig also has first-class, seamless interoperability with existing C code — a Zig program can `@cImport` C header files directly and call C functions with no bindings/wrapper layer required, and Zig can even be used as a drop-in C/C++ compiler — while Rust's C interop requires an `unsafe` FFI (foreign function interface) boundary with more ceremony. The key difference: Rust guarantees memory and thread safety through a complex compile-time ownership/borrow-checker system, while Zig stays deliberately simpler and closer to C, favoring explicitness (manual allocators, no hidden control flow) and optional runtime safety checks over a borrow checker, with much easier direct interop with existing C code.",
  ),
  k(
    'kb-gap-vb-vs-csharp',
    'Visual Basic vs C#',
    ['visual basic vs c#', 'vb.net vs c#', 'difference between visual basic and c#', 'c# vs visual basic'],
    "Visual Basic (in its modern form, VB.NET) and C# are both first-class languages on Microsoft's .NET platform, compiling down to the same Common Intermediate Language (CIL) and running on the same Common Language Runtime (CLR) — meaning a VB.NET project and a C# project can reference each other's compiled code freely, and the two languages have essentially equivalent capability and performance under the hood. The real difference is syntax and style, inherited from their very different histories: Visual Basic descends from the original BASIC language and Microsoft's earlier VB6, so it favors English-like, verbose keywords over symbols — blocks are opened and closed with words like `If...Then...End If` or `Sub...End Sub` rather than curly braces `{}`, statements typically don't need a terminating semicolon, and comparisons/assignment both traditionally use a single `=`. C# instead uses C-family syntax (curly braces for blocks, semicolons to end statements, `==` for comparison vs `=` for assignment), which is closer to Java, JavaScript, and C++ and is generally considered more concise, if less immediately readable to a total beginner. In practice, C# is now overwhelmingly the more popular and actively developed of the two — it gets new .NET language features first (or exclusively), has by far the larger developer community, and is the language Microsoft itself uses for most modern .NET documentation and tooling (ASP.NET, Unity scripting, etc.), while VB.NET is mostly still found maintaining older, legacy enterprise applications rather than in new projects. The key difference: both run on the same .NET runtime with equivalent capability, but Visual Basic uses verbose, English-like keyword syntax (no curly braces) inherited from classic BASIC, while C# uses concise, C-family curly-brace syntax and is by far the more actively used and developed of the two today.",
  ),
  k(
    'kb-gap-groovy-vs-java',
    'Groovy vs Java',
    ['groovy vs java', 'difference between groovy and java', 'java vs groovy'],
    "Groovy is a language that runs on the Java Virtual Machine (JVM) and is deliberately designed to be almost a superset of Java's syntax — most valid Java code is also valid Groovy code, and Groovy can call Java libraries directly and be called from Java, since both compile to the same JVM bytecode. Where Groovy differs is in adding a large layer of scripting-style convenience on top of that Java-compatible base: it supports dynamic typing (you can declare a variable with `def` and skip specifying its type, with type checks happening at runtime instead of compile time, unlike Java's strict static typing), much less boilerplate (no semicolons required, optional parentheses for method calls, automatic getters/setters generated for class fields, a more forgiving and terse syntax overall), built-in support for closures (blocks of code that can be passed around as values, similar to Java's lambdas but more flexible, predating Java 8's lambda support by years), and native, easy syntax for working with collections, lists, maps, and regular expressions. Groovy can optionally still be statically typed and compiled ahead-of-time (using `@CompileStatic` and its own compiler) for near-Java performance when needed, but its default dynamic mode trades some raw performance and compile-time error catching for much faster, more expressive scripting — which is why Groovy became the standard scripting language for build tools like Gradle and CI pipelines like Jenkins (Jenkinsfiles), where quick, flexible scripting matters more than raw execution speed. The key difference: Groovy runs on the same JVM as Java and is nearly syntax-compatible with it, but adds optional dynamic typing, closures, and much less boilerplate for faster, more flexible scripting, at some cost to the strict compile-time type safety and raw performance of plain statically-typed Java.",
  ),
  k(
    'kb-gap-matlab-vs-python',
    'MATLAB vs Python',
    ['matlab vs python', 'difference between matlab and python', 'python vs matlab'],
    "MATLAB is a proprietary, paid numerical-computing environment and language built from the ground up around matrices — literally every value in MATLAB, even a single number, is internally treated as a 1x1 matrix, and the language's syntax and built-in functions are optimized specifically for linear algebra, signal processing, and matrix/vector operations (array indexing starts at 1, not 0, unlike almost every other modern language, and many operations that need explicit library calls elsewhere are simply built into the core language). It ships as a single closed, all-in-one commercial product with its own IDE, plotting tools, and paid add-on toolboxes for specific domains (control systems, image processing, etc.), historically dominant in academia, engineering, and signal-processing research. Python is a free, open-source, general-purpose language not originally built for numerical computing at all — its scientific/numerical capability instead comes from external open-source libraries layered on top: NumPy provides fast array/matrix operations (closing much of the raw performance and syntax gap with MATLAB for numeric code), pandas adds data-table/dataframe handling, and Matplotlib/others handle plotting, with SciPy, scikit-learn, PyTorch, and TensorFlow extending it further into general scientific computing and machine learning. Because Python is free, general-purpose, and has an enormous open-source ecosystem well beyond just numerical work (web backends, scripting, automation, data engineering), it has largely overtaken MATLAB as the default choice for new data science and machine learning work, while MATLAB remains entrenched in certain engineering/academic niches (control systems design, hardware-in-the-loop testing, university coursework) partly due to institutional licensing and decades of existing toolboxes. The key difference: MATLAB is a paid, closed, matrix-first environment with numerical computing built into the core language (1-based indexing included), while Python is a free, open-source, general-purpose language whose numerical/scientific power comes from add-on libraries like NumPy and pandas rather than being native to the language itself.",
  ),
];
